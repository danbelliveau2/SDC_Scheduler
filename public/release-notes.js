// SDC Project Scheduler — release notes.
// Newest entry first. Each entry: { version, date (YYYY-MM-DD), notes: string[] }.
// The topbar revision pill reads the FIRST entry's version + date and shows the full
// array in the click popup. Edit this file directly when bumping the rev.
window.RELEASE_NOTES = [
  {
    version: '5.1',
    date: '2026-05-22',
    notes: [
      'Task-name editor stays inside the name area + backlog duration picker styled.',
      '',
      'NAME-EDIT FIX: when you click a task name to rename it, the input (and its text-selection highlight) used to stretch across the entire Task column — covering up the allocation %, the duration, and the % complete pill on the right. The actual editable text was only the middle portion, which made the wider highlight look broken.',
      '',
      '   ▸ enterCellEdit now special-cases the NAME column: it swaps only the .name-cell-main span for the input instead of wiping the whole td. The alloc, dash, dur, and % pill stay visible during edit.',
      '   ▸ The input is positioned absolute at left:64 right:110 (mirroring .name-cell-main) so its width and selection highlight exactly match the actual name area.',
      '   ▸ When the alloc column is hidden (View pill toggle), the input shifts to left:8 to follow .name-cell-main.',
      '',
      'BACKLOG DURATION PICKER: the inline <select> dropdown on the Backlog row got explicit styling (border, hover/focus rings, accent background on hover, ≥64 px min-width). Native form themes on Windows render selects very flatly — without the dedicated styling, the dropdown could read as static text. Now it clearly looks like an interactive control. Pick 1w / 2w / ... / 12w or "Custom…" for a typed value.',
    ],
  },
  {
    version: '5.0',
    date: '2026-05-22',
    notes: [
      'Bar meta cascade — settled. Debug markers removed.',
      '',
      'Over v4.85-v4.98 the bar-meta layout went through 14 iterations chasing a single bug: the centered name visually overlapped the inside-left meta on narrow bars. The cascade math was right the whole time; the chain of failures was about MEASUREMENT and APPLICATION.',
      '',
      '   ▸ Measurement: every browser API for SVG text width (getBBox, getComputedTextLength, getBoundingClientRect, canvas measureText with various fonts) was under-reporting on at least some bars. Settled on a per-character lookup for the meta (it\'s bounded text — "XX% · YYw"), plus reading the bar-label\'s actual rendered rect for the name.',
      '   ▸ Application: setAttribute("x", ...) on the bar-label was updating the SVG DOM (getBoundingClientRect returned the new position) but the rendered glyphs visually stayed at the old centered position — paint and layout disagreed. Switched Step 2\'s name shift to CSS `transform: translateX(...)`, which hooks into the paint pipeline directly.',
      '',
      'FINAL CASCADE:',
      '   STEP 1 — meta inside-left, name centered IN BAR. Used when the centered name has ≥3 px from the meta-right AND ≥3 px from the bar-right.',
      '   STEP 2 — meta inside-left, name shifted right to be centered between meta-right and bar-right (symmetric gaps). Triggers when Step 1 would overlap. Shift applied via CSS transform.',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar. Triggers when the bar is too narrow to fit both inside but the name still fits centered in the bar alone.',
      '   STEP 4 — both outside. Meta outside-left, name ALWAYS outside-right.',
      '',
      'v5.0 strips the debug fill colors and the magenta/blue alignment ticks now that the cascade is verified working.',
    ],
  },
  {
    version: '4.97',
    date: '2026-05-22',
    notes: [
      'Bar meta — debug ticks now show the FINAL positions after the cascade ran.',
      '',
      'BUG IN v4.96 DEBUG: the blue tick was drawn BEFORE the cascade had a chance to move the name (Step 2). So even when Step 2 shifted the name to clear the meta, the blue tick was still showing the pre-shift centered position — looked like the cascade did nothing.',
      '',
      'FIX:',
      '   ▸ Restructured the cascade as an if/else chain with a `chosenStep` variable instead of early `continue` returns.',
      '   ▸ Ticks are drawn AFTER the cascade decision, and the blue tick re-reads the bar-label\'s rect at that point so it lands at the name\'s real final left edge.',
      '   ▸ Magenta tick now only renders for Step 1 and Step 2 (meta inside). For Step 3/4 the meta is outside and the "meta-right inside the bar" position is meaningless.',
      '',
      'When you refresh, the blue tick should always land to the RIGHT of the magenta tick whenever the meta is inside the bar. If you see blue left of magenta, the cascade is genuinely failing to trigger Step 2 and we have a different bug to chase.',
    ],
  },
  {
    version: '4.96',
    date: '2026-05-22',
    notes: [
      'Bar meta — calibrated per-char width + blue name-left tick.',
      '',
      'TWO CHANGES based on v4.95 visual check:',
      '',
      '1. PER-CHAR WIDTHS trimmed ~20 %. v4.95\'s table (digits 7, % 8, space 3, · 4, w 8) put the magenta tick well past the rendered meta\'s right edge. New values (digits 5.5, % 6.5, space 2.5, · 3, w 6.5) put the tick right at the edge of the rendered text.',
      '',
      '2. BLUE DEBUG TICK — where the code thinks the name\'s left edge actually is, read from getBoundingClientRect on the bar-label. Use it side-by-side with the magenta tick: if BLUE is left of MAGENTA (or within 3 px), the name overlaps the meta and Step 2 should fire. If BLUE is well past MAGENTA, Step 1 is correct to leave the name centered.',
      '',
      'Hide both via `window.SDC_HIDE_BARMETA_DEBUG = true` in the console once we\'re done verifying.',
      '',
      'Cascade unchanged — the algorithm is correct, just calibrating the meta-width inputs.',
    ],
  },
  {
    version: '4.95',
    date: '2026-05-22',
    notes: [
      'Bar meta — per-char width estimate + visual debug tick.',
      '',
      'TWO CHANGES:',
      '',
      '1. META WIDTH — per-character lookup, not browser APIs.',
      '   Every browser/canvas measurement API returned widths smaller than what was actually rendered on screen. Switched to a per-char table calibrated for any common bold-9 px sans-serif: digits = 7 px, "%" = 8 px, space = 3 px, "·" = 4 px, "w" = 8 px, default 7 px. Plus 3 px for the meta\'s 2.5 px stroke halo. Deterministic, doesn\'t depend on which font the browser picks.',
      '',
      '2. VISUAL DEBUG TICK on each bar.',
      '   A thin magenta dashed line is now drawn on every bar at the position where the code believes the meta\'s right edge lands. If the actual rendered meta text extends PAST the tick, the per-char estimate is too small and we know to bump it. Hides via `window.SDC_HIDE_BARMETA_DEBUG = true` in the console once verified.',
      '',
      'The name\'s width is still read live from the bar-label\'s real rendered rect (getBoundingClientRect) — that part has always been reliable because clipBarLabels has already placed the bar-label by the time drawBarMeta runs.',
      '',
      'CASCADE (unchanged from v4.94 — algorithm has been correct since v4.85, the bug was always meta-width measurement):',
      '   STEP 1 — meta inside-left, name centered IN BAR.',
      '   STEP 2 — meta inside-left, name centered between meta-right and bar-right.',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar.',
      '   STEP 4 — both outside.',
    ],
  },
  {
    version: '4.94',
    date: '2026-05-22',
    notes: [
      'Bar meta — read the name\'s actual position, stop predicting it.',
      '',
      'CONTEXT: across v4.85-v4.93 the bug was the same. My width measurements for the SVG <text> elements were UNDER-REPORTING vs what the browser actually rendered. Every API I tried — getBBox, getComputedTextLength, getBoundingClientRect, canvas measureText with various fonts — returned widths smaller than reality on at least some bars. The overlap check then said "no overlap" when there clearly was one, and the name stayed centered on top of the meta.',
      '',
      'In v4.93 I tried to force the meta to render in "sans-serif" so canvas measureText would match. The font-family attribute I set on the SVG element was getting OVERRIDDEN by frappe-gantt\'s CSS rule on .gantt text. The SVG still rendered in the document\'s font stack while canvas measured Arial. Same 15-20 px discrepancy. Same overlap.',
      '',
      'v4.94 STOPS PREDICTING THE NAME\'S POSITION. By the time drawBarMeta runs, clipBarLabels has already placed the bar-label and the browser has laid it out. So we READ the bar-label\'s real rendered rect via getBoundingClientRect (reliable on already-laid-out elements) and compare it to the bar\'s rect:',
      '',
      '   ▸ availLeftPx = nameRect.left - barRect.left → the space the meta can use.',
      '   ▸ availRightPx = barRect.right - nameRect.right → for the Step 2 fit check.',
      '   ▸ Step 1 fires if availLeftPx ≥ metaW + INSIDE_PADDING + INSIDE_GAP.',
      '   ▸ No more "what would the centered name look like" prediction — we know exactly where it is.',
      '',
      'The ONLY width I still measure is the meta itself, via canvas measureText. The meta text is bounded (max "100% · 99w" ≈ 10 chars) so any reasonable measurement + the +5 px stroke+safety margin covers it. Plus we also set the meta\'s font-family via INLINE STYLE this time, which beats CSS specificity — so SVG and canvas finally do render in the same font.',
      '',
      'CASCADE (unchanged behaviorally):',
      '   STEP 1 — meta inside-left, name centered IN BAR.',
      '   STEP 2 — meta inside-left, name RE-centered between meta-right and bar-right.',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar.',
      '   STEP 4 — both outside.',
    ],
  },
  {
    version: '4.93',
    date: '2026-05-22',
    notes: [
      'Bar meta — accurate width measurement, tight 3 px gap.',
      '',
      'CONTEXT: the cascade logic has been correct since v4.85 — "leave the name centered until it overlaps the meta, then shift to centered-between-meta-and-bar-right." What kept failing was WIDTH MEASUREMENT.',
      '',
      'WHY measurement kept failing: the SVG <text> meta was inheriting the document\'s font-family stack (-apple-system, Segoe UI, Roboto, etc.) but canvas measureText\'s "sans-serif" resolves to a different OS-default font (Arial on Windows, Helvetica on Mac). Same text, different font, 15-25 px width discrepancy. My overlap check used the canvas-measured width, said "no overlap," and the name landed on top of the meta.',
      '',
      'v4.92 fixed the overlap by taking the MAX of 5 measurement methods plus a chars × 8 px floor — guaranteed no underestimate, but overestimated by 15-25 px on most bars (the "big gap" report).',
      '',
      'v4.93 FIX:',
      '   ▸ Force the meta to render in "sans-serif" explicitly (font-family attribute on the SVG element). No more font-stack inheritance.',
      '   ▸ Canvas measureText with "bold 9px sans-serif" now matches the SVG render exactly because both sides resolve to the same OS-default sans.',
      '   ▸ Drop the 5-methods-max + char-count floor + +8 safety margin paranoia. Just trust canvas measureText + 2.5 px for the meta\'s stroke halo (which is real width the rendered glyphs occupy).',
      '   ▸ Result: 3 px gap between meta-right and name-left, every time.',
    ],
  },
  {
    version: '4.92',
    date: '2026-05-21',
    notes: [
      'Bar meta — belt-and-braces width measurement.',
      '',
      'Every single measurement technique tried across v4.85-v4.91 (getBBox, getComputedTextLength, getBoundingClientRect, canvas measureText with computed-style font, canvas measureText with matched font-family) has under-reported widths on at least some bars. v4.92 stops betting on any single method.',
      '',
      'NEW WIDTH MEASUREMENT — run FIVE methods and take the MAX:',
      '   ▸ A: canvas measureText with the element\'s computed font',
      '   ▸ B: canvas measureText with hardcoded "bold/normal Xpx sans-serif"',
      '   ▸ C: getBoundingClientRect',
      '   ▸ D: getComputedTextLength',
      '   ▸ E: getBBox',
      '   ▸ F (floor): char-count × 8 px char',
      '',
      'Plus a +8 px safety margin (was +5 in v4.91). The combination makes it mathematically impossible to under-estimate the rendered width — whichever method is broken or under-reporting, the others fill in.',
      '',
      'DIAGNOSTIC: open DevTools and run `window.SDC_DEBUG_BARMETA = true`. The next render logs per-bar measurements so we can see exactly what each method returned. Helps verify the fix is doing what it claims.',
      '',
      'CASCADE (unchanged):',
      '   STEP 1 — meta inside-left, name centered IN BAR.',
      '   STEP 2 — meta inside-left, name centered between meta-right and bar-right.',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar.',
      '   STEP 4 — both outside.',
    ],
  },
  {
    version: '4.91',
    date: '2026-05-21',
    notes: [
      'Bar meta — meta now renders in the SAME FONT as the bar-label.',
      '',
      'ROOT CAUSE finally identified: across v4.85-v4.90, canvas measureText was measuring widths in ONE font, but the SVG was rendering the meta in a DIFFERENT (wider) font. The SVG <text> default font is a serif face in some browsers (Times-like) while canvas\'s "sans-serif" resolves to Arial/Helvetica (much narrower at the same point size). Canvas under-reported meta widths by 15-20 px, so Step 1 (centered name) fired on bars where the centered name was actually landing on top of the meta.',
      '',
      'FIX:',
      '   ▸ Read the bar-label\'s computed font-family (via window.getComputedStyle).',
      '   ▸ Force the meta\'s font-family attribute to the SAME value. Now both SVG <text> elements render in the same font.',
      '   ▸ Canvas measureText uses that same font-family. Widths now match what\'s rendered.',
      '   ▸ Safety margin bumped from +2 to +5 px to absorb any final font-metric drift, stroke padding, antialiasing.',
      '',
      'CASCADE (unchanged — was correct, just had bad inputs):',
      '   STEP 1 — meta inside-left, name centered IN BAR.',
      '   STEP 2 — meta inside-left, name centered between meta-right and bar-right.',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar.',
      '   STEP 4 — both outside.',
      '',
      'If it overlaps now, the font/measurement assumptions need yet another rethink — but the math is sound, and the measurements should finally be too.',
    ],
  },
  {
    version: '4.90',
    date: '2026-05-21',
    notes: [
      'Bar meta — canvas measureText (font-metric source of truth).',
      '',
      'v4.85-v4.89 all tried SVG-layout APIs (getBBox, getComputedTextLength, getBoundingClientRect). EVERY ONE of them was returning sizes smaller than the actual rendered text on at least some bars. The overlap check kept firing "no overlap" when there clearly was one. Name kept landing on top of the meta.',
      '',
      'FINAL FIX: switched to canvas measureText() with each element\'s computed font (extracted via window.getComputedStyle). Canvas measureText:',
      '   ▸ Works directly off the browser\'s font metrics — doesn\'t care if the SVG element has been laid out yet.',
      '   ▸ Returns synchronously with sub-pixel precision.',
      '   ▸ Doesn\'t require the element to be in the DOM (we use it because it\'s reliable, not because of DOM state).',
      '',
      'Conservative: take the MAX of (canvas measureText) and (getBoundingClientRect.width). If either method over-reports, we use the larger value — better to overshoot and have an extra-large gap than to overlap. +2 px safety margin on top of that absorbs any final sub-pixel variance, kerning, antialiasing, and the meta\'s 2.5 px stroke padding.',
      '',
      'Cascade applied via arithmetic with these conservative widths:',
      '   STEP 1 — meta inside-left, name centered IN BAR. Trigger: centered name has ≥3 px from meta-right AND ≥3 px from bar-right.',
      '   STEP 2 — meta inside-left, name centered between meta-right and bar-right. Trigger: (barRight - metaInsideRight) ≥ nameW + 6.',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar. Trigger: barW ≥ nameW + 6.',
      '   STEP 4 — both outside.',
    ],
  },
  {
    version: '4.89',
    date: '2026-05-21',
    notes: [
      'Bar meta — getBoundingClientRect as source of truth (fixes overlap, finally).',
      '',
      'Pattern across v4.85–v4.88: every SVG text-measurement API I tried (getBBox().width, getComputedTextLength()) was returning sizes smaller than the actual rendered text on at least some bars. The overlap check fired "no overlap" when there clearly was one, leaving the name centered on top of the meta. User saw the same overlap bug across 4 versions.',
      '',
      'FIX: getBoundingClientRect() reads the ACTUAL rendered rectangle from the browser\'s layout — it forces a sync layout flush and returns the real pixel-perfect bbox in CSS pixels. Now I use it for BOTH initial measurement AND post-placement verification. If a step\'s placement leaves the rendered name overlapping the rendered meta, the cascade advances to the next step.',
      '',
      'Pattern: place → measure rendered rect → check actual overlap → advance if needed.',
      '',
      'CASCADE (overlap detected from real rendered rects, not text-API guesses):',
      '   STEP 1 — meta inside-left at barX+3, name centered IN BAR. Stays IF rendered meta.right + 3 ≤ rendered name.left AND rendered name.right + 3 ≤ rendered bar.right.',
      '   STEP 2 — meta inside-left, name centered between meta-right and bar-right. Stays IF same checks pass after re-placement.',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar. Stays IF rendered name fits in bar with 3 px each side.',
      '   STEP 4 — both outside. Meta outside-left, name outside-right.',
      '',
      'No more arithmetic-on-guessed-widths. The browser tells us where everything actually is, and we react to that.',
    ],
  },
  {
    version: '4.88',
    date: '2026-05-21',
    notes: [
      'Bar meta — getComputedTextLength + arithmetic checks (fixes overlap).',
      '',
      'ROOT CAUSE of v4.86/v4.87 overlap bug: getBBox().width sometimes returns 0 or under-sized values immediately after appendChild, before the SVG has fully laid out. When the meta\'s measured width was smaller than its actual rendered width, the cascade thought Step 2 fit when it didn\'t — placing the name centered "between meta-right and bar-right" but using the WRONG meta-right value. Result: name landed too far left and overlapped the meta. The visible overlap is what was making "Robot" render as "wRobot" with the "R" jammed against the meta\'s "w".',
      '',
      'FIX: switched width measurement to getComputedTextLength(), which is the SVG API specifically designed for text run length and is reliable immediately after setting textContent. Added a +1 px safety margin to absorb sub-pixel rendering variance (kerning, antialiasing) so the visible gap stays ≥ INSIDE_GAP=3 even when the API under-reports by a fraction.',
      '',
      'ALSO: switched from post-render measure-and-check to upfront arithmetic. We measure widths ONCE, then compute whether each cascade step fits and apply only the first one that does. Cleaner and avoids the multiple getBBox passes that were the source of timing-dependent measurement errors.',
      '',
      'CASCADE (now arithmetic-driven, but using accurate measured widths):',
      '   STEP 1 — barW lets centered name clear meta + 3 AND bar-right - 3. Apply: meta inside-left, name centered IN BAR.',
      '   STEP 2 — (barRight - metaInsideRight) ≥ nameW + 6. Apply: meta inside-left, name centered between meta-right and bar-right.',
      '   STEP 3 — barW ≥ nameW + 6. Apply: meta OUTSIDE-left, name centered in bar.',
      '   STEP 4 — none of the above. Apply: meta outside-left, name outside-right.',
    ],
  },
  {
    version: '4.87',
    date: '2026-05-21',
    notes: [
      'Bar meta — Step 2 reverted to CENTERED between meta and bar-right.',
      '',
      'BUG: v4.86 anchored Step 2 at meta_right + 3 px (text-anchor="start"). The name ended up jammed against the meta with a huge dead space on the right side of the bar — visibly off-center. User: "It should be in the dead center of the bar, OR centered between the meta and the bar\'s right edge. Those are the only two options."',
      '',
      'FIX: Step 2 now centers the name at the midpoint of (meta\'s rendered right edge) and (bar\'s right edge). Symmetric gaps on both sides of the name. Same formula as v4.85, but using the MEASURED meta right edge from getBBox instead of the computed-from-assumed-width one — so the math matches what\'s actually on screen.',
      '',
      'KEPT FROM v4.86: overlap-driven cascade transitions. We still place → measure → check actual rendered overlap to decide whether to advance to the next step. Only Step 2\'s POSITIONING changed back to symmetric-center.',
      '',
      'CASCADE (now correct):',
      '   STEP 1 — meta inside-left, name CENTERED IN BAR (dead center).',
      '   STEP 2 — meta inside-left, name CENTERED between meta-right and bar-right (symmetric gaps).',
      '   STEP 3 — meta OUTSIDE-left, name centered in bar.',
      '   STEP 4 — both outside (meta outside-left, name always outside-right).',
    ],
  },
  {
    version: '4.86',
    date: '2026-05-21',
    notes: [
      'Bar meta — OVERLAP-DRIVEN cascade (no more pre-computed pixel thresholds).',
      '',
      'BUG: v4.85 was using barW thresholds (barW >= nameW + 2·metaW + 4·GAP, etc.) to decide which step fires. The math assumed measured widths matched rendered widths exactly. They don\'t — getBBox() can return 0 or slightly-off values depending on layout timing, and the threshold check then fires Step 1 (centered name) on bars where the actual rendered name overlaps the meta. User saw "90% · 2wRobot" visibly touching on multiple bars.',
      '',
      'FIX: place the elements, then MEASURE the actual rendered positions, and if they overlap (or run past the bar edge), advance to the next step. No more "barW >= some-number" decisions — purely overlap detection per the user\'s instruction: "if it overlaps, make the change, not based on the pixels of the bar."',
      '',
      'STEP 2 PLACEMENT CHANGED: was "name centered in available space (equal gaps from meta and bar\'s right)". NOW: "name\'s LEFT edge anchored at meta\'s right edge + INSIDE_GAP" — so the gap between meta and name is ALWAYS exactly 3 px, never less. The gap on the bar\'s right side varies. Per user: "there always should be the same gap between the meta and the name."',
      '',
      'CASCADE (overlap-driven):',
      '   STEP 1 — meta inside-left at barX+3, name CENTERED in bar. Keep IF measured name\'s left edge ≥ measured meta\'s right edge + 3 AND name\'s right edge + 3 ≤ bar\'s right edge.',
      '   STEP 2 — meta stays inside-left, name shifted to anchor at meta_right + 3. Keep IF name\'s right edge + 3 ≤ bar\'s right edge.',
      '   STEP 3 — meta pops OUTSIDE-left, name centered in bar. Keep IF name fits centered with 3 px each side.',
      '   STEP 4 — both outside. Meta outside-left, name ALWAYS outside-right (overrides clipBarLabels\' viewport-clip-to-left).',
      '',
      'IMPLEMENTATION: each measure() call after a mutation forces a layout flush via getBBox, so subsequent reads are accurate. Three getBBox-pairs total per bar (one per step transition) — negligible cost for typical schedule sizes.',
    ],
  },
  {
    version: '4.85',
    date: '2026-05-21',
    notes: [
      'Bar meta — 4-step cascade. "Only move the name when you need to."',
      '',
      'NEW CASCADE per approved spec. Gaps all 3 px (INSIDE_PADDING / INSIDE_GAP / OUTSIDE_GAP — configurable later via Setup).',
      '',
      '   STEP 1 — lots of room. Meta INSIDE-left at barX+3, name CENTERED in the bar.',
      '       Trigger: barW ≥ nameW + 2·metaW + 4·INSIDE_GAP.',
      '       Asymmetric gaps (more space on the right than between meta and name) are intentional. The principle is: as long as the centered name clears the meta by ≥ 3 px, leave the name alone.',
      '',
      '   STEP 2 — running out. Meta INSIDE-left at barX+3, name centered in the AVAILABLE SPACE (equal gaps from meta-right and bar-right).',
      '       Trigger: nameW + metaW + 3·INSIDE_GAP ≤ barW < Step 1 threshold.',
      '       nameCenterX = barX + (INSIDE_PADDING + metaW + barW) / 2. The name slides right as the bar shrinks, keeping gap-from-meta = gap-from-bar-right.',
      '',
      '   STEP 3 — meta pops OUTSIDE-left, name CENTERED in bar.',
      '       Trigger: nameW + 2·INSIDE_GAP ≤ barW < Step 2 threshold.',
      '       Meta\'s pill right edge ends 3 px from bar\'s left edge (OUTSIDE_GAP=3, PILL_PAD=2, so text right edge at barX-5 with text-anchor="end").',
      '       Note: clipBarLabels may have pushed the name outside (its PAD=8 threshold is stricter than ours). We FORCE the name back to centered-in-bar via setAttribute + classList.remove("bar-label-outside").',
      '',
      '   STEP 4 — BOTH outside. Meta outside-left, name ALWAYS outside-RIGHT.',
      '       Trigger: barW < nameW + 2·INSIDE_GAP.',
      '       Override clipBarLabels\' viewport-clip-to-left decision if it kicked in. The name should never end up on the left of the bar even when frappe-gantt thinks the right would clip the SVG edge.',
      '',
      'TRAILING-DASH SUFFIX REMOVED: the v4.84 " -" appended to inside meta is gone. With explicit gaps in the cascade, the dash is visual noise. Cleaner read.',
    ],
  },
  {
    version: '4.84',
    date: '2026-05-21',
    notes: [
      'Bar meta + Backlog duration picker + Backlog color (gray, lime on expire).',
      '',
      'BAR META — TIGHT FIT, NAME RIGHT-ALIGNED ON SHIFT:',
      '   ▸ Reverted v4.83\'s gap inflation. All gaps are 1px now (INSIDE_PADDING / SAME_ROW_GAP / META_NAME_GAP). Per user: "the gap should always be the same. Stop trying to fix gaps. Fix when they actually overlap."',
      '   ▸ Case C2 (overlap on centered name) now RIGHT-ALIGNS the name to the bar\'s right edge (text-anchor="end", x=barInsideRight) instead of placing it just past the meta. The description goes as far right as possible without extending past the bar — exactly what was requested: "almost all the way to the right side of the bar".',
      '   ▸ Post-shift overflow check kept as a sanity check: if the right-aligned bbox somehow exceeds the bar, revert and move meta outside.',
      '',
      'BACKLOG — DURATION PICKER:',
      '   ▸ The Duration column on the Backlog row is now a <select> dropdown with presets 1w / 2w / 3w / 4w / 5w / 6w / 8w / 10w / 12w + "Custom…". Pick a value, it saves immediately. "Custom…" opens the inline text editor for non-standard values (e.g. 3 days, 7w, etc).',
      '   ▸ Discoverable: it\'s an obvious dropdown, no hover-pencil-hint required. Click stays on the cell — doesn\'t fire the generic cell-edit path.',
      '',
      'BACKLOG — GRAY PILL, LIME BORDER WHEN EXPIRED:',
      '   ▸ Was red (#fee2e2 / #dc2626). Now gray (#e2e8f0 / #94a3b8) — reads as a neutral spine marker like the other anchor rows, not "behind schedule".',
      '   ▸ When the backlog\'s end_date is in the past (the calendar time has elapsed), the pill gets a lime-green border. Fill stays gray. No checkbox — backlog isn\'t a task you check off; it just expires.',
      '   ▸ Same logic on the Gantt: backlog bar is gray-filled with slate stroke by default; when expired, the stroke goes lime-green (2.5px).',
    ],
  },
  {
    version: '4.83',
    date: '2026-05-21',
    notes: [
      'Bar meta: real-measurement overflow check + bigger gaps.',
      '',
      '   1. POST-SHIFT OVERFLOW CHECK: after shifting the name right (case C2), the code re-measures the bar-label\'s actual rendered bbox via getBBox. If the right edge of the rendered name overflows barInsideRight by ANY amount, the shift is reverted and the meta moves outside-left (case B). Pre-shift measurements were off by a pixel or two on certain fonts/glyphs, which let "should have fit" cases overflow into the space past the bar. The post-shift check catches this empirically.',
      '',
      '   2. META_NAME_GAP bumped 6 → 12 px: clearer visual gap between the inside meta and the shifted name. Forces case B (meta outside) to trigger sooner on borderline bars, so the meta and name don\'t get crammed together inside.',
      '',
      '   3. SAME_ROW_GAP bumped 4 → 8 px: when meta lands outside-left, the gap between the meta pill and the bar\'s left edge is now 8 px instead of 4. Reads cleanly at typical zoom — no more "they look touching" perception.',
    ],
  },
  {
    version: '4.82',
    date: '2026-05-21',
    notes: [
      'Bar meta cascade — strict fit check, no tolerance, no double-render bug.',
      '',
      'THREE FIXES IN ONE:',
      '',
      '   1. STRICT FIT CHECK — NO TOLERANCE: the 6 px shift-overflow tolerance is gone. Either meta + gap + name fits inside the bar, or it doesn\'t. As soon as the math says it fits, the layout stays inside; as soon as it doesn\'t fit, meta pops outside. No "should have fit" pixel-jitter cases.',
      '',
      '   2. LEGACY DOUBLE-RENDER BUG GONE: after Step 4 called moveMetaOutside(), the function used to fall through to a leftover block that created a SECOND outside meta text on top of the first. Both block deleted. One meta element per bar, period.',
      '',
      '   3. CLEAN THREE-CASE CASCADE per your spec:',
      '       A. nameW > bar inside width → name outside-right (clipBarLabels default), meta outside-left.',
      '       B. metaW + 6 + nameW > bar inside width → meta outside-left, name stays centered.',
      '       C. Both fit inside → name centered if it clears the meta, otherwise shifted right just enough to clear meta + 6 px gap.',
      '',
      'EFFECT: as the bar gets narrower, the description gradually moves right from center toward the right edge of the bar (because shifted-name x = meta_right + 6 stays fixed while the bar\'s right edge moves leftward). When the description hits the right edge and there\'s no more room, meta pops outside-left and the description goes back to center. When even the description alone doesn\'t fit, the description goes outside-right and the meta joins it outside-left.',
    ],
  },
  {
    version: '4.81',
    date: '2026-05-21',
    notes: [
      'Bar meta: never inside-bar while name is outside-bar.',
      '',
      'RULE FIX (per user):',
      '   ▸ When the description doesn\'t fit inside the bar (clipBarLabels puts it outside-right because the bar is too narrow for the name), the META is now ALSO moved outside-left. Previously the meta stayed inside, which left a bar with "85%" inside it and "Configure Machine" floating to the right — visually broken.',
      '   ▸ Invariant: if name is outside, meta is outside. They live together.',
      '',
      'SHIFT-RIGHT MORE GENEROUS:',
      '   ▸ When a centered name overlaps the inside meta, we shift the name right. The previous check required the shifted name to fit STRICTLY inside the bar. Now there\'s a 6px overflow tolerance — borderline cases keep the meta inside instead of bouncing to outside-left. The user reads "barely fits" as "should fit".',
      '',
      'CASCADE NOW READS:',
      '   ▸ 1. Name is outside-bar (frappe-gantt couldn\'t fit it) → meta outside-left too.',
      '   ▸ 2. Name centered + meta inside-left don\'t overlap → keep both.',
      '   ▸ 3. They overlap, shifted-name fits (with 6px tolerance) → shift name right, meta inside-left.',
      '   ▸ 4. Shifted name doesn\'t fit → meta outside-left, name stays centered.',
    ],
  },
  {
    version: '4.80',
    date: '2026-05-21',
    notes: [
      'Action items now show on the personal Gantt (even without dates).',
      '',
      '   ▸ Personal Gantt was filtering out tasks/actions without start/end dates. Scheduled tasks without dates can\'t be positioned on a timeline so they still skip — but ACTION items always show now, defaulting to TODAY when their dates are null (old data created before v4.76\'s default-to-today, or anything migrated in).',
      '   ▸ The date projection happens at render time only — no mutation of state.tasks, so the underlying DB record is unchanged. Edit the action in the schedule to set a real date.',
      '',
      'PLACEHOLDER-ASSIGNED ACTIONS SURFACE TO THE DISCIPLINE:',
      '   ▸ Actions auto-assigned to "ME Placeholder" / "CE Placeholder" / etc. (because the user didn\'t pick a specific person at create time) now appear on EVERY engineer in that discipline\'s personal Gantt. So a freshly-added mech action shows on Ian\'s timeline, Mike\'s timeline, every mech engineer — not just the placeholder\'s.',
      '   ▸ Once a manager assigns the action to a specific person (via the inline Assigned To dropdown in the action list), it stops showing on everyone else\'s timeline and lives only on the assignee\'s.',
      '',
      'BACKLOG DURATION REMINDER:',
      '   ▸ The Backlog row\'s Duration column has been click-to-edit since v4.71. Hover the cell on the Backlog row in the schedule grid — you\'ll see a ✎ pencil hint and a lime tint. Click to type a new duration (2w / 3w / 10d / etc.) and Enter. The schedule cascades to reflect it.',
    ],
  },
  {
    version: '4.79',
    date: '2026-05-21',
    notes: [
      'Bar meta layout — center-first cascade, dash separator.',
      '',
      'NEW PRIORITY CASCADE:',
      '   ▸ 1. DEFAULT: description stays CENTERED (frappe-gantt\'s default). Meta lives inside the bar on the left with a trailing " -" dash. If they don\'t overlap, keep this layout.',
      '   ▸ 2. OVERLAP: if the centered name\'s left edge would overlap the meta\'s right edge, SHIFT the description right just enough to clear the meta (text-anchor = start, x = meta_right + 6px gap). Description never goes LEFT of center — only right.',
      '   ▸ 3. CAN\'T SHIFT: if shifting would push the description past the bar\'s right edge, the meta moves OUTSIDE the bar to the LEFT with the pill occluder. Description goes back to its natural position (centered, or outside-right if the bar is too narrow for the name itself).',
      '',
      'SEPARATOR CHANGED TO " - " (dash):',
      '   ▸ Was " · " (middle dot). Trailing the meta now reads "85% · 2w -" with a hyphen at the end. Inside the meta itself the " · " between alloc and dur stays (it\'s the convention from the rest of the app).',
      '',
      'IMPLEMENTATION NOTES:',
      '   ▸ Reads the bar-label\'s current x + text-anchor (set by clipBarLabels) so we can compute the centered name\'s actual edges without re-rendering.',
      '   ▸ Inline style.textAnchor is RESET at the top of drawBarMeta so a previous "shifted right" state doesn\'t accumulate across renders.',
      '   ▸ When meta goes outside-left, the trailing dash is dropped from its text (it pointed at the name, which is now far away on the right).',
      '   ▸ getBBox measures actual rendered widths — no more char-count guesses for the centered-vs-shifted decision.',
    ],
  },
  {
    version: '4.78',
    date: '2026-05-21',
    notes: [
      'Bar meta — always try inline first, getBBox decides, with 4px slack.',
      '',
      '   ▸ v4.77 had a char-count estimate gate ("does the estimated width fit?"). Even when optimistic, the estimate was wrong on bars with obvious room (e.g. "Panel" at 3w) and the meta got pushed outside.',
      '   ▸ Dropped the estimate gate entirely. Now: build the combined inline element, append to the DOM, measure with getBBox, decide.',
      '   ▸ Added 4px slack to the bbox check. Fits-or-close-enough → keep inline. Only when the actual text is more than 4px wider than the available bar room (barW − 8px padding) do we fall back to outside-left.',
      '   ▸ Browser font metrics can disagree by a px or two on the same string; the slack absorbs that without making borderline bars flicker to outside.',
    ],
  },
  {
    version: '4.77',
    date: '2026-05-21',
    notes: [
      'Bar meta inside the bar — same styling as outside, real bbox check.',
      '',
      'STYLE PARITY:',
      '   ▸ v4.75 mistakenly bumped the inside meta to match the task name\'s bigger / 600-weight styling. Reverted. The meta now keeps its outside-left styling exactly: 9px / 700 weight / dark fill / white halo (via paint-order stroke). Looks identical whether it lands inside or outside the bar.',
      '   ▸ Task name is the bigger / 600-weight part. Separator " · " is normal weight. Reads as "[small bold meta] · [bigger name]" — the name is the loudest text on the bar, the meta supports it.',
      '',
      'REAL BBOX CHECK INSTEAD OF CHAR-COUNT GUESS:',
      '   ▸ v4.75\'s char-count estimates were conservative enough that some bars that visibly had room (e.g. "Panel" at 3w) were getting the meta pushed outside anyway.',
      '   ▸ Now: estimate is OPTIMISTIC — bias toward trying the inline label. We then render it, measure actual width via getBBox, and only fall back to outside-left if the real bbox overflows the bar.',
      '   ▸ Result: more bars fit the combined inline label. Mismatched-estimate edge cases self-correct.',
      '',
      'IMPLEMENTATION:',
      '   ▸ Meta tspan: font-size 9 / font-weight 700 / paint-order stroke / stroke white / fill #1e293b.',
      '   ▸ Name tspan: font-size = nameFontSize (8-12 capped by bar height) / font-weight 600 / fill = hierarchy palette text / stroke none.',
      '   ▸ Both tspans share dominant-baseline central on the parent text so their baselines align.',
    ],
  },
  {
    version: '4.76',
    date: '2026-05-21',
    notes: [
      'Actions when signed in — unified Gantt, no separated list below.',
      '',
      'ACTION CREATION DEFAULTS TO TODAY:',
      '   ▸ Quick-add without a due date used to leave start/end null, which meant the action never appeared on the personal Gantt (Gantt requires a date to position the diamond). Now: missing due date → today\'s date as start AND end. Every action shows up on the Gantt as a milestone diamond.',
      '',
      'UNIFIED VIEW WHEN SIGNED IN:',
      '   ▸ When a person is signed in, the personal Gantt above IS the view. The lime section divider, filter chips, action list, and per-department dashboard cards all hide. Reads as one continuous "your stuff" page instead of "top is mine / bottom is team".',
      '   ▸ Quick-add bar stays visible so the user can keep adding actions.',
      '   ▸ When the user clears the sign-in, everything below the divider returns to the team-wide view.',
      '   ▸ Implementation: .actions-page gets a .is-signed-in class; CSS hides the redundant sections.',
      '',
      'PERSONAL GANTT BARS MATCH THE MAIN SCHEDULE LAYOUT:',
      '   ▸ Bar content was previously a separate <name>...<meta> split (name left, meta right). Now matches the v4.75 main Schedule Gantt: ONE inline label "85% · 2w · Configure Machine" at the bar\'s LEFT edge — meta in 700 weight, " · " separator in 400, task name in 600.',
      '   ▸ Same visual system across all Gantts in the app — schedule view, personal view, anywhere bars + meta render together.',
      '',
      'ACTIONS ON THE GANTT AS MILESTONES:',
      '   ▸ Action items (is_milestone=1 by default) render as diamond markers on the personal Gantt, positioned at their due date. Same diamond styling as the schedule Gantt — SDC-blue for action diamonds, hierarchy color for regular milestones, red border for overdue.',
    ],
  },
  {
    version: '4.75',
    date: '2026-05-21',
    notes: [
      'Bar meta — proper width-based fit decision + milestone double-render fix.',
      '',
      'BUGFIX — MILESTONE NAMES DOUBLED WHEN % WAS ON:',
      '   ▸ drawMilestoneDiamonds intentionally hides frappe-gantt\'s bar-label on milestones (opacity = 0) because drawMilestoneLabels renders its own labels. My v4.72 reset of ALL bar-labels\' opacity at the top of drawBarMeta was undoing that — so when % was on, every milestone showed two name labels stacked / offset.',
      '   ▸ Fix: the reset now scopes to non-milestone bar-wrappers only — `.bar-wrapper:not(.is-milestone) .bar-label`. Milestone bar-labels stay hidden.',
      '',
      'NEW LAYOUT ALGORITHM — actual width-based fit decision:',
      '   ▸ Old behavior: a fudge-factor approximation ("barW >= 2 × labelW") that decided "inside" vs "outside" without considering the task name length. Result: lots of bars where the description WOULD have fit alongside the meta got pushed out, AND some narrow bars kept the meta inside where it overlapped the centered name.',
      '   ▸ New behavior: compute the actual width of "{meta} · {name}" using char-count × font-size estimates. Then:',
      '       • Combined fits inside the bar → render ONE INLINE label "85% · 2w · Configure Machine" at the bar\'s left edge. Frappe-gantt\'s centered label is hidden so it doesn\'t double up.',
      '       • Combined doesn\'t fit → meta moves OUTSIDE to the LEFT of the bar (with pill occluder so arrows pass behind). Frappe-gantt\'s label stays wherever clipBarLabels put it — centered if name fits inside, outside-right if not.',
      '   ▸ Meta renders in 700 weight, name in 600, separator (" · ") in normal — reads as "[meta pill] · description" without the pill border. Color follows the bar\'s hierarchy palette (mech blue, controls green, etc.).',
      '   ▸ No more right-aligning the description. No more guessing. Direct width arithmetic.',
      '',
      'STILL TODO IF YOU SEE EDGE CASES:',
      '   ▸ Char-width estimates (META_CHAR_W = 6, NAME_CHAR_W = nameFontSize × 0.6) are conservative — they err on "won\'t fit" so bars don\'t overflow. If a bar looks like it should fit and doesn\'t, we can tune those down to ~5.5 / 0.55.',
    ],
  },
  {
    version: '4.74',
    date: '2026-05-21',
    notes: [
      'Actions list — per-row assignee filter, dept grouping, Open header fix.',
      '',
      'ASSIGNED-TO DROPDOWN IS NOW DEPARTMENT-SCOPED:',
      '   ▸ Each row\'s Assigned To select now shows ONLY the team members in THAT action\'s department. A Mech Eng action only lists mech engineers, a Controls Eng action only lists controls engineers, etc. Previously every row showed every name on the team — confusing once actions spanned multiple departments.',
      '   ▸ Edge case handled: if an action\'s current assignee belongs to a different discipline (e.g. after a department change), they\'re still listed in the dropdown so you can see + change them.',
      '',
      'PLACEHOLDERS DISPLAY AS "— UNASSIGNED —":',
      '   ▸ Placeholders (ME Placeholder / CE Placeholder / etc.) no longer appear as separate options in the dropdown — they ARE the unassigned state. When a row\'s saved assignee is a placeholder, the dropdown shows "— unassigned —" as the selected option.',
      '   ▸ Picking "— unassigned —" sets the assignee back to the action\'s discipline placeholder under the hood (keeps the invariant that every action has someone owning the slot, even if it\'s the placeholder).',
      '',
      'OPEN HEADER NO LONGER TRUNCATED:',
      '   ▸ Open column widened from 28px → 60px so the "Open" header text fits. Previously truncated to "Op…" because the cell was sized for the → glyph only.',
      '',
      'LIST GROUPED BY DEPARTMENT:',
      '   ▸ Actions are now grouped by discipline: Mech Eng → Controls Eng → Build → Wire → Cross-cutting. Each group gets a colored dotted-line divider with a colored dot, the discipline name, and the item count.',
      '   ▸ Empty departments don\'t render a divider. Sort within each group preserves the existing overdue-first → due-date → id order.',
      '',
      'NOTE — per-department dashboard with item-level breakdown is queued for a future round (per your "I think that\'s okay for now").',
    ],
  },
  {
    version: '4.73',
    date: '2026-05-21',
    notes: [
      'Bar label finally right-aligns when % toggle is on — clean override.',
      '',
      '   ▸ v4.69 + v4.71 tried to shift frappe-gantt\'s bar-label via setAttribute + inline style. Neither stuck reliably — names still overlapped the alloc/dur meta on bars like Builder 1 / Builder 2 / Machine Wiring.',
      '   ▸ New approach: stop fighting frappe-gantt\'s CSS. When the % toggle is on AND the alloc/dur meta lands INSIDE the bar:',
      '       1. The frappe-gantt bar-label is hidden via visibility: hidden.',
      '       2. We render OUR OWN <text> at the right end of the bar with text-anchor="end" + x = barX + barW − 4.',
      '       3. Font size mirrors clipBarLabels so short bars don\'t get clipped descenders.',
      '       4. Fill follows the bar\'s hierarchy color (matches what frappe-gantt would have shown).',
      '   ▸ When % is toggled OFF, drawBarMeta restores visibility on all bar-labels so the centered name reappears at its normal position.',
      '   ▸ Outside-placement bars (narrow bars where the meta sits outside the bar to the left) are unchanged — there\'s no overlap to fix in that case.',
    ],
  },
  {
    version: '4.72',
    date: '2026-05-21',
    notes: [
      'Actions quick-add — labels above every field + "Done" not "Create" on the section picker.',
      '',
      'SECTION PICKER — BUTTON SAYS "DONE" (not "Create"):',
      '   ▸ The picker that opens when you click "Section ▾" in the Actions quick-add now has a "Done" button instead of "Create" — because the picker only STASHES the section. The real create happens later when the user clicks "+ Add action" at the end of the row. "Create" was misleading: users clicked it expecting the action to be saved, then lost their fields.',
      '   ▸ Picker title changed from "Where should this task go?" → "Pick the section" to match the simpler one-shot intent.',
      '   ▸ The schedule\'s + Add task and + Add action flows still see the picker labelled "Create" — those flows DO commit immediately when the picker closes. Different intent, different label.',
      '   ▸ Implementation: showSectionPicker now accepts opts.okLabel + opts.title overrides. Default still "Create" / "Where should this task go?".',
      '',
      'QUICK-ADD BAR — LABELED FIELDS:',
      '   ▸ Each field is now wrapped in .actions-qa-field with a small uppercase label up top: TASK / PROJECT / SECTION / DEPARTMENT / DUE. Reads as "filling out a new row" instead of an unlabeled soup of inputs.',
      '   ▸ Labels are the same typography (10px / 700 / uppercase / muted) as the action list\'s column headers below, so the user can scan top-to-bottom and see the column meanings match the input meanings.',
      '   ▸ Field widths preserved — Task is the wide one, the rest are auto-sized.',
    ],
  },
  {
    version: '4.71',
    date: '2026-05-21',
    notes: [
      'Bar-label shift actually works + Backlog duration is editable.',
      '',
      'BAR LABEL SHIFT — CSS OVERRIDE FIX:',
      '   ▸ v4.69 added the shift via setAttribute("text-anchor", "end") but frappe-gantt\'s stylesheet sets .bar-label { text-anchor: middle } which has higher specificity than the SVG presentation attribute. Result: the attribute was set on the DOM but the rendered text stayed centered, so "Test/Debug Engineer 1" continued to sit smack on top of the alloc/dur meta.',
      '   ▸ Fix: ALSO set the inline style — barLabel.style.textAnchor = "end". Inline style beats stylesheet rules. Same reason clipBarLabels uses style.fontSize instead of setAttribute (matching comment up top of that function).',
      '   ▸ End result: when the % toggle is on AND the alloc/dur fits inside the bar, the task name slides to the right edge of the bar. The two labels claim opposite ends, no more overlap on long names.',
      '',
      'BACKLOG DURATION — DISCOVERABLE + EDITABLE:',
      '   ▸ The Backlog duration cell has always been editable (same td[data-col="duration"] click handler as every other duration column). But the Backlog row reads as a spine block — no progress pill, no drift chip — so users asked "can I change this?".',
      '   ▸ Pencil ✎ hint appears in the Backlog\'s Duration cell on hover. Cell tinted SDC-lime on hover so it visibly invites a click. Tooltip says "Click to edit — backlog duration varies based on team load."',
      '   ▸ Click the duration cell, edit the value ("2w", "3w", "10d", etc.), Enter to save. Cascade picks up the new duration and re-flows the rest of the schedule.',
    ],
  },
  {
    version: '4.70',
    date: '2026-05-21',
    notes: [
      'Actions page polish — uniform headers + section divider.',
      '',
      'COLUMN HEADERS ARE NOW UNIFORM:',
      '   ▸ Previous build re-used the data-row classes on each header cell, so column-specific styles leaked through — "Done" was centered + small, "Task" was bold + bigger, "Dept" had a pill background, "Open" was a bordered button, "×" was a red glyph. Headers read as eight different elements.',
      '   ▸ Each header is now its own class (.actions-hdr-done, .actions-hdr-name, .actions-hdr-project, etc.) with NO column-specific styles. The header row resets every child via `> span` rules: 10px, 700 weight, uppercase, muted color, no background, no padding, no border.',
      '   ▸ "Done" stays centered (matches the checkbox below). "Due" and "Open" stay right-aligned (matches the data below). Everything else left-aligned.',
      '   ▸ The "×" label on the delete column header is gone — it was reading like an action button instead of a column title. Header cell is blank now.',
      '   ▸ Done column widened from 22px → 36px so the "Done" header no longer crowds the "Task" header.',
      '',
      'LIME GREEN SECTION DIVIDER:',
      '   ▸ Thick lime bar inserted between the SIGN-IN section (person picker / personal summary / personal Gantt) and the TASK section (quick-add / filters / list / dept cards). Always visible, regardless of whether someone\'s signed in.',
      '   ▸ Reads as a clear visual break: stuff above the bar is YOU; stuff below is the WORK. Was: those two areas bled together with no separation.',
      '',
      'NOTE: department column kept for now — once you add actions across multiple departments, the colored Dept pill is the quickest visual scan.',
    ],
  },
  {
    version: '4.69',
    date: '2026-05-21',
    notes: [
      'Backlog auto-restore + bar-label shifts right when % toggle is on.',
      '',
      'BACKLOG ROW IS BACK:',
      '   ▸ Templates and any project where the Backlog had been deleted now auto-recreate it on the next refresh. ensureAnchorsForProject() checks if a Backlog exists; if not, it inserts one anchored to the project\'s Receipt of PO with a 10-business-day (2-week) default duration, allocation 0, and anchor_key="backlog".',
      '   ▸ isBacklogTask() now ALSO matches on anchor_key="backlog" (not just name + null phase_group), so a Backlog row with custom phase_group or a slightly renamed task still gets the special spine treatment.',
      '',
      'BAR LABEL SHIFTS RIGHT WHEN % IS ON:',
      '   ▸ When the % bar-meta toggle is enabled and the alloc/dur label fits INSIDE the bar (left-aligned), the task NAME now slides to the right end of the bar — text-anchor: end, x: barX + barW - 4. The two labels claim opposite ends of the bar so neither overlaps the other on long task names.',
      '   ▸ When % is off, the name stays in frappe-gantt\'s default centered position — no behavior change for the common read-only view.',
      '   ▸ When the meta lands OUTSIDE the bar (small bars), the name stays centered too — the conflict only existed for the inside-meta case.',
    ],
  },
  {
    version: '4.68',
    date: '2026-05-21',
    notes: [
      'Actions page polish — headers, inline edit, no accidental nav.',
      '',
      'ACTIONS LIST — proper grid layout:',
      '   ▸ Column headers: Done / Task / Project / Assigned To / Dept / Due / Open / ×. The list now reads like a table instead of a wall of mystery columns.',
      '   ▸ Row body no longer clicks-through to the schedule. Each row has an explicit "→" Open button at the right; everything else stays put for editing.',
      '   ▸ Assigned-To is now an INLINE select on every row — pick a name and the assignee updates immediately (no popup, no schedule jump). Falls back to "— unassigned —" if the user clears it.',
      '   ▸ Done checkbox now has a labelled column header so the column actually reads as "this is the complete toggle".',
      '   ▸ Project name + Task name columns rebalanced (1.4fr / 1fr / 1fr) so titles don\'t get crammed into a sliver.',
      '',
      'QUICK-ADD project dropdown filters templates:',
      '   ▸ Template projects (SDC_StandardProject_Template etc.) and Sales-workspace projects no longer appear in the Project picker. Fixes the "my new action ended up on the template" issue.',
      '',
      'PERSONAL GANTT — bar content + UX fixes:',
      '   ▸ Bars now show TASK NAME on the left + alloc/dur/progress meta on the right inside the bar (was just alloc/dur, no name).',
      '   ▸ Row body click no longer jumps to the schedule (it was eating drag attempts and felt like a trap). Each row has an explicit "→" button at the right edge to open the parent project.',
      '   ▸ Bottom padding bumped 22 → 32 + last row\'s border-bottom removed so the chart\'s final task isn\'t crammed against the card edge.',
      '   ▸ Hover affordance softened from 4% to 3% tint — less twitchy when you\'re reading the bars.',
      '',
      'NOTE — drag-to-move on the personal Gantt still requires the frappe-gantt cross-project rewire (see v4.67 notes). The "→ Open" button is the bridge to the editable view for now.',
    ],
  },
  {
    version: '4.67',
    date: '2026-05-21',
    notes: [
      'Personal Gantt — matches the main Schedule Gantt look.',
      '',
      '   ▸ COLORS FIXED: bars now use the same HIERARCHY palette as the main Schedule view (rowColorKey → HIERARCHY_BAR_COLORS). Mech tasks are BLUE (not green), Controls green, Build orange, Wire yellow — same everywhere in the app.',
      '   ▸ BAR META: each bar now shows the standard "85% · 2w · 50% done" label inline — allocation, duration in weeks, and percent-done when started — matching the main Gantt\'s % toggle.',
      '   ▸ PROGRESS FILL: tasks with progress > 0 get a darker overlay on the bar at width = % done, exactly like the main Gantt\'s bar-progress styling.',
      '   ▸ OVERDUE: bars past their end date with progress < 100 get a 2px red inset shadow + red border so they pop visually.',
      '   ▸ SPACING: row height bumped 26 → 32, padding around the chart bumped 12 → 18+22 so the chart breathes instead of feeling crammed.',
      '   ▸ Action item diamonds keep the SDC-blue paint they had in v4.66.',
      '',
      'NOTE — DRAGGING: this is currently a read-only preview. Click any row to jump to that project\'s schedule, where the bars are fully draggable + editable via frappe-gantt. Inline drag on this per-person chart is a follow-up — frappe-gantt is project-scoped today and would need to be rewired to feed it a cross-project task list.',
    ],
  },
  {
    version: '4.66',
    date: '2026-05-21',
    notes: [
      'Sign-in label + per-person Gantt on Actions.',
      '',
      '   ▸ "Who are you?" → "Sign in". Reads as an explicit account action instead of a quiz prompt.',
      '   ▸ NEW: per-person Gantt visualisation appears on the Actions page when someone signs in. Horizontal-bar timeline showing every task + action item they\'re assigned to, with valid dates, across every project.',
      '   ▸ Each bar is colored by project (stable hash so the same project gets the same color across reloads). Bars show the task name overlaid; tasks with progress > 0 < 100 get a dark overlay showing % done.',
      '   ▸ Zero-duration items (milestones + most action items) render as diamond markers in the project color. Action item diamonds paint SDC-blue; overdue ones turn red-bordered.',
      '   ▸ Vertical dotted red line marks TODAY across the chart.',
      '   ▸ Date axis up top labels each visible week (or every other week when the range is long).',
      '   ▸ Clicking any row jumps to that project\'s schedule.',
      '   ▸ When nothing\'s assigned with dates, an inline empty-state explains why instead of leaving the chart blank.',
    ],
  },
  {
    version: '4.65',
    date: '2026-05-21',
    notes: [
      'Actions "Who are you?" picker — cascading dropdowns.',
      '',
      '   ▸ v4.63 listed every team member as a chip in a horizontal strip. With 20+ people that\'s a wall of names. Replaced with two cascading dropdowns:',
      '       1. Department: Mech Eng / Controls Eng / Build / Wire / Project Mgmt',
      '       2. Person: populated with that department\'s members (leads marked with a ★)',
      '   ▸ Changing department clears the previously-picked person.',
      '   ▸ A small "× Clear" button appears once someone is picked, to jump straight back to Everyone view without going through the dropdowns.',
      '   ▸ Selection still persists in localStorage so Ian\'s next visit remembers him.',
    ],
  },
  {
    version: '4.64',
    date: '2026-05-21',
    notes: [
      'Departments UX + SDC-themed password dialog.',
      '',
      'PASSWORD CHANGED + IN-APP DIALOG:',
      '   ▸ TEAM_PASSWORD bumped from "sdc" to "sdcautomation" (all lowercase).',
      '   ▸ Browser-native prompt() replaced by an SDC-themed modal — SDC-blue background, SDC-lime green border, white text, lime "Unlock" button. Lives inside the app shell instead of feeling like a generic website popup.',
      '   ▸ Wrong-password shows the in-app alert dialog (same theme) instead of an OS alert.',
      '',
      'CLICK A PERSON → FOCUS THE WHOLE DEPARTMENTS PAGE ON THEM:',
      '   ▸ Removed: the v4.63 👁 View button + the v4.61 side-panel popup. Replaced by a much simpler interaction.',
      '   ▸ NEW: clicking anywhere on a team-member row (except inputs and the lead/remove buttons) FOCUSES that person. The row highlights with an SDC-blue outline.',
      '   ▸ When focused:',
      '       • A colored banner appears above the Resources timeline naming the person + their discipline, with a "× Clear focus" button.',
      '       • The Resources timeline filters to just their bars.',
      '       • The Behind / Coming Due / Key Dates dashboard cards filter to just their tasks.',
      '   ▸ If the focused person belongs to a different discipline than the one currently shown, the Resources tab auto-switches to their discipline so the bars actually render.',
      '   ▸ Clicking the same row again clears focus.',
      '',
      'ACTION ITEMS INCLUDED IN THE BEHIND CARD:',
      '   ▸ The Behind-schedule dashboard card was excluding milestones (which dropped all action items along with them). Updated filter: still excludes spine milestones (Receipt of PO / FAT / etc.) but INCLUDES action items, so an overdue action shows up alongside an overdue scheduled task.',
      '',
      'OTHER:',
      '   ▸ Team-member rows no longer have a separate cursor:pointer / cursor:text override — the row click only fires off blank space + the buttons stop propagation. Inputs still focus and edit normally without triggering a re-render.',
    ],
  },
  {
    version: '4.63',
    date: '2026-05-21',
    notes: [
      'Personal view + Departments password gate + bugfix.',
      '',
      'BUGFIX — DEPARTMENTS ROW CLICK NEVER OPENED THE DASHBOARD:',
      '   ▸ Each row\'s name + specialty inputs use flex: 1, so they take 100% of the row\'s horizontal space. The v4.61 "click anywhere on the row" handler ran but every click hit an <input> or <button> and returned early. The dashboard never appeared.',
      '   ▸ Fix: explicit 👁 View button on each team-member row next to the lead-toggle and remove buttons. Click it to open the per-person dashboard side panel. Tooltip on hover says whose tasks you\'re about to see.',
      '',
      'PASSWORD GATE ON THE DEPARTMENTS TAB:',
      '   ▸ Departments is now manager-only. Clicking the sidebar entry prompts for a password (default: "sdc") the first time per browser session. Auth is cached in sessionStorage so it doesn\'t re-prompt on every click.',
      '   ▸ Not real security — wrong-password just bounces the navigation. The point is to keep regular engineers from accidentally seeing the manager view, not to protect against motivated bypass.',
      '   ▸ Change the password by editing TEAM_PASSWORD at the top of app.js.',
      '',
      'PERSONAL VIEW ON THE ACTIONS PAGE:',
      '   ▸ New "Who are you?" chip bar at the top of the Actions page lists every real team member. Click your name → page flips into PERSONAL mode for you.',
      '   ▸ Personal mode shows:',
      '       • Banner with your name + discipline + specialty (colored to match your team).',
      '       • Stats strip: Open total / Overdue (red when > 0) / Scheduled tasks / Action items / Avg overrun (days past due across your closed actions — driven by completed_on minus end_date).',
      '       • Filtered list: ONLY your assigned action items, with the same filter chips + Active/Closed tabs available.',
      '   ▸ "Back to Everyone" button on the banner exits personal mode.',
      '   ▸ Selection persists in localStorage so the next time Ian opens the app it remembers him.',
      '   ▸ Stats reflect ALL your assignments (scheduled tasks AND action items), not just actions — gives the "what\'s on my plate" snapshot. The list below still scopes to action items so it stays focused.',
      '',
      'NOTE ON ACCESS CONTROL:',
      '   ▸ This is Option A from the design conversation — a "view as" picker, no real auth. Anyone with the URL can pick any name. Later upgrades can swap the picker out for a real login without changing the rest of the wiring.',
    ],
  },
  {
    version: '4.62',
    date: '2026-05-21',
    notes: [
      'Action items — bugfix + dept selector + dashboard + completed_on.',
      '',
      'BUGFIX (the big one):',
      '   ▸ The Active/Closed tab switch was leaving the list stuck — switching to Closed (or adding an action) would visually clear the page and subsequent renders would silently throw, so rows never re-appeared.',
      '   ▸ Root cause: the empty-state <p id="actions-empty"> lived in static HTML. When the list had rows, innerHTML rewrites removed the <p> from the DOM. Next render fetched it via getElementById → null → .classList.add() threw, aborting the render mid-flight before the new rows were drawn.',
      '   ▸ Fix: the empty state is now built inline by the renderer. No static element, no stale reference, no throw.',
      '',
      'NEW: DEPARTMENT DROPDOWN IN THE QUICK-ADD BAR:',
      '   ▸ Section 40 engineering used to silently auto-assign to ME Placeholder even when the action might have been a Controls task. Now there\'s an explicit Department dropdown (Mech Eng / Controls Eng / Build / Wire) right after the section picker.',
      '   ▸ Auto-fills from the section when unambiguous (section 10 > engineering > mech → Mech Eng). User can override for cross-cutting sections.',
      '   ▸ Discipline-aware actionDisciplineKey() now prefers the assignee\'s discipline (lookup via team) over the section mapping, so manual overrides stick.',
      '',
      'NEW: COMPLETED_ON COLUMN:',
      '   ▸ DB migration adds tasks.completed_on (TEXT). FIELDS array updated server-side.',
      '   ▸ Auto-stamped: when a PUT changes progress from < 100 to ≥ 100, server writes today\'s date (YYYY-MM-DD) into completed_on. When progress drops back below 100, it clears.',
      '   ▸ Editable: send completed_on in the same PUT to override (manual edits win — auto-stamp doesn\'t run if completed_on is explicitly in the request).',
      '   ▸ Closed action rows now show "{completed_on} ✓" in green where the due date used to be.',
      '',
      'NEW: PER-DEPARTMENT DASHBOARD CARDS:',
      '   ▸ Below the action list — small card per discipline (Mech / Controls / Build / Wire) showing Open / Overdue (red when > 0) / Done counts.',
      '   ▸ Cards reflect the FULL action set, not the filtered list, so the big-picture health stays visible while you filter.',
      '   ▸ Click a card to filter the list to that discipline; click again to clear.',
      '',
      'OTHER:',
      '   ▸ Filter dropdown labelled "All assignees" → "All people" (per user wording).',
      '   ▸ Adding an action force-switches the tab back to Active so the new row is always visible (even if user was on Closed at creation time).',
      '   ▸ Filter dropdowns now preserve selection across re-renders.',
      '   ▸ api.create wrapped in try/catch so a server error surfaces as an alert instead of failing silently.',
    ],
  },
  {
    version: '4.61',
    date: '2026-05-20',
    notes: [
      'Action items — three new pieces.',
      '',
      'PHASE 1 — ACTION ITEMS PAGE (sidebar > Actions):',
      '   ▸ New left-rail icon (checklist glyph) between Departments and Setup. Opens a master list of every action item (is_action = 1) across every project.',
      '   ▸ Active / Closed tabs at top — Active hides completed (progress = 100), Closed shows them. Default is Active.',
      '   ▸ Quick-add bar: title + project + section picker + due date + create. Pressing Enter in the title box creates the action.',
      '   ▸ Filter chips: Behind, Due this week, Unassigned (still on a Placeholder). Plus project / assignee / department selects.',
      '   ▸ Row layout: [✓] [name] [project] [assignee] [dept pill] [due] [×]. Overdue rows get a red left border + red due date. Done rows strike-through the name.',
      '   ▸ Clicking a row opens that project\'s schedule. Clicking the checkbox toggles done. Clicking × confirms-and-deletes.',
      '',
      'PHASE 2 — + ADD ACTION DEFAULTS TO THE DEPARTMENT PLACEHOLDER:',
      '   ▸ When you click + Add action in the schedule view and pick a section, the new action is automatically assigned to the discipline\'s Placeholder (ME Placeholder / CE Placeholder / Build Placeholder / Wire Placeholder). User can still edit the assignee inline after creation.',
      '   ▸ New helpers: disciplineForSection(dept, sub) + placeholderNameForDiscipline(disc). Same mapping powers the Action Items quick-add bar.',
      '   ▸ Sub-department wins when present (mech / controls / build / wire). Department fallbacks: teardown→build, install→build, engineering→mech, shop→build. Procurement / cross-cutting sections leave the assignee blank.',
      '',
      'PHASE 3 — PER-PERSON DASHBOARD PANEL:',
      '   ▸ Click any team member row on the Departments tab → a side panel slides in from the right showing their assigned scheduled tasks + action items.',
      '   ▸ Stats strip: Open count, Overdue count (red when > 0), Avg overrun (days past due for closed actions — naive but useful).',
      '   ▸ Filter chips: All / Tasks / Actions — same axis as the Schedule view\'s Schedule/Combined/Actions toggle.',
      '   ▸ Each row shows a Task or Action tag, name, project, and due date. Overdue rows get the red border + red date. Click a row → opens that project\'s schedule + closes the panel.',
      '   ▸ Closes on outside click, the × button, or Escape.',
      '',
      'WIRING NOTES:',
      '   ▸ Wired-once handlers on the actions page tabs / filter row / quick-add use a marker dataset attribute so renderActionsPage() can safely re-run.',
      '   ▸ Placeholder rows on the Departments tab were added in v4.56 specifically to make this workflow possible (manager sees the queue building up against the placeholder).',
    ],
  },
  {
    version: '4.60',
    date: '2026-05-20',
    notes: [
      'Milestone labels — pill occluder removed.',
      '   ▸ v4.58 added a white pill behind milestone NAME + DATE labels so arrows visually vanished under the text. User prefers seeing the arrow line pass through. Reverted just for milestones — bar-meta labels keep the pill (it\'s the only way to distinguish meta text from arrow line).',
    ],
  },
  {
    version: '4.59',
    date: '2026-05-20',
    notes: [
      'Bar meta — always LEFT-aligned.',
      '',
      '   ▸ Inside placement: label now sits at the LEFT end of the bar (was the right end). Centered task name still has the middle.',
      '   ▸ Left / below / above fallbacks: all left-aligned too — labels stack predictably along the left edge of every bar.',
      '   ▸ Pill occluder + halo behavior unchanged from v4.58.',
    ],
  },
  {
    version: '4.58',
    date: '2026-05-20',
    notes: [
      'Bar meta — fallback to LEFT + invisible pill that occludes arrows.',
      '',
      'PLACEMENT REORDERED:',
      '   ▸ INSIDE the bar at the right end (still preferred; halo so it reads on any phase color).',
      '   ▸ Outside fallback is now LEFT of the bar (not right). Reason: when a bar is too narrow for inside placement, frappe-gantt drops the bar\'s NAME outside-right. Putting our "85% · 8w" label to the right would collide with the name; the left side is clear.',
      '   ▸ If left would land outside the chart (bar at x ≈ 0), falls back to below → above with arrow-collision checks.',
      '',
      'INVISIBLE PILL OCCLUDER:',
      '   ▸ Every OUTSIDE label (left/below/above placements) now has a white rect drawn behind it sized to the text\'s actual bbox. The rect has no stroke and matches the chart background, so it\'s invisible — but it blocks the arrow line where it would otherwise cross the text. Arrows look like they cleanly stop at the label edge and continue out the other side.',
      '   ▸ The same occluder is applied to milestone NAME labels and milestone DATE labels. Fixes the v4.57 bug where arrows visually appeared to run straight through milestone text.',
      '   ▸ Inside-the-bar placement still uses the white halo via paint-order instead — a white rect there would cover the colored bar fill.',
      '',
      'NEW HELPER:',
      '   ▸ addPillOccluder(parent, textEl, pad) — reused by drawBarMeta and drawMilestoneLabels. Uses getBBox after the text is in the DOM so the rect snaps to actual glyph metrics rather than character-count estimates.',
    ],
  },
  {
    version: '4.57',
    date: '2026-05-20',
    notes: [
      'Bar meta — single combined label, smart placement.',
      '',
      'WHY THE CHANGE:',
      '   ▸ v4.55–v4.56 split alloc above + duration below. On stacked rows (Test/Debug Eng 1 directly above Test/Debug Eng 2) the labels collided — one task\'s below-label sat right where the next task\'s above-label wanted to go.',
      '',
      'NEW BEHAVIOR — feels like one pill:',
      '   ▸ Single combined label: "85% · 8w". Reads as one unit even without a visual pill border.',
      '   ▸ Placement priority (first slot that fits wins):',
      '       1. INSIDE the bar at the right end — preferred when the bar is comfortably wider than the label (≥ 2× label width). Label gets a white halo via SVG paint-order so it stays legible against any phase color.',
      '       2. JUST OUTSIDE the bar to the right (same row) — usually clear since later weeks are empty.',
      '       3. BELOW the bar at the right end (right-aligned) — used if no arrow blocks that strip.',
      '       4. ABOVE the bar at the right end (right-aligned) — same arrow check.',
      '   ▸ If every position is blocked, the label is skipped for that bar. Hover tooltip still has the data.',
      '',
      'EFFECT ON THE TEST/DEBUG STACK:',
      '   ▸ Test/Debug Eng 1, Test/Debug Eng 2, and Shop Debug bars are wide enough that the label drops INSIDE each bar at the right end — no collisions between rows because every label lives inside its own bar\'s territory.',
    ],
  },
  {
    version: '4.56',
    date: '2026-05-20',
    notes: [
      'Bar-meta + Gantt + Departments fixes.',
      '',
      'BAR META — ALLOCATION NOW SHOWS UNCONDITIONALLY:',
      '   ▸ The % toggle was still suppressing the allocation label on Sales-workspace projects (v4.22 carryover). Dropped the suppression in bar-meta specifically — if the user explicitly turns the toggle on, they want to see allocation regardless of workspace. The grid Allocation column still hides for Sales (separate path; unchanged).',
      '',
      'GANTT ARROW HEADS SHRUNK:',
      '   ▸ Was 6px headSize, which filled the whole vertical gap between adjacent rows at default row heights and looked clunky. Now 3px (half size) — points without dominating. Affects both the production drawCustomArrows and the legend reference renderer.',
      '',
      'DEPARTMENTS TAB — PLACEHOLDERS AT THE BOTTOM:',
      '   ▸ Placeholders ("ME Placeholder", "Build Placeholder", etc.) used to be hidden from the Departments dashboard entirely. Now they show at the BOTTOM of each discipline card under a dashed-border "Placeholders" label, below the real members.',
      '   ▸ Why: action items will default to a placeholder assignee so the manager can see them and assign to a real person later. Placeholders need to be visible for that workflow.',
      '',
      'TEAM SPECIALTY — LEVEL 1/2/3 DATALIST:',
      '   ▸ The Specialty input on each team member row now has a datalist offering Level 1 / Level 2 / Level 3 as type-ahead suggestions. Tooltip explains the field. Data model unchanged — still a single free-text string, so any existing custom specialty stays put and the user can still type anything.',
      '',
      'DEFERRED:',
      '   ▸ Action Items sidebar icon + landing view — needs a design pass. Will scope next round.',
      '   ▸ Existing 100%-allocation tasks: future autocomputes use 85/90 (v4.55); current tasks need a bulk reset or per-project re-autocompute to actually change.',
    ],
  },
  {
    version: '4.55',
    date: '2026-05-20',
    notes: [
      'Zoom + allocation fixes.',
      '',
      'ZOOM 1-10 EXPONENTIAL:',
      '   ▸ Was 1-5 with huge jumps (1=5% → 2=25% was 5×, way too much). New: 1-10 levels mapped exponentially so each click multiplies by ~1.527× — the small end barely moves visually, the big end still spans up to 250%.',
      '       1 → 5%    (Month, very far out)',
      '       2 → 7.6%       3 → 11.7%       4 → 17.8%',
      '       5 → 27.2%      6 → 41.5%       7 → 63.4%',
      '       8 → 96.8%      9 → 147.8%      10 → 225.6%',
      '   ▸ getZoomConfig auto-switches Month → Week → Day based on the zoom %, so the time scale follows the level.',
      '',
      'WHEEL ZOOM UPDATES THE DROPDOWN DISPLAY:',
      '   ▸ setZoom() now calls renderZoomMenu() at the end, so scrolling the mouse wheel inside the Gantt updates the Zoom dropdown\'s displayed value in real time. Was only updated by clicking + / − before — confusing because the actual zoom WAS changing.',
      '',
      'BAR META — ALLOCATION ABOVE, DURATION BELOW:',
      '   ▸ The % toggle\'s tooltip has always said "ALLOCATION % above each bar and DURATION (wks) below". The code was joining both into one combined label and dropping it on whichever side avoided arrows. Now it actually splits: alloc tries the ABOVE side first (falls back to below if blocked), duration tries the BELOW side first (falls back to above).',
      '   ▸ Allocation default for null values fixed: now reads as 90% (matching the rest of the app) instead of being skipped silently when alloc was unset.',
      '',
      'SERVER AUTOCOMPUTE — 85% ENGINEERING / 90% SHOP:',
      '   ▸ Was using a single fullAllocPct (= 90) for every task type, contradicting the v4.20 SDC standard that engineering = 85, shop = 90. Server-side allocation logic now uses ENG_ALLOC_PCT (85) for engineering work (mech eng, controls design/software/drawings, gen hmi/robot/vision/device, configure machine, T/D 1+2) and SHOP_ALLOC_PCT (90) for physical work (build, wire, shop debug, teardown, install).',
      '   ▸ Affects FUTURE projects created via the autocompute path. Existing tasks already in the DB with allocation = 100 or 90 stay as-is — they need to be re-edited or the project re-autocomputed for the new defaults to apply.',
    ],
  },
  {
    version: '4.54',
    date: '2026-05-20',
    notes: [
      'Filters + Zoom tuning.',
      '',
      'FILTERS BACK TO CHECKBOX LIST:',
      '   ▸ The pill/chip layout is gone. Each filter is now a row with a checkbox + label inside the Filters popover — easier to scan top-to-bottom. For Customer mode is just another checkbox under a VIEW section.',
      '   ▸ Hovering a row highlights the whole stripe so the click target is obvious.',
      '',
      'ZOOM — 1 to 5 WITH AUTO TIME-SCALE:',
      '   ▸ Was a 1-10 scale (25%-250%). New: 1-5 discrete presets, each tied to a specific time scale so pressing + / − walks through Month → Week → Day automatically:',
      '       1 → 5%   (Month, very far out)',
      '       2 → 25%  (Month, still wide)',
      '       3 → 70%  (Week, the comfortable middle)',
      '       4 → 150% (Day, comfortable per-day columns)',
      '       5 → 250% (Day, dense)',
      '   ▸ getZoomConfig\'s auto-mode-switch (Month / Week / Day based on px-per-day) handles the time scale based on the preset %, so the Time scale section is gone from the dropdown — one less control.',
      '',
      'ROW HEIGHT — RANGE TIGHTENED + RENUMBERED:',
      '   ▸ Was a 10–100 scale mapping to 12–30px. New: 0–100 scale mapping to 16–30px (friendly 0 = the previous friendly 30 = 16px, the smallest you\'d want; friendly 100 stays at 30px).',
      '   ▸ Step of 10 friendly = 1.4px (was 2px). Lets you nudge in finer increments.',
      '   ▸ ROW_H_MIN dropped from 12 to 16.',
    ],
  },
  {
    version: '4.53',
    date: '2026-05-20',
    notes: [
      'Toolbar tuning — third pass.',
      '',
      'PANE + MODE BACK AS VISIBLE SEG-CONTROLS:',
      '   ▸ The v4.51 combined View dropdown and the v4.52 "band above the banner" are both gone. Grid/Both/Gantt and Schedule/Combined/Actions are now two visible 3-button seg-controls sitting on the same toolbar row alongside everything else. Click directly to switch — no dropdown, no second layer.',
      '   ▸ The hidden #btn-view-mode / #view-mode-menu DOM stays so the legacy setupViewModeDropdown / syncViewModeButton helpers keep no-op-ing safely; no rewiring of callers needed.',
      '',
      'FILTERS BACK AS A LABELLED DROPDOWN:',
      '   ▸ The v4.52 icon bracket is gone — too many ambiguous symbols. Filters are a dropdown again with text labels for every chip ("Show completed", "Behind schedule", etc.) plus the For Customer mode toggle in a VIEW section below.',
      '',
      'ZOOM — 1 to 10 with discrete +/− STEPS:',
      '   ▸ Was a 0-100 friendly projection of the 5%-500% internal range. Problem: 1.25× per click is too small to advance the displayed value reliably (the user could press + multiple times and watch the number not change).',
      '   ▸ New: friendly Zoom is a discrete 1–10 scale. Each click of + or − changes the displayed value by exactly 1. Linear mapping: friendly 1 = 25%, friendly 10 = 250%, step 25% per unit. Plenty for the typical project span; mouse-wheel zoom still has the full 5%-500% range for finer control.',
      '   ▸ Buttons auto-disable at 1 and 10 so you stop at the boundaries.',
      '',
      'ROW HEIGHT — RANGE TIGHTENED:',
      '   ▸ User feedback: even the v4.52 ceiling (friendly 100 = 40px) was way taller than you\'d ever want. New ceiling: 100 = 30px. Step of 10 friendly units = 2px now (was ~3.1px). Lets you nudge in much smaller increments near the small end.',
      '',
      'CARRYOVER FROM v4.52 (still in place):',
      '   ▸ View bracket: ┃ ≡ │ α │ $ │ ↔ │ % ┃ with tooltips, framed by thick SDC-dark outer borders.',
      '   ▸ Undo/Redo as rounded icon buttons (↺ ↻) next to Show/hide columns.',
      '   ▸ Quote vs Schedule lives in the bottom footer.',
      '   ▸ Vertical 1px dividers between every adjacent toolbar group.',
      '   ▸ Right-side order: Baseline → Zoom → Zoom to fit.',
    ],
  },
  {
    version: '4.52',
    date: '2026-05-20',
    notes: [
      'Toolbar tuning — second pass.',
      '',
      'VIEW DROPDOWN MOVED ABOVE THE PROJECT NAME:',
      '   ▸ The combined Grid/Both/Gantt + Schedule/Combined/Actions picker is back as a centered band IMMEDIATELY ABOVE the project name banner. Single bigger pill button (8×22px padding, 15px font, min-width 220px) — popover content unchanged.',
      '',
      'FILTERS COLLAPSED INTO AN ICON BRACKET:',
      '   ▸ The Filters dropdown is gone. Every filter is now a visible icon toggle in a bracket next to the View toggles (same Smartsheet-style framing: thick outer borders, thin inner dividers):',
      '       ✓   Show completed              ▼   Behind schedule            ▲   Ahead of schedule',
      '       ◆   Milestones only             👤  Assigned (real work)       ⚠   Over-allocated',
      '       ⛓   Critical path               ★   Critical path only         👁  For Customer mode',
      '   ▸ Tooltips on every icon. Active filters paint blue.',
      '   ▸ The Filters popover and chip-count are gone from the toolbar (the underlying DOM is kept hidden so renderFilters() doesn\'t crash).',
      '',
      'TOOLBAR ORDER + DIVIDERS:',
      '   ▸ Right-side order flipped: Baseline → Zoom → Zoom to fit (was Zoom → Baseline → Fit).',
      '   ▸ Vertical 1px dividers between EVERY adjacent group, not just between major blocks:',
      '       [Show/hide cols] | [↺][↻] | [view bracket] | [filter bracket] — gap — [◎ Baseline] | [🔍 Zoom] | [⛶ Fit]',
      '',
      'ZOOM DROPDOWN — SCALES RETUNED:',
      '   ▸ Row height friendly scale: 10 = 12px (smallest), 100 = 40px (largest), increments of 10. Was 10 = 18px / 100 = 120px — too big to start, too tall at the top. Internal ROW_H_MIN dropped from 18 to 12 to let rows actually go that small.',
      '   ▸ Zoom value now shows 0–100 (was 5%–500%). The internal range is still 5%–500% so the wheel-zoom + Zoom-to-fit math is unchanged — just the displayed value is mapped onto a friendly scale where 0 = smallest, 100 = largest.',
    ],
  },
  {
    version: '4.51',
    date: '2026-05-20',
    notes: [
      'Toolbar reorg — Smartsheet-style layout.',
      '',
      'NEW: COMBINED VIEW DROPDOWN',
      '   ▸ Grid/Both/Gantt (pane layout) and Schedule/Combined/Actions (content mode) folded into a single dropdown. Button label shows current state ("Both · Schedule ▾"). Click opens a popover with both axes inside, divided by a line — pane modes on top, content modes on bottom. Active rows get a soft blue tint + ✓ checkmark.',
      '   ▸ Replaces the two seg-controls + .view-picker band from v4.46–v4.50. Reclaimed ~30px of vertical space above the grid.',
      '',
      'NEW: ZOOM DROPDOWN',
      '   ▸ Single button "🔍 Zoom ▾" in the toolbar replaces the stacked Weeks/Rows pill. Popover holds three sections divided by lines:',
      '       TIME SCALE — Days / Weeks / Months as radio rows with ✓ on the active one.',
      '       ZOOM       — [−] 60% [+] (drives Gantt density at the same 1.25× step as before).',
      '       ROW HEIGHT — [−] 40 [+] on a friendly 10–100 scale in increments of 10 (10 = smallest, 100 = biggest). Internally maps onto the existing 18–120 px range so setRowHeight() didn\'t need to change — just the UI now speaks in 10s.',
      '   ▸ +/− buttons auto-disable when the value hits the boundary (5%/500% for zoom, 10/100 for row height).',
      '',
      'NEW: VIEW TOGGLES IN A BRACKET',
      '   ▸ The five view icons (≡ flatten, α alloc-prefix, $ financials, ↔ arrow lags, % bar meta) are now framed in a bracket — thick 2px outer borders left + right (SDC-dark), thin 1px inner dividers between each icon. No more "View" label above; the bracket + tooltips on every icon communicate the group purpose.',
      '   ▸ Hover any icon to see its full description (tooltips were already there but the new layout makes them more discoverable).',
      '',
      'NEW: UNDO/REDO AS ROUND ICON BUTTONS',
      '   ▸ Was "↶ Undo" / "↷ Redo" flat 6px-radius buttons on the right side. Now circular icon-only buttons with curly arrows (↺ ↻) sitting just to the right of Show/hide columns. Disabled state fades to 35% opacity.',
      '',
      'MOVED: "FOR CUSTOMER" INTO FILTERS',
      '   ▸ Lived as its own toolbar button before. Now a checkbox in the Filters popover under a new VIEW section, with a one-line hint explaining what it does. Wiring drives the existing enterCustomerView/exitCustomerView so flatten + zoom-to-fit side effects still fire.',
      '',
      'MOVED: "QUOTE VS SCHEDULE" TO BOTTOM FOOTER',
      '   ▸ Was crowding the toolbar mid-row. Now lives in a thin footer strip below the schedule split, right-aligned. Hidden in customer view.',
      '',
      'MOVED: BASELINE TO THE RIGHT',
      '   ▸ "◎ Baseline ▾" was floating in the middle of the toolbar between Quote vs Schedule and the zoom pill. Now docked to the right side, just left of "⛶ Zoom to fit".',
      '',
      'NEW: VERTICAL DIVIDERS BETWEEN TOOLBAR GROUPS',
      '   ▸ Thin 1px lines partition the toolbar into logical groups so the row reads as distinct sections rather than one flat sea of buttons:',
      '       1) Show/hide columns + Undo + Redo',
      '       2) View dropdown + Filters',
      '       3) View toggle bracket',
      '       — flex gap —',
      '       4) Zoom + Baseline',
      '       5) Zoom to fit',
      '',
      'UNDER THE HOOD:',
      '   ▸ Legacy zoom-controls (#btn-zoom-out / #btn-zoom-in / #zoom-mode / #btn-row-h-up / #btn-row-h-down / #btn-customer-view) stay in the DOM as hidden inputs. The new dropdowns dispatch into them so the existing setZoom / setRowHeight / change handlers don\'t need to be rewritten — fewer moving parts, smaller diff, easier rollback.',
      '   ▸ syncActionsModeButtons() preserved as a thin wrapper around syncViewModeButton() + renderViewModeMenu() so existing callers (+ Add action auto-switching Schedule → Combined) still work.',
      '   ▸ setPaneMode() self-syncs the View dropdown at the end so the button label stays current no matter what triggered the pane change.',
    ],
  },
  {
    version: '4.50',
    date: '2026-05-20',
    notes: [
      'Two fixes per user feedback.',
      '',
      'VIEW PICKER repositioned + simplified:',
      '   ▸ Out of the toolbar entirely. Now sits as a centered band IMMEDIATELY ABOVE the project name banner.',
      '   ▸ Dropped the wrapper chip, the "VIEW" label, and the vertical divider — that whole v4.48/v4.49 chrome read as noise. Just two bare seg-controls stacked vertically: Grid/Both/Gantt on top, Schedule/Combined/Actions underneath.',
      '   ▸ Background matches the schedule-project-header (var(--surface-alt)) so it reads as a continuation of the header area rather than a floating widget.',
      '   ▸ Same IDs + handlers — no JS changes.',
      '',
      'GANTT-ONLY MODE row order — FIXED:',
      '   ▸ Bug: in Gantt-only view (or whenever sortByStart was off), bars rendered via sortByPhaseThenOrder which ranks tasks by their `phase` field. Anchors (Receipt of PO / FAT / Ship Machine / Machine Power-Up) have no `phase` set → rank 99 → they fell to the BOTTOM of the chart. So FAT appeared above PO, Ship above Power-Up, etc. Same problem visible in Actions mode where anchors fell below the action items.',
      '   ▸ Fix: when NOT in sortByStart mode, renderGantt now uses buildCanonicalTaskOrder() — the same hierarchy walk the GRID uses for its line numbers. Receipt of PO at top, Backlog right under it, then section 10 bucket walk (Mech / Controls / General / Procurement / Build / Wire), then FAT closing section 40, then teardown / Ship Machine / install for section 50.',
      '   ▸ Gantt bars now match grid row order in every pane mode (Grid / Both / Gantt) and every actionsMode (Schedule / Combined / Actions).',
      '   ▸ buildCanonicalTaskOrder already applies filters internally, so we don\'t double-filter. sortByStart mode is unchanged — it deliberately ignores hierarchy in favor of chronological order.',
    ],
  },
  {
    version: '4.49',
    date: '2026-05-20',
    notes: [
      'Three Actions follow-ups.',
      '',
      'OVERDUE ACTIONS render RED — grid + Gantt:',
      '   ▸ Any action item past its due date and not at 100% gets an `is-overdue` class on its <tr>. CSS flips the task name + duration to red-600 (#dc2626). Visible without applying the Behind filter.',
      '   ▸ Matching Gantt diamond paints red-300 fill + red-600 stroke (vs the default SDC-blue for active actions). Same color language grid → chart.',
      '   ▸ Overdue logic: `is_action && progress < 100 && end_date < today`. Today is computed once at render time.',
      '   ▸ Limited to actions for now. Regular scheduled work has the existing drift chip / Behind filter — didn\'t want to blanket-paint that red.',
      '',
      'VIEW PICKER moved to the LEFT edge of the toolbar:',
      '   ▸ Was sitting in the middle next to Quote vs Schedule + View pill. Now first thing on the toolbar — primary control, deserves the front position.',
      '   ▸ Same wiring (setPaneMode, syncActionsModeButtons), same IDs, same data-attrs. Pure DOM relocation.',
      '',
      'FILTERS — yes, all standard quick filters still work in every actionsMode:',
      '   ▸ Behind, Ahead, Milestones, Assigned, Over-allocated, Critical Path, Show completed — all compose with the Schedule/Combined/Actions toggle.',
      '   ▸ The actionsMode filter narrows the SET of rows in scope (schedule vs actions vs both). The quick filters narrow WITHIN that set.',
      '   ▸ Example: Combined view + "Behind" filter = both behind scheduled tasks AND behind/overdue actions show up. Switch to Actions + "Behind" = only overdue actions.',
      '   ▸ applyFilters runs first, then renderTable layers the actionsMode on top, then critical-path-only on top of that. Order is invariant.',
    ],
  },
  {
    version: '4.48',
    date: '2026-05-20',
    notes: [
      'Picker combined per user spec (Option A).',
      '',
      'Two seg-controls — pane mode (Grid/Both/Gantt) and content mode (Schedule/Combined/Actions) — now share one rounded container with a "View" label on the left.',
      '',
      'Layout:',
      '┌─ VIEW ──────────────────────────┐',
      '│  [ Grid ] [ Both ] [ Gantt ]    │',
      '│  [Schedule][Combined][Actions]  │',
      '└─────────────────────────────────┘',
      '',
      '   ▸ New `.view-picker` wrapper holds a `.view-picker-label` ("View") + a `.view-picker-rows` flex column containing both seg-controls.',
      '   ▸ The inner seg-controls drop their own background + padding inside the wrapper (CSS: `.view-picker-rows .seg-control { background: transparent }`) so they read as rows of buttons inside one chip, not chips-inside-a-chip.',
      '   ▸ Buttons get slightly tighter padding (4px 12px / font 11) so the two rows fit without dominating the toolbar.',
      '   ▸ Same IDs + data-attributes as before — all the existing handlers (setPaneMode + syncActionsModeButtons) still work without changes.',
      '',
      'Toolbar gains ~14px of vertical height for the second row but loses one horizontal seg-control width, so net horizontal footprint shrinks. The grouping makes the two axes feel like one decision ("how do you want to view this project?") rather than two disconnected controls.',
    ],
  },
  {
    version: '4.47',
    date: '2026-05-20',
    notes: [
      'Actions polish — visibility, Gantt sync, and a picker-design pitch.',
      '',
      'ACTION ROWS — now SDC BLUE + italic:',
      '   ▸ Was just italic; user said it didn\'t stand out enough. Added `color: var(--sdc-primary)` to `tr.is-action .name-cell-main`. Italic blue task name vs upright black for scheduled work. Pops at a glance.',
      '',
      'ACTION MILESTONES on the Gantt — now SDC blue diamonds:',
      '   ▸ `drawMilestoneDiamonds` checks `task.is_action` and paints the diamond SDC-primary blue (fill #3a8edc, stroke #1574c4) instead of the default slate. Same visual language as the grid row — italic blue grid name + blue diamond on the chart.',
      '   ▸ Anchors (Receipt of PO / FAT / etc.) stay lime — action milestones can\'t collide with anchors visually.',
      '',
      'GANTT now FILTERS by actionsMode — matches the grid:',
      '   ▸ Bug: switching to "Actions" hid action rows in the grid but the Gantt kept showing every scheduled bar. The two panes were out of sync.',
      '   ▸ Fix: renderGantt applies the same actionsMode filter renderTable does. Anchors always render (project spine). Otherwise: schedule → drop is_action; actions → drop non-action; combined → keep both. The Gantt now mirrors the grid in every mode.',
      '',
      'PICKER DESIGN PITCH — see chat. Three options proposed for combining the pane-mode (Grid/Both/Gantt) and actions-mode (Schedule/Combined/Actions) into one cohesive widget. No code change yet — waiting on user choice.',
    ],
  },
  {
    version: '4.46',
    date: '2026-05-20',
    notes: [
      'ACTIONS feature — first cut shipped.',
      '',
      'DATA MODEL:',
      '   ▸ New `tasks.is_action` column (INTEGER DEFAULT 0). Migration runs on server startup; the existing tasks table is reused so every renderer, filter, edit flow, and undo path applies to actions for free.',
      '   ▸ Server FIELDS whitelist + POST insert now include is_action so the API accepts the flag.',
      '',
      'VIEW TOGGLE — Schedule / Combined / Actions:',
      '   ▸ New 3-way segmented control next to the Grid / Both / Gantt pane toggle.',
      '   ▸ Schedule (default): only duration tasks. Actions hidden. Existing behavior, no change.',
      '   ▸ Combined: shows both. Actions sort to the BOTTOM of each sub-dep group so scheduled work appears first.',
      '   ▸ Actions: only action items. Use it to focus on the project\'s ad-hoc to-do list.',
      '   ▸ Anchor rows (Receipt of PO, FAT, Ship Machine, etc.) ALWAYS show — they\'re project-spine markers, not work items.',
      '   ▸ Choice persists in localStorage via scheduleView.actionsMode.',
      '',
      '+ ADD ACTION BUTTON — sits next to + Add task. Lime green (vs Add task\'s yellow), italic. Click → section picker → creates a row with is_action=1, duration_days=0, is_milestone=1, Assigned To blank. If you\'re in Schedule mode when adding, the toggle auto-bumps to Combined so you can see what you just created.',
      '',
      'VISUAL DIFFERENTIATION — action body rows render with `is-action` class. Task name flips to italic font-style. Same columns, same hierarchy, same click-to-edit pills — just italic so they read as "ad-hoc work" vs the upright scheduled bars.',
      '',
      'TERMINOLOGY: "Assigned To" instead of "assignee" in visible tooltips and labels (the field name in code stays `assignee` — only user-facing strings changed).',
      '',
      'NOT in this PR (next):',
      '   ▸ Departments page: filters bar (status / project / person) + person-detail view + department-lead unassigned bucket.',
      '   ▸ Actions on the Gantt (toggleable). Currently actions render in the grid only; their bars don\'t appear on the Gantt yet.',
      '   ▸ Central Actions hub across all projects (per user: "can come later").',
    ],
  },
  {
    version: '4.45',
    date: '2026-05-20',
    notes: [
      'Three changes + design doc for the Actions feature.',
      '',
      'LINK BADGE — transparent + baseline-aligned:',
      '   ▸ Was a small light-blue pill that sat slightly lower than the duration text. Dropped the background fill entirely, dropped the padding, switched vertical-align from middle to baseline. Now reads as quiet inline blue text whose bottom sits exactly on the bottom of the duration value.',
      '',
      'COMPLETED TASK row styling — lime outline + diagonal hashes:',
      '   ▸ Any non-milestone duration task at 100% progress now gets a `task-done` class on its <tr>. CSS paints every TD in the row with a -45deg repeating-linear-gradient at low opacity (so cells underneath still read), plus a lime box-shadow border top and bottom. The first and last TDs in the row carry the side borders too so the lime outline wraps the row uniformly.',
      '   ▸ Default behavior: completed tasks HIDE. The old "Not complete" filter chip flipped polarity to "Show completed" (default OFF = hide). Toggle it ON in the Filters popover to see the done rows with the lime hash style.',
      '   ▸ Milestones included in the hide-by-default: a done Receipt of PO / FAT / Mech 1 Release also drops out unless "Show completed" is on. They\'re not "remaining work."',
      '',
      'SIDEBAR DEPARTMENTS ICON — changed from 👥 emoji to an inline-SVG org chart (one node up, two nodes connected by lines below). Reads more clearly as "team structure" than the generic people emoji.',
      '',
      'ACTIONS feature — design locked, build starting next:',
      '   ▸ New "Actions" tab inside each project schedule. Same Section → Dept → Sub-dept hierarchy as Schedule. Same columns (predecessor, ALOC, DESCRIPTION, DUR, %COM). Duration usually 0 (most actions are date-only milestones).',
      '   ▸ Combined view: actions render at the BOTTOM of each sub-dept group, after the duration tasks. Italic font so they read differently from scheduled work.',
      '   ▸ Flatten view: actions interspersed with regular tasks by date, still italic.',
      '   ▸ Actions on Gantt: optional via a View toggle. When on, show as small italic markers at their due date.',
      '   ▸ Assign to a person OR to a department lead (e.g. ME team → Mike Sendzak). Single-assignee always.',
      '   ▸ Status = % complete only (0 / 100). No separate "open / blocked / in-progress" states.',
      '   ▸ "+ Add action" button alongside the existing "+ Add task".',
      '   ▸ Person view on Departments page shows duration tasks + actions assigned to that person, combined and sorted by date.',
      '',
      'Building Actions tab + DB schema next. The completed-task hash style + Show-completed filter that landed today is the visual + filter foundation it\'ll reuse.',
    ],
  },
  {
    version: '4.44',
    date: '2026-05-20',
    notes: [
      'Task column header now has FOUR labeled sub-regions on the bottom row.',
      '',
      '   ▸ Top half: TASK banner (centered, no longer bold — now `font-weight: 600` matching other column headers like ASSIGNED TO / START / FINISH).',
      '   ▸ 1px separator line below the TASK banner spanning the full TH width.',
      '   ▸ Bottom half: ALOC | DESCRIPTION | DUR | %COM, all light gray (--text-muted), 9px, positioned to land directly above their body counterparts:',
      '       • ALOC at left: 8px (above body alloc value)',
      '       • DESCRIPTION centered between left: 64 and right: 110 (above body task name)',
      '       • DUR at right: 60px (above body dur value)',
      '       • %COM at right: 8px (above the body % complete pill)',
      '',
      'Each sub-label tells you what the body value below it is. DESCRIPTION makes it explicit that the middle area is the task description; otherwise the centered TASK banner had to do double duty.',
    ],
  },
  {
    version: '4.43',
    date: '2026-05-20',
    notes: [
      'Five polish fixes.',
      '',
      'HEADER SUB-LABELS now align with their body values:',
      '   ▸ Bug: TH had `padding-left: 64` so the `.th-label-name` wrapper started 64px from the TH edge. Absolute children inside (.th-label-alloc / .th-label-dur / .th-label-pct) anchored to the wrapper, so `left: 8` ended up at 64+8=72px from the TH edge. Body absolute children anchor to the TD directly (TD\'s padding doesn\'t shift its padding-box edges), so body alloc was at 8px from TD edge. 64px misalignment.',
      '   ▸ Fix: dropped TH padding to 0. `.th-label-name` now spans the full TH width, and its absolute children use the SAME coordinate system as body absolute children (TH outer edge = TD outer edge for the same column). ALOC header at left: 8 matches body alloc at left: 8. DUR header at right: 60 matches body dur. %COM at right: 8 matches the % pill.',
      '',
      'TASK HEADER centered above the body name area:',
      '   ▸ TASK label is absolute-positioned spanning left: 64 to right: 110 — the exact range body task names occupy. `justify-content: center` inside that range centers TASK above the actual task descriptions.',
      '   ▸ Separator line below TASK spans the FULL TH width via a `::after` pseudo on `.th-label-name`.',
      '',
      'BODY NAME range standardized to left: 64 / right: 110 for ALL task rows (no longer conditional on `has-meta`). Anchors, milestones, and regular tasks all share the same name area, AND it matches the TASK header range exactly.',
      '',
      'PICK MODE cursor: crosshair → pointer. "Click this to select" reads better as a hand than a crosshair.',
      '',
      'DURATION text now uses UPPERCASE W (e.g. "10W" not "10w"). Placeholder is "—W". One-character change in cellHtml.',
      '',
      'LINK BADGE moved to the LEFT of the duration value:',
      '   ▸ Was `10W 🔗24` (dur first, then badge). Now `🔗24 10W` (badge first, dur right-aligned).',
      '   ▸ With `text-align: right` on the dur span and the badge inside it, the duration value `10W` stays anchored at right: 60 from the cell edge regardless of whether the row is linked. Consistent dur position across rows.',
      '   ▸ The badge is still inside the .name-cell-dur span with `pointer-events: none` so clicks pass through to the dur for re-editing.',
    ],
  },
  {
    version: '4.42',
    date: '2026-05-19',
    notes: [
      'ROOT CAUSE FOUND for the duration link not working: the node server was started on May 17 and never restarted in 2 days. Ctrl+Shift+R reloads the browser but doesn\'t restart node — so the new server.js code (FIELDS whitelist + cascadeDurationLinks + migration) was sitting on disk but never executing. The `duration_link_task_id` column had never been added to the actual DB. Killed the stale node processes, restarted from disk, verified the migration ran and the column exists.',
      '',
      'CLICK-PICK DURATION LINK now works the way you described:',
      '   ▸ Click duration on row A → editor opens → press = → editor closes, app enters PICK MODE',
      '   ▸ Every other duration cell gets a SDC-lime outline + crosshair cursor to show it\'s clickable',
      '   ▸ Click the duration on row B → row A links to row B, row B\'s duration value copies over, small 🔗N badge appears on row A',
      '   ▸ Escape or clicking outside any duration cell cancels the pick without linking',
      '   ▸ When row B\'s duration changes later, row A\'s cascadeDurationLinks() on the server propagates the change — row A\'s duration follows automatically',
      '',
      '🔗N BADGE is now tiny (font 0.7em, 3px padding) per your "small" request. Lives inside the dur span with pointer-events: none so clicks still hit the dur for re-editing.',
      '',
      'ALIGNMENT — every task name + anchor chip now lines up at the same X:',
      '   ▸ All col-name TDs get padding-left: 64 (was conditional on has-meta). Milestones (Mech 2 Release, BOM Review), anchors (Receipt of PO, Mech 1 Release, Machine Power-Up, FAT, Ship Machine), and regular tasks all start at the same column position. The 0-64px area is empty on rows without alloc/dash.',
      '   ▸ When the α toggle is off (body.hide-alloc-pre), all rows drop to padding-left: 8 since the alloc reservation is gone.',
      '',
      'TASK COLUMN HEADER restyle (two-row layout):',
      '   ▸ Top half: "TASK" centered, bold, with a 1px separator line below.',
      '   ▸ Bottom half: ALOC | DUR | %COM in light gray (--text-muted), 9px font, smaller and quieter than the TASK label.',
      '   ▸ ALOC at left: 8, DUR at right: 60, %COM at right: 8 — same X-coordinates the body values use, so the sub-labels still indicate which column each value lives in.',
      '',
      'The v4.41 diagnostic console.warn / console.log calls in saveCellEdit\'s typed-=N path are still there — if click-pick fails for any reason the console will tell you.',
    ],
  },
  {
    version: '4.41',
    date: '2026-05-19',
    notes: [
      'Three fixes.',
      '',
      'DURATION LINK is now more permissive + emits diagnostic logs:',
      '   ▸ Regex changed from strict `^=\\s*(\\d+)\\s*$` to "starts with =, has digits somewhere after". So "=24", "=24w", "= 24", "=24 ignored text" all link to row 24. Less surprising failures from formatting quirks.',
      '   ▸ Each fail path now logs to the browser console (open DevTools): bad line number, self-link, source-not-found. If a link save mysteriously does nothing, the console will tell you which guard fired.',
      '   ▸ Successful link emits "[duration link] linking task X → task Y (line N) duration D days" so you can verify the right source was picked.',
      '   ▸ Link badge now emits with INITIAL `🔗?` content from cellHtml — visible immediately, doesn\'t depend on the line-number resolver running afterward. updateLineNumbersAndPreds replaces `?` with the source\'s actual line number on the next pass.',
      '',
      'DESCENDERS no longer clip at the row bottom:',
      '   ▸ `.name-cell-main` was `display: block`, which flowed the task name at the TOP of the TD content area. With `overflow: hidden` on the TD, descenders (g, y, p, j) hanging below the line box got chopped by the cell border.',
      '   ▸ Fix: `.name-cell-main` is now absolutely positioned with `top: 50%; transform: translateY(-50%)`, vertically centered in the row. Line-height bumped 1.1 → 1.2 so the line box has slightly more room for descenders above + below the baseline. Tested at every row height from 18 (floor) up — descenders sit comfortably inside the cell.',
      '   ▸ The name-cell-main now uses CSS class-aware positioning: `left: 8px / right: 52px` by default, `left: 64px / right: 110px` when the TD has the `has-meta` class (alloc + dur present), and `left: 8px` again when `body.hide-alloc-pre` is on. Same alignment with the TASK header label as before.',
      '',
      'If the link STILL doesn\'t work after this — check DevTools console while you save. The diagnostic logs will show which guard is firing.',
    ],
  },
  {
    version: '4.40',
    date: '2026-05-19',
    notes: [
      'Root cause of the alloc-misalignment bug FOUND.',
      '',
      '   ▸ The Task column has long had per-depth indentation: `tr.depth-2/3/4 > td.col-name { padding-left: 22/36/50 }`. Sub-dep tasks (depth-4) got padding-left: 50px on their TD.',
      '   ▸ My v4.30+ has-meta rule (`td[data-col="name"].has-meta { padding-left: 64 }`) had LOWER CSS specificity than the depth rules (0,2,1 vs 0,2,2), so depth-4 won and body TD ended up with padding-left: 50, while the header TH had padding-left: 64.',
      '   ▸ Per CSS spec, absolute children of a `position: relative` TD anchor to the TD\'s padding-edge (= outer edge with no border), so the alloc at left: 8 SHOULD be at 8 from cell edge regardless of padding-left. But some browsers anchor to the content-edge instead, putting body alloc at 50 + 8 = 58 while header sits at 64 + 8 = 72 — both shifted, and at different X positions, so they didn\'t align.',
      '   ▸ FIX: deleted the depth-N padding-left rules entirely. The section + sub-dep headers already make hierarchy obvious, the per-row indent was redundant chrome. Body TD padding-left now consistently 64 (from has-meta) — matching the header.',
      '   ▸ ALSO simplified the alloc-pre and th-label-alloc styling so they\'re byte-for-byte identical: same left: 8px, same padding: 1px 3px, same border-radius. No more margin-left tricks. Both text positions land at 11px from cell edge, perfectly aligned.',
      '',
      'If you re-link a duration and the source still doesn\'t propagate, the server needs a restart so the v4.38 cascadeDurationLinks() function with the end_date recompute is actually running. Hot-reload doesn\'t pick up server.js changes — only the public/ static files.',
    ],
  },
  {
    version: '4.39',
    date: '2026-05-19',
    notes: [
      'BIG restructure: absolute positioning everywhere + task name font bump + link icon. Should finally fix the alloc alignment and link visibility issues.',
      '',
      'ABSOLUTE-POSITIONED TASK COLUMN (no more flex wrapper):',
      '   ▸ The v4.30–v4.38 .name-cell-row flex wrapper kept producing alignment glitches between body and header alloc. Replaced entirely.',
      '   ▸ Now: alloc, dash, and dur are absolute-positioned children of the TD with EXACT pixel offsets from the cell edge (alloc at left: 8px, dash at left: 46px, dur at right: 60px). The TD has padding-left: 64px reserved for alloc + dash, and padding-right: 64px reserved for dur + the % pill.',
      '   ▸ Header uses the SAME absolute offsets — ALOC label at left: 8px, DUR label at right: 60px, %COM at right: 8px. Body and header now align pixel-for-pixel regardless of browser flex quirks.',
      '   ▸ TASK NAME flows in the TD\'s content area between padding-left and padding-right.',
      '   ▸ Tasks WITHOUT alloc (milestones / anchors / backlog) skip the has-meta class so they get default padding-left: 8px — name flush at the column edge like before.',
      '   ▸ α toggle off (body.hide-alloc-pre) drops padding-left back to 8 on both header and body, so the task name and TASK label slide left to fill the freed space.',
      '',
      'TASK NAME font BIGGER (proportional to row height):',
      '   ▸ font-size: clamp(9px, calc(var(--row-h) * 0.55), 18px) — was ratio 0.42 capped at 13px.',
      '   ▸ At default rh=26: ~14px (was 11). At rh=44: capped at 18 (was 13). At rh=18 floor: clamped 9.',
      '   ▸ Alloc + dur ratios unchanged at 0.36 / 0.42 — task name is now visibly the biggest text in the column, with alloc and duration as smaller metadata around it.',
      '   ▸ The ratio is constant via clamp\'s calc() term, so shrinking the row scales the name down proportionally (within the 9-18px clamp bounds).',
      '',
      'LINK BADGE now shows a real 🔗 icon:',
      '   ▸ Was "→5". Now "🔗5". The link emoji makes the linkage visible at a glance.',
      '   ▸ Lives inside the dur span (pointer-events: none) so clicks still open the dur editor.',
      '',
      'NOTE: if the duration link isn\'t propagating to the dependent (e.g. you change line 17 and line 18 stays put), make sure the SERVER has been restarted after the v4.38 cascadeDurationLinks() fix landed. The DB migration that adds duration_link_task_id only runs on server start.',
    ],
  },
  {
    version: '4.38',
    date: '2026-05-19',
    notes: [
      'Four fixes.',
      '',
      'ALLOC at left edge (for real this time):',
      '   ▸ Both `.name-cell-alloc-pre` (body) and `.th-label-alloc` (header) dropped horizontal padding from `1px 4px` → `1px 0`. The 4px left padding was pushing alloc text 4px right of where the header sat — and combined with the row-phase-color stripe overlaying the leftmost 4px of the TD, the visual offset felt much larger than it actually was.',
      '   ▸ `.name-cell-dur` + `.th-label-dur` got the same `1px 0` padding for consistency. min-width values unchanged so the click target stays the same.',
      '',
      'DURATION LINK cascade now updates dependent END_DATE too:',
      '   ▸ Symptom: changing line 17 propagated `duration_days` to line 18 (linked via "=17") but line 18\'s bar on the Gantt didn\'t shorten/extend because `end_date` was stale.',
      '   ▸ Fix: `cascadeDurationLinks()` in server.js now recomputes `end_date` = `start_date + (new_dur - 1) business days` for every dependent on every cascade pass. Also toggles `is_milestone` when the new duration is 0. Existing linked rows pick up correctly the next time their source\'s duration changes.',
      '   ▸ NOTE: requires a SERVER RESTART for the new cascade logic to take effect. The DB migration that added `duration_link_task_id` runs on startup too.',
      '',
      'GANTT PHASE LEGEND removed:',
      '   ▸ The "ME blue / CE green / Engineering purple / Build orange / Wire yellow / Testing red" legend below the Gantt was redundant — the hierarchy palette + section dividers already make the assignments obvious without a key.',
      '   ▸ `renderGanttLegend()` retained as a no-op so call sites stay valid; deleted only the markup in index.html and made the function null-safe.',
      '',
      'ROW-HEIGHT DRAG HANDLE removed:',
      '   ▸ The "⇕ Drag to resize rows" bar below the Gantt is gone. The zoom-stack +/- buttons in the toolbar (top right) give the same control — the bar was a redundant second affordance eating vertical space below the chart.',
      '   ▸ `setupRowHeightHandle()` already had an early-return for missing element, so JS stays happy.',
    ],
  },
  {
    version: '4.37',
    date: '2026-05-19',
    notes: [
      'Two changes: alloc column alignment fix + NEW duration linking feature.',
      '',
      'ALOC HORIZONTAL ALIGNMENT — body alloc now lands under the ALOC header.',
      '   ▸ The body cell\'s alloc-pre had padding 1px 4px (~44px effective width with min-width 36). The header\'s th-label-alloc had no padding (36px wide). That offset everything to the right of alloc — task-name + duration drifted right of where the matching header labels were.',
      '   ▸ Fix: header labels (.th-label-alloc, .th-label-dur) now carry the same padding as the body cells, and a new .th-label-dash spacer in the header matches the body\'s en-dash position. Body + header columns line up pixel-for-pixel.',
      '',
      'DURATION LINKING — type "=N" to link this task\'s duration to row N.',
      '   ▸ Click duration → editor opens with the current value (or "=<line>" if already linked) → type "=5" to link this task\'s duration to row 5\'s duration → save.',
      '   ▸ When the source task\'s duration changes, every linked dependent updates automatically (server-side cascade on every PUT).',
      '   ▸ Visual indicator: a small SDC-blue chip "→5" appears after the duration value when linked, showing the source row number. Pointer-events: none so clicks pass through to the duration for re-editing.',
      '   ▸ Unlink: re-open the editor, replace with a normal duration like "2w" (no = prefix), save. The link clears and the duration is independent again.',
      '   ▸ Edge cases handled server-side: self-link refused, deleted source falls back to the locally-stored duration_days, chained links (A→B→C) cascade over multiple iterations.',
      '   ▸ Storage: new column `tasks.duration_link_task_id` (nullable INTEGER), backfill-safe via the existing column-migration array in db.js. Existing tasks have it as null = no link.',
      '   ▸ Use case (per your request): create a Shop Debug task linked to the Engineer Debug task — change the Engineer duration and Shop Debug shifts with it. Or duplicate any task and decide later whether the duplicate should follow its source.',
      '',
      'Click-to-select pick mode (type "=" then click another task) wasn\'t implemented in this pass — typing "=N" directly works for now. Tell me if you want the click-pick flow too and I\'ll wire it.',
    ],
  },
  {
    version: '4.36',
    date: '2026-05-19',
    notes: [
      'α (alloc) toggle now overrides the v4.22 Sales-workspace allocation suppression.',
      '',
      '   ▸ Problem: opening any Sales-workspace project (e.g. SDC_Sales_Template) showed no allocation in the Task column even with the α icon turned ON. v4.22 had hardcoded Sales tasks to skip the allocation pre-pill entirely, which made the α toggle look broken on those projects.',
      '   ▸ Fix: removed the Sales-workspace short-circuit from cellHtml. The α icon is now the SINGLE authoritative control for allocation visibility in the Task column — flip it off when presenting to a customer / sales rep (alloc hides across ALL projects regardless of workspace), flip it on otherwise. Sales projects show alloc just like Active projects when α is on.',
      '   ▸ The dedicated Allocation column (hidden by default since v4.27) still shows a hatched empty cell for Sales tasks if you un-hide it. That standalone column wasn\'t part of this fix — let me know if you want the Sales suppression dropped there too.',
      '   ▸ Assignee column still hatches blank for Sales tasks (separate from alloc, unchanged).',
    ],
  },
  {
    version: '4.35',
    date: '2026-05-19',
    notes: [
      'Three small fixes in the Task column.',
      '',
      '   ▸ ALLOC visibility — removed the v4.34 negative-margin trick (margin: 0 -2px combined with padding: 0 2px) on .name-cell-alloc-pre and .name-cell-dur. Negative margins inside a flex container with gap can interact unexpectedly across browsers, and the alloc text was rendering blank in some cases as a result. Plain padding now — visual position shifts a few pixels but the text shows reliably.',
      '',
      '   ▸ TASK header label is bold — `.th-label-task` got `font-weight: 800` and the SDC navy color so the primary column label pops above the smaller alloc / dur / %COM sub-labels. The whole label row reads now as "ALOC  TASK  DUR  %COM" with TASK as the visual anchor.',
      '',
      '   ▸ Customer-view hide list updated — the rule was still referencing `.name-cell-dur-post`, which was removed in v4.34 (duration is now a direct flex child, no wrapper). Replaced with `.name-cell-dur` + `.name-cell-dash` so customer view properly hides ALL the internal Task-column chrome (alloc, dash, duration), leaving just the task name.',
      '',
      '   ▸ The ALOC header label still hides when the α View pill toggle is off (via body.hide-alloc-pre .th-label-alloc { display: none }, added in v4.34). Same toggle controls both the body cells AND the header label.',
    ],
  },
  {
    version: '4.34',
    date: '2026-05-19',
    notes: [
      'Task column polish — alloc + duration act like the % complete pill: hover gets a subtle background tint, click opens an inline editor, no underlines.',
      '',
      'HOVER + EDIT BEHAVIOR — alloc and duration now read as pills WITHOUT looking like pills:',
      '   ▸ Removed `text-decoration: underline` on hover for both. The earlier underline was confusing on alloc because it stretched under the " – " dash separator too (since the dash was a CSS ::after pseudo on the alloc span).',
      '   ▸ Replaced with a subtle `background: rgba(15, 23, 42, 0.08)` tint on hover with a `border-radius: 3px`. Reads as a soft pill hover state without any persistent pill border / background — same UX pattern the % complete pill uses, just no static fill.',
      '   ▸ Click target unchanged: clicking opens the inline editor exactly like the % pill does, no jumping (the v4.33 font-inherit fix still applies).',
      '',
      'DASH SEPARATOR moved out of the alloc span:',
      '   ▸ The " – " between alloc and task name is now its OWN sibling element (`<span class="name-cell-dash">`), not a CSS ::after pseudo on the alloc span. Hover effects on alloc no longer bleed into the dash.',
      '   ▸ The dash has `pointer-events: none` so clicks on it pass through to the row, not to the dash (which isn\'t editable).',
      '',
      'DURATION clickability:',
      '   ▸ Dropped the `.name-cell-dur-post` wrapper that was nesting the duration inside a non-edit span. The `.name-cell-dur` editable span is now a DIRECT flex child of `.name-cell-row`. Clicks land cleanly without the nested-flex layout that was confusing earlier.',
      '',
      'TASK COLUMN HEADER now shows multi-label sub-regions:',
      '   ▸ Header reads `ALOC | TASK | DUR | %COM`, laid out with the same flex skeleton as the body cell so each label sits directly above the body value it describes. ALOC is hidden when the α View pill toggle is off.',
      '   ▸ Header uses the same SDC-blue color for ALOC and dark navy for DUR so the colored values in the body cells echo their header labels.',
    ],
  },
  {
    version: '4.33',
    date: '2026-05-19',
    notes: [
      'Two follow-up fixes for v4.32.',
      '',
      'ANCHOR ROW BORDERS now span the FULL ROW again (heavy 2px restored).',
      '   ▸ v4.30 moved the Task column to display:flex on the <td> itself. That demotes the cell from `display: table-cell`, which in some browsers prevents border-collapse from rendering the cell\'s borders in sync with the rest of the row — so anchor rows looked like they had a dark border around every column EXCEPT the task cell.',
      '   ▸ Fix: the flex layout moved off the <td> and onto an inner <div class="name-cell-row"> wrapper. The TD goes back to being a real table-cell. With border-collapse rendering working again, the anchor row\'s 2px top + bottom borders span the entire row uniformly — same look you had before v4.32.',
      '   ▸ Anchor TD vertical padding zeroed to keep row height matching every other row even with the 2px borders. Chip font scales with --row-h (matches the .name-cell-main font) and uses tight line-height so the chip never forces the row taller.',
      '',
      'INLINE EDITOR no longer jumps when you click alloc or duration.',
      '   ▸ Allocation editor used to slide DOWN slightly when opened — the old .name-pill-input rule forced font-size 10px + text-align:center + its own height, which gave the input a different baseline + line metrics than the surrounding alloc text. The alloc text scaled with --row-h (e.g. 11px) and was naturally left-aligned, so swapping in a 10px-center input shifted everything.',
      '   ▸ Duration editor used to shift LEFT for the same reason (text-align:center reflowed the duration text\'s position).',
      '   ▸ Fix: alloc + dur inputs now INHERIT font-size, font-weight, color, text-align, line-height, height from their parent span. The input renders pixel-identical to the text it replaces, no shift.',
      '   ▸ The right-edge % complete pill editor is unaffected by these overrides — it still uses the 10px center-aligned style designed for that pill\'s 38×16 footprint.',
    ],
  },
  {
    version: '4.32',
    date: '2026-05-19',
    notes: [
      'Two bug fixes.',
      '',
      'INLINE EDITOR JUMP fixed.',
      '   ▸ Clicking the allocation, duration, or % complete text used to open the editor in a visibly different spot from the text you clicked. Root cause: the <input> created by enterPillEdit had no explicit width, so the browser-default ~150px width either (a) blew up the flex layout — pushing the task name right when editing alloc/dur — or (b) grew the absolute-positioned % pill leftward, repositioning the editor.',
      '   ▸ Fix: enterPillEdit now sets input.style.width per column type — 36px for allocation, 44px for duration, 40px for progress. Matching min-width on the parent spans (.name-cell-alloc-pre, .name-cell-dur) so they don\'t collapse the other direction either. Dropped !important on .name-pill-input\'s width so the inline style actually wins specificity. Editor opens exactly where you clicked.',
      '',
      'MILESTONE ROW HEIGHT now matches every other row.',
      '   ▸ Anchor / milestone rows (Receipt of PO, Mech 1 Release, FAT, Ship Machine, Backlog) had 2px borders + 4px vertical padding + an inline-block chip with its own 2px vertical padding. Combined with box-sizing: border-box and the chip\'s inherited line-height, the content footprint exceeded var(--row-h) by 3-6px → row grew taller than the rest of the grid, creating visible jumps in row height across the spine markers.',
      '   ▸ Fix: borders 2px → 1px, td vertical padding 4px → 2px, chip vertical padding 2px → 0, chip font scales with --row-h (matching .name-cell-main), chip line-height 1.1. Visual emphasis comes from the anchor-fill background tint + bold + the lime chip — same look, just no extra height. Anchor rows now share the exact same height as task rows across every row-h setting.',
    ],
  },
  {
    version: '4.31',
    date: '2026-05-19',
    notes: [
      'Task column polish — alloc smaller with an en-dash separator, duration bold without the leading minus, more space before the % pill.',
      '',
      '   85% – Configure Machine   1w        [○ 25%]',
      '   \\_/   \\______________/   \\__/        \\_____/',
      '   blue    bold name         bold dur    % pill',
      '',
      'CHANGES vs v4.30:',
      '   ▸ ALLOCATION font dropped one notch (scale ×0.36 vs name\'s ×0.42), so it sits slightly smaller than the task name — reads as metadata, doesn\'t compete with the name. Still SDC primary blue, still click-to-edit.',
      '   ▸ EN-DASH SEPARATOR after the allocation, rendered via CSS ::after on .name-cell-alloc-pre. Muted color so it reads as punctuation, not part of either side. The dash STAYS PUT when you click to edit (since it\'s a pseudo-element, not part of the editable span\'s text).',
      '   ▸ DURATION is now BOLD (was regular muted) and uses dark text (sdc-dark) so it stands out on its own — no leading dash needed. The earlier " - 1w" pattern was reading as a literal minus sign on the duration value.',
      '   ▸ DURATION → % PILL gap bumped from ~6px to ~18px via margin-right: 12px on .name-cell-dur-post. The duration doesn\'t crowd the right-edge progress pill anymore.',
      '',
      'OTHER OPTIONS WE COULD TRY if this still feels off:',
      '   • Parens around duration: "Configure Machine (1w)"',
      '   • Middle dot before duration: "Configure Machine · 1w"',
      '   • Push duration all the way RIGHT (next to but not crashing into the % pill) so name + dur read as bookends',
      '   • Drop the en-dash and just rely on color/weight contrast for the alloc/name boundary',
      '   Just say the word for any of these.',
    ],
  },
  {
    version: '4.30',
    date: '2026-05-19',
    notes: [
      'TASK COLUMN restructured to a single-line PLAIN-TEXT layout. No more pills wrapping alloc / duration.',
      '',
      'New format:',
      '   85%  Configure Machine - 1w           [○ 25%]',
      '   \\_/  \\______________/ \\__/            \\_____/',
      '   alloc    task name      dur            % pill',
      '',
      '   ▸ ALLOCATION (e.g. "85%") renders as plain SDC-blue text in front of the task name. Click to edit (input takes its place, same enterPillEdit machinery). No more pill border or hover background.',
      '   ▸ TASK NAME stays bold black, font scales with --row-h (clamp(9px, rh × 0.42, 13px)).',
      '   ▸ " - DURATION" (e.g. " - 1w") renders as plain muted-gray text after the task name. Also click-to-edit.',
      '   ▸ % COMPLETE PILL unchanged — still pinned to the right edge of the cell.',
      '',
      'NEW VIEW TOGGLE α — show/hide the inline allocation prefix.',
      '   ▸ The α icon sits between ≡ (flatten) and $ (financials) in the View pill cluster. Click to hide just the leading "85% " portion of every task row; click again to bring it back.',
      '   ▸ Use case: presenting to a customer or anyone else who shouldn\'t see staffing percentages. Customer-view mode hides it automatically along with the other internal chrome.',
      '   ▸ State persists in localStorage (sdcSchedulerScheduleView.showInlineAlloc) — defaults to ON.',
      '',
      'ROW_H_MIN dropped 22 → 18 since single-line content fits in 18px comfortably (was 22 for the stacked layout in v4.27–v4.29). Hitting the row-height minus button now actually does something useful again.',
      '',
      'Cleanup:',
      '   ▸ Stacked-layout CSS (.name-cell-stack, .name-meta-row, .name-meta-pill, .name-meta-dot) all deleted.',
      '   ▸ body.row-h-compact toggle in applyRowHeight removed — single-line layout doesn\'t need it.',
      '   ▸ Customer-view hide list updated: drops the new .name-cell-alloc-pre + .name-cell-dur-post in customer mode (alongside the % pill / milestone check / drift chips / section numbers already hidden there).',
    ],
  },
  {
    version: '4.29',
    date: '2026-05-19',
    notes: [
      'CUSTOMER VIEW: section numbers now hide.',
      '   ▸ Section header rows render as "10 DESIGN & BUILD" / "40 MACHINE TESTING" / "50 TEARDOWN & INSTALL" in normal mode — the leading "10/40/50" is an SDC-internal convention.',
      '   ▸ In customer view, the leading number portion hides via CSS (.group-label-num gets display:none under body.customer-view). The section name itself stays so the customer still sees the work-phase headers, just without the SDC accounting code.',
      '   ▸ Implementation: headerRowHtml wraps the leading "NN " in its own <span class="group-label-num"> so the rest of the label is untouched. No JS re-render needed when toggling customer view — pure CSS swap.',
    ],
  },
  {
    version: '4.28',
    date: '2026-05-19',
    notes: [
      'GANTT BAR LABELS — short bars now show the FULL task name OUTSIDE the bar (like milestone labels) instead of ellipsis-truncating inside.',
      '',
      '   ▸ Before: a 1-week bar (e.g. "Configure Machine" at Week zoom) was too narrow to fit its name, so clipBarLabels would either render an ellipsis-truncated stub ("Conf…") or — at extreme zoom-out — nothing at all. You couldn\'t tell what the bar was without hovering.',
      '   ▸ After: when the name doesn\'t fit inside, it renders to the RIGHT of the bar with full text, exactly like milestone labels do. Reads cleanly even on bars 1-day wide.',
      '   ▸ Smart placement: if going right would clip past the SVG\'s right edge AND there\'s room on the left, the label flips to the LEFT side (text-anchor: end). Otherwise stays right (the Gantt panel scrolls horizontally so off-edge labels are still reachable).',
      '   ▸ Z-order: labels live inside their bar-wrapper which paints AFTER `.sdc-arrows`, so arrows pass behind the text — exactly what you asked for ("the wording is the top layer").',
      '   ▸ Title-attribute hover tooltips are dropped on outside labels since the full text is now visible.',
    ],
  },
  {
    version: '4.27',
    date: '2026-05-18',
    notes: [
      'Task column meta stays visible at every row height + Alloc / Duration columns hidden by default.',
      '',
      'STACKED LAYOUT — no more disappearing alloc/duration:',
      '   ▸ Hitting the row-height minus button used to hide the alloc · duration meta row entirely once row-h dropped below 28px (the "row-h-compact" trick). That made small rows look broken — you\'d shrink for density and lose the data you wanted to see.',
      '   ▸ Now: the meta row STAYS at every row height. Instead, the TASK NAME font scales down with --row-h via CSS clamp(9px, --row-h × 0.42, 13px). Smaller rows just have smaller name text; the meta row\'s pills stay at the readable 9px floor.',
      '   ▸ ROW_H_MIN bumped 14 → 22px since the stacked content needs ~21px to fit without clipping. The user accepted "won\'t go quite as small" — and that\'s the trade-off.',
      '',
      'ALLOC + DURATION columns now HIDDEN by default:',
      '   ▸ Both values already live as click-to-edit pills inside the Task name column\'s meta row, so the dedicated grid columns just eat horizontal space. Hiding them mirrors what we did with % Complete in v4 (its column went away when the pill moved into Task).',
      '   ▸ Existing users get a one-time localStorage migration (LAYOUT_VERSION bumped 5 → 6) that drops `allocation` + `duration` from saved visibleCols. Toggle them back on from the columns dropdown if you actually want the standalone columns.',
      '',
      'Result: schedule grid reads tighter horizontally (lost ~140-160px of column width), and shrinking rows keeps the alloc/dur context — just at smaller name text.',
    ],
  },
  {
    version: '4.26',
    date: '2026-05-18',
    notes: [
      'Reverted v4.25 single-line Task column — user preferred the stacked layout (meta row UNDER the task name).',
      '',
      '   ▸ Task name on top, allocation · duration meta row underneath. Order is now ALLOCATION first, DURATION second (carrying forward the v4.25 ordering decision).',
      '   ▸ Compact-mode meta-hide restored: when row-height drops below 28px, the meta row hides so rows can still shrink to the floor. Allocation + duration stay editable from their dedicated grid columns when the meta row is hidden.',
      '   ▸ The click-outside dismiss fix from v4.25 stays — clicking the Gantt (or anywhere else outside the inline editor) still commits + closes the editor, same as Enter. That was a separate fix unrelated to layout.',
      '   ▸ Customer view: meta row hides alongside the % pill / milestone check / drift chips so customers see a clean task-name-only column.',
    ],
  },
  {
    version: '4.25',
    date: '2026-05-18',
    notes: [
      'TASK COLUMN flattened to a SINGLE LINE. Layout is now:',
      '   [alloc] [dur]   Task Name      …       [% complete pill]',
      '',
      '   ▸ The allocation + duration pills moved out of the stacked meta row (which sat underneath the task name in v4.19–v4.24) and onto the same line, BEFORE the task name. Reads left-to-right as "85% · 3w · Configure Machine" — the staffing context comes first, then the work.',
      '   ▸ Rows can now shrink all the way to the row-h floor (14px) without losing alloc/duration visibility. The v4.23 compact-mode hack that hid the meta row at small sizes is gone — single-line layout makes it unnecessary.',
      '   ▸ Both pills still click-to-edit via the same enterPillEdit machinery the % pill uses. Pills hover a thin border so they read as clickable.',
      '   ▸ Milestones / anchors / backlog rows skip the pre-pills entirely (no meaningful allocation × duration there) — the task name uses the whole cell.',
      '',
      'CLICK-OUTSIDE DISMISS for inline pill / cell editors:',
      '   ▸ Previously, clicking a duration pill opened the inline editor, but clicking the Gantt didn\'t dismiss it — you had to click another grid cell to lose focus. The Gantt is SVG which isn\'t focusable by default, so blur() never fired.',
      '   ▸ Fix: both enterPillEdit and enterCellEdit now attach a document-level mousedown listener (capture phase) that commits + closes the editor when the user clicks ANYWHERE outside the input — Gantt area, toolbar, sidebar, anywhere. Same behavior as Enter. Esc still cancels without saving.',
      '',
      'Customer view: the new pre-pills hide in customer mode along with the % pill / milestone check / drift chips — clean task name only when presenting to customers.',
    ],
  },
  {
    version: '4.24',
    date: '2026-05-18',
    notes: [
      'Reverted the v4.23 vertical-band experiment + cleaned up the leftover issues it surfaced.',
      '',
      'BAND COLUMN → ROLLED BACK. The vertical-text band column on the left of the grid was conceptually right (kill the empty Gantt rows that horizontal sub-dep headers created) but visually worse than the original — too much color, collapse interaction felt awkward, label clipping on short bands was ugly. Reverted entirely.',
      '',
      'HORIZONTAL SUB-DEP HEADERS — slimmed to ~14px (was ~24px).',
      '   ▸ level-2 (dept) and level-3 (sub-dep) header rows now use a tighter height + 10px font + smaller caret. They still read as clear section dividers but eat half the vertical space they used to. Net effect: schedule looks ~as tight as the v4.23 band experiment was supposed to deliver, without any of the visual cost.',
      '   ▸ level-1 (Section 10/40/50) headers unchanged — those are structural and should still feel substantial.',
      '   ▸ Collapse interaction is back to what felt good before — click the slim header row to fold the group.',
      '',
      'MILESTONE DIAMOND SIZE — decoupled from the v4.23 bar-height ratio bump.',
      '   ▸ Task bars stay at the bumped 0.7 × row-height (so they dominate the row, per your earlier feedback).',
      '   ▸ Milestone diamonds now compute off a SEPARATE reference of 0.5 × row-height (the OLD bar ratio). So diamonds stay the same size they were pre-v4.23 even though task bars grew. Also fixes the arrow-termination + label-offset radii so arrows still meet the diamond edge cleanly.',
      '',
      'TASK COLUMN META ROW — flipped + shrunk.',
      '   ▸ Order is now DURATION · ALLOCATION (was allocation · duration). Reads as "how long? at what staffing?" which matches the natural mental flow.',
      '   ▸ Font 10px → 9px, line-height 1.3 → 1.1, margin-top 1 → 0, pill padding 1px-5px → 0-4px. Net: the meta row\'s vertical footprint drops by ~4px, so two-line task rows feel meaningfully tighter without losing the click-to-edit affordance.',
    ],
  },
  {
    version: '4.23',
    date: '2026-05-18',
    notes: [
      'Vertical band column + Gantt density tuning.',
      '',
      'HOTFIX (same day): the first-cut band column had `.band-cell-inner` as a normal-flow block with writing-mode: vertical-rl. That made the rotated label\'s INLINE-SIZE = text length (~80px for "CTRLS ENG"), which combined with rowspan forced EVERY row in a band to be 80px+ tall. Table unusable — each row as big as the whole table used to be. Fixed by absolute-positioning the inner so it can\'t contribute to the <td>\'s intrinsic height; the rowspan\'d cell now keeps the natural sum-of-row-heights and the label clips (overflow: hidden) when the band is too short to show it all. Title attr on the cell still shows the full label on hover.',
      '',
      'NEW: VERTICAL BAND COLUMN replaces the horizontal sub-dep header rows.',
      '   ▸ Section 10 alone used to spend 6 rows just on sub-dep headers (MECH ENG, CTRLS ENG, GEN ENG, PROCUREMENT, BUILD, WIRE) — each one a full-width strip with NOTHING on the Gantt next to it, so the schedule grew vertically just to show the dividers.',
      '   ▸ Now every sub-dep / leaf-dept renders as a NARROW LEFT COLUMN with its label rotated bottom-to-top (book-spine style). The cell carries the hierarchy color (mech blue, controls green, build orange, wire yellow, etc.) and a ▾ / ▸ caret for collapse / expand.',
      '   ▸ Tasks stack DIRECTLY against each other on the Gantt — no more empty filler rows between groups. The vertical band on the left tells you which group each task belongs to.',
      '   ▸ Click the band to collapse the group → it shrinks to a single placeholder row "(N tasks hidden — click band to expand)". Drop a dragged task on an empty / collapsed band to re-classify it into that group.',
      '   ▸ Section-level (10 / 40 / 50) headers stay as full-width horizontal strips — only 3 of them, and they\'re the structural backbone you want to read horizontally.',
      '   ▸ Anchor rows (Receipt of PO, FAT, Ship Machine, Backlog) get an empty band cell so the column stays aligned without false labels on the spine markers.',
      '',
      'GANTT BAR HEIGHT ratio bumped 0.5 → 0.7 so bars FILL more of each row.',
      '   ▸ At the default 26px row, bar grew from 13px → 18px. At the 14px floor, bar grew from 10px → 10px (BAR_H_MIN floor) but padding tightened so adjacent bars sit nearly touching.',
      '   ▸ Visually: tall grid rows used to contain a short Gantt bar floating in empty space. Now the bar dominates the row.',
      '',
      'ROW SHRINKING actually works again.',
      '   ▸ The v4.19 stacked-Task-column layout (name + meta row) was forcing TD body height to ~24px regardless of the row-height slider — the slider would drag the Gantt smaller but the grid rows refused to follow.',
      '   ▸ New compact mode kicks in when row height drops below 28px: the meta row hides (allocation + duration are still editable from their own grid columns), name line-height tightens, and the band column squeezes from 30px to 24px.',
      '   ▸ You can now drag the row-height slider all the way to the 14px floor and the grid + Gantt shrink together. Tight density view for project overview; spread back out by bumping the slider up.',
    ],
  },
  {
    version: '4.22',
    date: '2026-05-17',
    notes: [
      'SDC STANDARD ALLOCATION baked into the standard template + Sales-workspace suppression.',
      '',
      'STANDARD ALLOCATION on SDC_StandardProject_Template:',
      '   ▸ ENGINEERING tasks → 85% (was a mix of 90% / 100% / 85%). Covers: Mechanical Design, ME 2, Configure Machine, Test/Debug Engineer 1+2, Controls Design, Controls Drawings, Controls Software, HMI Programming, Robot Programming, Vision Programming, Install Engineer 1.',
      '   ▸ SHOP tasks → 90% (was a mix of 90% / 100%). Covers: Builder 1+2, Build and Wire Panel, Machine Wiring, Shop Debug, Teardown 1+2, Install Shop 1+2.',
      '   ▸ Milestones, anchors, procurement, and the spine-level rows (Receipt of PO / FAT / Ship Machine / Mech 1 Release / Order Long Lead / etc.) untouched — they don\'t consume staffing hours.',
      '   ▸ Why the split: SDC engineers reserve ~15% of their week for meetings / interruptions / non-project work; shop staff sit at ~10%. These numbers come straight from how time clocks have averaged out historically.',
      '   ▸ Future projects copied from this template inherit the new allocations automatically (the copy reads task rows verbatim).',
      '',
      'SALES WORKSPACE — allocation + assignee suppressed in the grid:',
      '   ▸ Pre-quote schedules don\'t need staffing detail and showing "85% × ME Placeholder" risks accidentally communicating commitments to the customer or sales rep before the deal is signed.',
      '   ▸ Effect on every Sales-workspace project (auto-detected by name match OR explicitly set via right-click → Workspace: Sales):',
      '       • Task column meta row drops the allocation pill — duration pill still shows so the work block has scope.',
      '       • Assigned To column renders blank with a subtle diagonal-hatch background (cell is intentionally empty, not "missing data").',
      '       • Allocation column renders the same blank-hatch cell.',
      '       • Gantt bar-meta overlay (% icon) drops the "85%" portion; the "2w" portion still annotates the bar.',
      '       • Clicking the suppressed cells does nothing (no editor opens) — tooltip explains why.',
      '   ▸ ACTIVE-workspace projects keep all the staffing detail you expect; nothing else changed for them.',
      '   ▸ If a Sales project later moves to Active workspace (right-click → Workspace: Active), the allocation + assignee data the underlying tasks carry is unchanged — just hidden. Move it back and the staffing detail reappears.',
    ],
  },
  {
    version: '4.21',
    date: '2026-05-17',
    notes: [
      'Sidebar icon polish + Flatten toggle in customer view.',
      '',
      'SIDEBAR ICONS — finally settled on a set that reads cleanly:',
      '   ▸ PROJECTS → inline-SVG stacked Gantt bars (3 staggered bars). Replaces the 📋 clipboard emoji — automation-themed, on-brand for a scheduler, and renders white like every other rail element instead of fighting with its own emoji colors.',
      '   ▸ FAVORITES → text-style ★ (white). Replaces the colored ⭐ emoji which was the only orange-yellow thing on the rail and pulled the eye to it constantly. The plain white character matches the rest of the sidebar.',
      '   ▸ RECENTS → inline-SVG clock outline (circle + 2 line hands). Replaces the 🕒 emoji which renders with bright colored hands on most platforms.',
      '   ▸ SETUP → reverted ⚙ gear (you said the hammer-and-wrench was too busy after a day with it; the gear reads as "settings" without needing a glance).',
      '   ▸ DEPARTMENTS → 👥 unchanged.',
      '   ▸ The two SVGs use stroke="currentColor" / fill="currentColor" so they pick up the same muted-white → solid-white hover state the labels do.',
      '',
      'CUSTOMER VIEW — Flatten toggle:',
      '   ▸ New floating "≡ Flatten" button sits next to the existing "✕ Exit customer view" button (top-right). Clicking it toggles flatten + sort-by-start together — same linked pair the editing view\'s ≡ pill icon controls (per v3.58, those two flags always move in lockstep since flatten without start-date sort almost never makes sense).',
      '   ▸ When ON, the button fills with solid SDC lime so you can see at a glance that the customer view is currently flattened.',
      '   ▸ When entering customer view, the button reflects the CURRENT flatten state (carried over from the editing view) — it doesn\'t reset.',
      '   ▸ Exit + Flatten share the same lime-outlined floating-button styling so they read as a paired control cluster.',
      '',
      'No other behavior changes — Task column meta pills, project tab simplification, and v4.20\'s grid editing all behave the same as before.',
    ],
  },
  {
    version: '4.20',
    date: '2026-05-15',
    notes: [
      'Three polish passes:',
      '',
      '   ▸ TASK COLUMN meta row is now click-to-edit. Click the allocation pill (e.g. "50%") → small inline input opens to edit the allocation, same enterPillEdit machinery the % complete pill uses. Click the duration pill (e.g. "3.2w") → inline edit, accepts both "5d" and "2w" syntax. Both pills always show now, even at 0 (rendered as "—%" / "—w") so they\'re click-targets for first-time setting. The Allocation and Duration grid columns can be hidden via the column dropdown now — the meta row + click-to-edit makes them redundant.',
      '',
      '   ▸ PROJECT TABS simplified. The previous version tinted each tab\'s entire background by workspace (lime / light-blue / yellow), which made it hard to tell at a glance which tab was actually SELECTED — too many colors fighting for attention. Now:',
      '       • All inactive tabs share the same neutral dark background',
      '       • A small COLORED DOT (8px) before the project name shows the workspace — lime for Active, light blue for Sales, yellow for Closed',
      '       • The SELECTED tab gets a bold 2px SDC-LIME BORDER (top + sides) + a brighter background, so it pops unmistakably regardless of which workspace it\'s from',
      '       • All-projects pseudo-tab has no dot (it spans all workspaces)',
      '',
      '   ▸ SIDEBAR ICONS — Favorites changed from ★ (line) to ⭐ (filled, more recognizable). Setup changed from ⚙ to 🛠 (hammer-and-wrench, reads as "fix things" more clearly than the gear). Projects / Recents / Departments unchanged at your request.',
    ],
  },
  {
    version: '4.19',
    date: '2026-05-15',
    notes: [
      'Two-line TASK COLUMN + Departments dashboard fixes.',
      '',
      'TASK COLUMN — stacked layout with inline meta:',
      '   ▸ Top line: task name (bold) + the inline drift chip ("+3d" / "−2d") when relevant.',
      '   ▸ Bottom line: small muted meta row showing ALLOCATION% in SDC-blue + DURATION (weeks) in slate — separated by a · dot. Example: "50% · 3.2w". Drops to single-line when those values are 0 / missing.',
      '   ▸ Right side: % complete pill (unchanged) still anchored to the right of the row.',
      '   ▸ Net: you now see TASK NAME + ALLOCATION% + DURATION + % COMPLETE all in the Task column without dragging the grid sideways.',
      '   ▸ Skipped for milestones / anchors / backlog rows (their allocation × duration isn\'t meaningful).',
      '',
      'If the stacked layout doesn\'t feel right, alternates we could try next:',
      '   • Inline single-line: "Task Name [50%][3w][75%]" — three tight pills after the name',
      '   • Pill cluster on the right: replace the % complete pill with a 3-pill cluster (D · A · %)',
      '   • Hover reveal: name only, hover or click to expand the meta',
      '',
      'DEPARTMENTS DASHBOARD fixes:',
      '   ▸ Project name no longer truncates. The 90px column was clipping anything longer than "1129_Schnei…" — useless. Layout is now a two-column grid with the project name + task name stacked on the left (wrapping to multiple lines when needed) and the date/drift chip pinned to the right.',
      '   ▸ TEMPLATE projects (SDC_StandardProject_Template, SDC_Sales_Template, …) excluded from Behind schedule / Coming due / Key dates cards. Templates aren\'t real scheduled work so they shouldn\'t surface there.',
      '   ▸ SALES-workspace projects excluded from the Departments dashboard AND from the resource timeline AND from the discipline capacity calculation. Sales schedules are pre-quote work that hasn\'t been staffed yet, so they shouldn\'t inflate "X hrs scheduled" on every discipline. If a Sales project later moves to Active workspace, its tasks flow back into resource management automatically.',
      '',
      'Better SIDEBAR ICONS still TBD — let me know which alternates you want:',
      '   ▸ 📋 Projects → 📅 / 🗂 / 📊 / 📑 / ▦ / 🏗',
      '   ▸ ★ Favorites → ⭐ / ♥',
      '   ▸ 🕒 Recents → ⌚ / ↻',
      '   ▸ 👥 Departments → 🏢 / 👤 / 🗃',
      '   ▸ ⚙ Setup → 🛠 / ⚒ / 🔧',
    ],
  },
  {
    version: '4.18',
    date: '2026-05-15',
    notes: [
      'Tab bar unified across workspaces, color-coded by workspace.',
      '',
      'BEFORE: tabs were filtered to the currently active workspace. Click an Active project → tab bar showed only Active tabs; click a Sales project → tab bar swapped to Sales tabs; previously-open tabs from other workspaces disappeared until you went back to that workspace. Disorienting.',
      '',
      'NOW: ALL open project tabs show simultaneously regardless of workspace. You can have an Active project, a Sales template, and a Closed schedule all open at the same time. To tell which workspace each one belongs to, tabs are TINTED by workspace using SDC palette colors:',
      '   ▸ Active workspace tabs  → SDC LIME tint (#befa4f) — currently active tab fills with solid lime',
      '   ▸ Sales workspace tabs   → SDC LIGHT BLUE tint (#AACEE8) — active fills with solid light blue',
      '   ▸ Closed workspace tabs  → SDC YELLOW tint (#FFDE51) — active fills with solid yellow',
      '   ▸ The "All projects" pseudo-tab stays neutral (no workspace).',
      '',
      'A glance at the tab bar tells you what mix of workspaces you have open. Active tab is the one with full color saturation; the others sit in tinted background. Inactive tabs of the same workspace still share the tint so you can scan groups visually.',
      '',
      'No state change — same openProjects list, same click behavior, just different rendering. activeWorkspace still tracks the workspace of the project you most recently opened (used by the Projects page to remember where you were).',
    ],
  },
  {
    version: '4.17',
    date: '2026-05-15',
    notes: [
      'Quote vs Schedule modal restructured + SDC_Sales_Template auto-lands in Sales workspace.',
      '',
      'TABLE LAYOUT (Quote vs Schedule):',
      '   ▸ Rows GROUPED by SDC section. Three dark section-header rows split the table:',
      '       • Section 10 — Design & Build  (Mech Eng / Controls Design / Drawings / Software / HMI / Robot / Vision / Build / Wire Panel / Wire Machine)',
      '       • Section 40 — Machine Testing / Debug  (Configure / Test-Debug Engineer / Shop Debug)',
      '       • Section 50 — Teardown & Install  (Teardown / Install)',
      '   ▸ Quoted and Scheduled COMBINED into one column with a "/" separator (e.g. "100 / 150"). Saves a column and reads more compactly.',
      '   ▸ VARIANCE column re-added next to it: scheduled − quoted, signed and colored:',
      '       • green when under budget (variance < 0)',
      '       • slate when within budget (variance == 0)',
      '       • amber when within 10% over quote',
      '       • red when more than 10% over quote',
      '       • "—" when no quote attached',
      '   ▸ Remaining column kept on the far right.',
      '   ▸ Final columns: Discipline | Quoted / Scheduled | Variance | Remaining.',
      '',
      'CALCULATION (confirming after your question):',
      '   ▸ Yes, allocation IS factored. Per-task scheduled hours = duration_days × 8 × (allocation % / 100). The Add-Additional-Resource flow inherits the source allocation, so duplicated resources are counted correctly. Default allocation of 90% applies only when a task literally has no allocation value set.',
      '',
      'SALES TEMPLATE WORKSPACE:',
      '   ▸ projectWorkspace() now AUTO-DETECTS the workspace from the project name when no explicit assignment exists in localStorage. Names matching /sales/i default to the Sales workspace; /closed|archive/i defaults to Closed; everything else still defaults to Active. So SDC_Sales_Template — and any future "_sales" / "_closed" project — lands in the right workspace on first load without manual right-click setup.',
      '   ▸ Explicit assignments via right-click → Workspace: ___ still win (they\'re saved in state.projectWorkspaces and override the name-based default).',
      '',
      'Refresh the page — SDC_Sales_Template should now appear under the Sales workspace section on the Projects page. You\'ll still need to right-click → Mark as template ★ to get the lime-pinned template treatment.',
    ],
  },
  {
    version: '4.16',
    date: '2026-05-15',
    notes: [
      'Three fixes / one delivery:',
      '',
      '   ▸ PROJECTS ROW LAYOUT FIXED. Star + project name + favorite ★ all sit on ONE line now. The previous version put the row inside a <button> AND nested another <button> for the favorite — invalid HTML, and Chrome renders nested buttons as block elements, breaking the row onto two lines. Outer row is now a <div role="button" tabindex="0"> so the nested ★ button is valid and stays inline. Same fix applied to Favorites + Recents pages.',
      '',
      '   ▸ QUOTE VS SCHEDULE bucket matcher now handles many more name variants. The previous matcher required exact names like "Controls Design" and "Wire Machine"; projects with shorter or alternate names (e.g. "CE Design", "CE Drawings", "CE Software", "Machine Wiring", "Panel Build") fell through to the generic sub-department fallback or returned null, which is why your Schneider-Electric project showed zeros in those columns. Now matches:',
      '       • Controls vs CE prefix: "Controls Design" / "CE Design" / "CE-Design" → ce_design',
      '       • Wire Machine ↔ Machine Wiring (both word orders) → wire_machine',
      '       • Wire Panel ↔ Panel Build / Panel Wiring (both orders) → wire_panel',
      '       • Build / Builder + design_build phase → build',
      '       • Mech Design / Mechanical Design + design_build → mech_eng',
      '       • Trailing " N" / "-N" / "_N" still stripped first (so Wire Machine 2 / Builder-3 / Test Engineer_4 still bucket correctly).',
      '       Schedules with task names in the SDC_Template convention still match exactly. Projects with custom names should now read accurate scheduled hours.',
      '',
      '   ▸ SDC_SALES_TEMPLATE created. Cloned every task from SDC_StandardProject_Template into a new project named SDC_Sales_Template. 32 tasks, 31 predecessors remapped to the new task IDs. Real-person assignees blanked; placeholders preserved.',
      '       Two manual steps for you to finish: open the SDC_Sales_Template tab (Projects → Active → click the row), then right-click the tab and (1) Mark as template ★, (2) Workspace: Sales. Those flags live in localStorage so they\'re per-browser — can\'t pre-set them from the server.',
      '       Once both are set, the Sales workspace will show SDC_Sales_Template in its Templates section, and clicking "+ New from SDC_Sales_Template" on that workspace will clone it into new sales schedules.',
    ],
  },
  {
    version: '4.15',
    date: '2026-05-15',
    notes: [
      'Big batch — Quote vs Schedule cleanup, Projects-page templates section, and favorites + recents wired up.',
      '',
      'QUOTE VS SCHEDULE MODAL:',
      '   ▸ "Δ (sched − quote)" column REPLACED with "Remaining" — hours still to work, computed as SUM(scheduled_hrs × (1 − task_progress%/100)) per bucket. The columns now read Quoted / Scheduled / Remaining left-to-right.',
      '   ▸ Schedule column accuracy FIX: tasks created via "+ Add additional resource" (e.g. "Wire Machine 2", "Builder 3", "Machine Wiring-2") were falling out of their named buckets because the matching regex required exact names. Now the matcher strips trailing " N", "-N", or "_N" before matching, so duplicates roll back into their original bucket. Also added Build*/Mechanical Design name patterns as fallbacks for the design-build sub-departments. Schneider-Electric and similar projects should now show accurate scheduled hours.',
      '   ▸ Quoted column shows "—" when no quote is attached (instead of a fake 0). Scheduled column still reads accurate hours regardless of whether a quote was ever imported.',
      '   ▸ Schedule-vs-quote color highlight moved to the Scheduled column itself (was on the now-removed Delta column).',
      '',
      'PROJECTS PAGE:',
      '   ▸ TEMPLATES section per workspace — pinned at the top of each workspace\'s expanded body with a heavy SDC-lime left border + green-tinted background + uppercase "TEMPLATES" label. Reads as its own thing, not mixed in with the active schedules below.',
      '   ▸ "+ New schedule" button per workspace is now WORKSPACE-AWARE. When the workspace has exactly ONE template, the button reads "+ New from <template>" and clicking opens a simple name prompt → builds the new project from that template, auto-assigns it to the workspace, opens it as a tab. No template-picker dropdown to fight. Workspaces with 0 or multiple templates fall back to the legacy picker.',
      '',
      'FAVORITES + RECENTS:',
      '   ▸ ★ button on every project row — click to favorite/unfavorite. Persisted in localStorage (sdcFavoriteProjects).',
      '   ▸ Favorites sidebar icon now renders an actual page: list of every favorited schedule, click-to-open, click the ★ again to unfavorite.',
      '   ▸ Recents sidebar icon renders the last 10 opened schedules, newest first. Tracked automatically on every click-to-open from Projects / Favorites / Recents.',
      '   ▸ Both pages share the row layout with Projects, so visual identity is consistent.',
      '',
      'INTERNAL: state.favoriteProjects + state.recentProjects (saved in localStorage as sdcFavoriteProjects / sdcRecentProjects). toggleFavoriteProject() + recordRecentProject() helpers.',
    ],
  },
  {
    version: '4.14',
    date: '2026-05-15',
    notes: [
      'Three fixes:',
      '',
      '   ▸ TEMPLATE TABS can now be closed via the × on the tab — same as any other tab. Closing only removes the tab from the open-tabs list; the underlying project + template flag stay in the DB. Reopen anytime via the Projects page. Previously the × was hidden on templates as a safeguard, which left users stuck unable to clear them off the tab bar.',
      '',
      '   ▸ "ADD ADDITIONAL RESOURCE" — really fixed this time. v4.13\'s server-side bump approach was correct in theory but kept dropping the new row at the bottom on real projects, because the wire section had non-sequential sort_orders (Machine Power-Up sandwiched between wire-section tasks at unpredictable values) and an integer +1 didn\'t always land where we expected.',
      '       New approach: the client picks the MIDPOINT between the source\'s sort_order and the next task in the project (sorted by sort_order globally). The server accepts fractional sort_orders verbatim (no rounding, no downstream bump) — SQLite\'s INTEGER affinity is flexible and stores them as REAL. Sequential adds keep halving the gap (18.5, 18.25, …) — 52 bits of mantissa = effectively infinite headroom for normal use.',
      '       Net result: the new row lands IMMEDIATELY below whatever row you right-clicked, regardless of bucket structure or anchor positions.',
      '',
      '   ▸ NAME INCREMENT now recognizes dash and underscore separators, not just space. "Machine Wiring-1" → "Machine Wiring-2" (was: "Machine Wiring-1 2"). Same for "Test_Engineer_3" → "Test_Engineer_4". Space-separated names still work the same: "Builder 1" → "Builder 2". If there\'s no trailing number, " 2" is appended (e.g. "Builder" → "Builder 2"). Collision check still bumps further if the next-step name already exists.',
    ],
  },
  {
    version: '4.13',
    date: '2026-05-15',
    notes: [
      '"Add additional resource" now actually inserts the new row immediately BELOW the source — not at the bottom of the section.',
      '',
      'Root cause: POST /api/tasks on the server was ignoring the client\'s sort_order field and always assigning MAX(sort_order) + 1 — so every new task piled at the bottom of its bucket, regardless of what the client requested. (v4.12\'s client-side fractional-sort-order math was correct in spirit but never had any effect — the server just dropped it.)',
      '',
      'Two fixes (server + client):',
      '   ▸ Server: POST /api/tasks now reads sort_order from the request body if provided. When the client requests a specific slot, the server first BUMPS every downstream task in that project up by 1 (UPDATE ... WHERE project = ? AND sort_order >= ?), then inserts the new task at the requested integer slot. Atomic within the request. Append-style callers (no sort_order) still default to MAX + 1 like before.',
      '   ▸ Client: addAdditionalResource passes src.sort_order + 1 as an integer (the server handles the shift). Drops you right under the row you clicked, every time.',
    ],
  },
  {
    version: '4.12',
    date: '2026-05-15',
    notes: [
      'Three fixes:',
      '',
      '   ▸ DEPARTMENTS now excludes template-project artifacts. The ME / Build / Wire Placeholder team rows no longer show up in the discipline cards — they\'re template scaffolding, not real people. Discipline capacity hours and the resource-timeline bars also skip tasks whose project is marked as a template (otherwise every job built from a template was double-counted).',
      '',
      '   ▸ Clicking a project tab while you\'re on the Projects (or Departments / Setup / etc.) page now JUMPS BACK to the schedule view of that project — like it always should have. v4.11 introduced this bug because the tab click only set state.filters.project and called render(), but render() respects state.view. Now it also calls setView(\'schedule\').',
      '',
      '   ▸ NEW: "＋ Add additional resource" right-click action on any task row. Increments the trailing number on the name (Builder 1 → Builder 2, Mechanical Design → Mechanical Design 2), keeps the same department / phase / duration / allocation, drops the new row right below the source in the grid, and sets the predecessor to "<source>FF" (finish-to-finish). Assignee left blank — you fill it in. Skips collisions by bumping further (Builder 2 → Builder 3 if Builder 2 already exists). Hidden from the menu on milestones / anchors / backlog rows (duplicating those as resources doesn\'t make sense).',
    ],
  },
  {
    version: '4.11',
    date: '2026-05-15',
    notes: [
      'Major nav restructure — topbar removed, sidebar widened and made the primary navigation, Projects page replaces the side panel.',
      '',
      'Removed:',
      '   ▸ Blue topbar with "Project Scheduler" title and the Schedule / Departments / Setup tabs.',
      '   ▸ Sidebar slide-out PANEL — replaced by full PAGES (much closer to Smartsheet\'s Browse model).',
      '',
      'New sidebar (76px wide, fits labels):',
      '   ▸ 📋 PROJECTS — full page with each workspace as a collapsible section.',
      '   ▸ ★ FAVORITES — placeholder for now.',
      '   ▸ 🕒 RECENTS — placeholder for now.',
      '   ▸ 👥 DEPARTMENTS — moved from the old topbar Team tab; same page underneath.',
      '   ▸ ⚙ SETUP — moved from the old topbar Setup tab; same page underneath.',
      '   ▸ 📌 REV pill at the bottom — was top-right, now bottom-of-sidebar.',
      '',
      'Projects page behavior:',
      '   ▸ Three workspace sections (Active / Sales / Closed). All start COLLAPSED — click the header bar to expand. State persists in localStorage (sdcProjectsExpanded) so the next visit remembers which ones you had open.',
      '   ▸ Each section shows its project count, templates pinned with ★, currently-open projects badged "open".',
      '   ▸ Click a project row → opens as a tab + switches to the schedule view.',
      '   ▸ Right-click a row → existing project context menu (rename / duplicate / move workspace / etc.).',
      '   ▸ Each workspace header has a "+ New schedule" button. CLOSED workspace\'s button is disabled with a "Closed — no new schedules allowed" tooltip.',
      '',
      'Sidebar icon + label naming alternatives (let me know your pick):',
      '   ▸ Current: 📋 "Projects"',
      '   ▸ Alternatives: 📅 Schedules / 🗂 Catalog / 📊 Pipeline / ▦ Browse / 🏗 Builds / 📑 Plans',
      '',
      '"+ New tab" button on the project tab bar still works — clicking it sets the workspace to the current active workspace and opens the legacy picker scoped to that workspace.',
      '',
      'Coming next: actually wire the Favorites + Recents pages (localStorage list + star/unstar action + last-N opened tracking), migrate the estimate-sheet + Smartsheet flows out of the legacy picker into a workspace-aware modal triggered from the "+ New schedule" buttons.',
    ],
  },
  {
    version: '4.10',
    date: '2026-05-15',
    notes: [
      'Left SIDEBAR navigation — moved the workspace tabs off the top of the page into a Smartsheet-style left rail.',
      '',
      'New layout:',
      '   ▸ Narrow (56px) icon rail along the left edge — always visible.',
      '   ▸ Four icons: 📁 Workspaces, ★ Favorites, 🕒 Recents, ＋ Create.',
      '   ▸ Click an icon → a 300px-wide panel swings out to the right of the rail with that icon\'s content. Click again to collapse.',
      '   ▸ Workspaces panel shows every workspace as a section (Active / Sales / Closed) with its projects listed underneath. Templates pinned at top with ★. Click a project row → opens as a tab and switches to the schedule view. Right-click → the regular project context menu (rename, duplicate, move workspace, etc.).',
      '   ▸ Bottom of the panel: "＋ New schedule…" button that jumps to the Create panel.',
      '   ▸ Create panel: three action cards (Build from template / SDC estimate sheet / Smartsheet import). The template card auto-picks the active workspace\'s template when there\'s only one. New schedules auto-assign to the active workspace.',
      '   ▸ Estimate sheet + Smartsheet import cards currently say "Open legacy picker…" — those flows still live in the legacy + New tab picker until they\'re fully migrated. Click and the panel closes + legacy picker opens.',
      '',
      'Removed from v4.9:',
      '   ▸ Top workspace bar (Active / Sales / Closed tabs above the project tabs) — replaced by the sidebar Workspaces panel.',
      '   ▸ Standalone workspace landing page (state.view=\'workspace\') — its contents merged into the sidebar panel, which is always reachable regardless of what view you\'re in.',
      '',
      'Button changes:',
      '   ▸ "+ New tab" now opens the Workspaces panel directly. Right-click still falls back to the legacy picker popup.',
      '',
      'Coming next: per-engineer favorites (★ icon panel — wire the localStorage list + star/unstar action), Recents panel (last 10 opened projects), and migrating the estimate + Smartsheet flows from the legacy picker into the Create panel.',
    ],
  },
  {
    version: '4.9',
    date: '2026-05-15',
    notes: [
      'Workspaces are now FULL PAGES, not just tab filters.',
      '',
      'Click any workspace tab in the workspace bar (Active / Sales / Closed) → you land on a dedicated workspace page that shows:',
      '   ▸ Header: "{Workspace} Schedules" + a count',
      '   ▸ List of every project in that workspace, templates pinned at top with ★, currently-open projects badged "open". Click a row to open it as a tab + switch to the schedule view. Right-click a row to delete (templates protected).',
      '   ▸ Three "build a new schedule" action cards: ★ Build from template / 📊 Build from SDC estimate sheet / 📥 Import from Smartsheet',
      '',
      '"+ New tab" button changed: now navigates to the workspace landing page for the active workspace (instead of popping the old picker). Right-click that button if you want the legacy picker (used as fallback while we finish wiring estimate/Smartsheet on the workspace page).',
      '',
      'Build from template auto-picks the template when the workspace has exactly ONE template — no dropdown to fight, just type the new schedule name and hit Build. With multiple templates in a workspace it falls back to a select. Build also auto-assigns the new project to the current workspace.',
      '',
      'Cross-workspace cleanup: when you open a project that lives in a different workspace, the workspace bar auto-switches to match. Last-viewed project survives reloads — its workspace becomes the initial workspace on next load.',
      '',
      'KNOWN GAPS in this v4.9:',
      '   ▸ The "Build from estimate sheet" + "Import from Smartsheet" cards on the workspace page show an alert pointing you back to the legacy "+ New tab" picker for now — those flows have a lot of plumbing (xlsx parsing, modals, post-create wiring) that still needs to be migrated. Will tackle next.',
    ],
  },
  {
    version: '4.8',
    date: '2026-05-15',
    notes: [
      'Workspace navigation — first cut at a Smartsheet-style page structure.',
      '',
      'New WORKSPACE BAR above the project tabs (top bar, not a sidebar — Gantt views need the horizontal width). Three workspace tabs: Active / Sales / Closed. Click one → the project tab bar below filters to just that workspace\'s schedules. The active workspace gets a yellow underline + light tint so you can see which one you\'re in.',
      '',
      'How tabs behave now:',
      '   ▸ "All projects" pseudo-tab is always visible (special).',
      '   ▸ Templates (★) within each workspace pin to the FRONT of the tab list when you\'re in that workspace. So your "SDC_Template" in Active sits at the head of the Active tabs row; your "SDC_Sales_Template" in Sales sits at the head of the Sales tabs row.',
      '   ▸ Open projects in the current workspace come after the templates.',
      '   ▸ If your currently-viewing project is in a different workspace than the active one, its tab stays visible (never strands you).',
      '   ▸ Workspace persists in localStorage (sdcActiveWorkspace).',
      '',
      '"+ Open project" button → renamed "+ New tab". Same picker, just clearer that clicking opens a new tab in the top bar.',
      '',
      'Opening a project from the picker now AUTO-SWITCHES the active workspace to that project\'s workspace, so the project\'s tab appears in the bar without you having to manually switch.',
      '',
      'Picker reorganized: no more separate "Templates ★" group. Templates appear within their workspace group (Active / Sales / Closed), pinned to the top of that group with a ★ next to the name. Matches the new tab-pinning behavior.',
      '',
      'Top-level navigation tweak: the "Team" tab in the topbar is now labeled "Departments" — the page is more about team / resource organization by department than a flat team list. The functionality is the same; the data view (#view-team) is unchanged.',
      '',
      'Coming next: per-engineer favorites (star specific projects regardless of workspace), and a left-or-right-side panel for resource management / search / homepage (if it doesn\'t crowd the Gantt — TBD on placement). The Smartsheet-style "open in current tab vs new tab" picker is also on the list.',
    ],
  },
  {
    version: '4.7',
    date: '2026-05-15',
    notes: [
      'WORKSPACES — first cut at organizing projects into groups so the picker doesn\'t pile every sales schedule alongside active customer projects.',
      '',
      'Three fixed workspaces for now: Active / Sales / Closed.',
      '',
      'How it works:',
      '   ▸ Every project belongs to exactly one workspace. New projects default to "Active". Projects from before v4.7 are also "Active" (existing assignments are preserved).',
      '   ▸ The "+ Open project" picker now groups projects: Templates ★ first (across all workspaces — they\'re a separate concept), then Active, then Sales, then Closed. Empty groups don\'t render a header.',
      '   ▸ Right-click any project tab → Workspace: Active / Sales / Closed (the current one shows ✓). Clicking a different workspace moves the project. Lives in the same right-click menu as Rename / Duplicate / Quote vs Schedule etc.',
      '   ▸ Renaming and deleting a project carry the workspace assignment along (rename) or clean it up (delete).',
      '',
      'Persisted in localStorage (sdcProjectWorkspaces) per-browser. If your team needs shared workspace assignments across machines, that\'s a future server-side move.',
      '',
      'Use case unlocked: park your standard SDC_Template + SDC_Sales_Template in Templates ★, all active customer projects in Active, every sales-quote schedule in Sales, and shipped/archived projects in Closed. Picker reads top → bottom in importance, so the things you reach for most (templates, active work) are right there.',
      '',
      'Coming next: favorites (per-engineer starring of specific schedules), and possibly the Smartsheet-style left sidebar layout — those would build on top of this workspace foundation. Also: a simpler "sales-style" template you can build from when starting a quote — for now, build it manually from the standard template using the v4.6 picker entry.',
    ],
  },
  {
    version: '4.6',
    date: '2026-05-14',
    notes: [
      'Two project-management additions:',
      '',
      '   ▸ BUILD FROM TEMPLATE — new section in the "+ Open project" picker, sits between "Start a new schedule" and "Build from estimate sheet". Pick any template (any project marked with ★ in the right-click menu), enter a new schedule name, click Build. Clones every task / milestone / predecessor from the template into the new project. Real-person assignees get blanked; placeholders carry through. Same code path as right-click → Duplicate, just exposed as a top-level workflow with a template-only dropdown.',
      '',
      '   ▸ Use case unlocked: you can now mark multiple templates (e.g. "SDC_Template" for full project schedules + "SDC_Sales_Template" for simplified sales schedules) and pick which one to start from when creating a new project. Start the sales template by building it FROM the standard template, then trimming it down.',
      '',
      '   ▸ DRAG-TO-REORDER project tabs. Grab any project tab in the top bar and drag it left/right to reorder. The "All projects" pseudo-tab stays pinned at position 0 (non-draggable). New order persists across reloads via the existing localStorage save.',
    ],
  },
  {
    version: '4.5',
    date: '2026-05-14',
    notes: [
      'New Filters chip: "Not complete". Sits at the top of the Quick filters group, hides anything at 100% progress — including milestones (and yes, including anchor milestones like Receipt of PO once you check them off).',
      '',
      'Explicit "show what\'s left to do" mode. Unlike the other quick filters (which leave milestones always visible), this one DOES drop done milestones from view since a checked-off PO isn\'t remaining work. Works alongside Behind / Assigned / etc. — checking both narrows the view further (e.g., "Not complete" + "Behind schedule" = remaining work that\'s currently behind).',
      '',
      'Cleared by the popover\'s "Clear all" button, same as the other quick chips.',
    ],
  },
  {
    version: '4.4',
    date: '2026-05-14',
    notes: [
      'Three progress-visualization changes based on feedback:',
      '',
      '   ▸ HATCH PATTERN reverted (v4.3 → v4.4). Back to a solid progress fill (the bar\'s text/stroke color overlaid on the bar), but at 0.4 opacity — less aggressive than the original 0.55 in v4.0/4.1, more visible than v4.2\'s 0.28. Filled-in but not bold.',
      '',
      '   ▸ 100% complete DURATION tasks: bar keeps its HIERARCHY color (Mech slate / Controls green / Build tan / Wire yellow / etc.) and gets an SDC-LIME BORDER (2.5px). Preserves "what kind of work" identity, adds an unmistakable "done" cue. Replaces v4.2\'s lime-fill / v4.3\'s hatch.',
      '',
      '   ▸ MILESTONES (anchor + non-anchor) keep their NORMAL color when done — no more deep-green flip. Receipt of PO done and Receipt of PO not-done are visually the same diamond, just one with a ✓ overlay; same for Mech 1 Release, Power-Up, FAT, Ship. Fixes the v3.x bug where "Mechanical Release done" looked completely different from "Receipt of PO not done" even though they\'re the same kind of marker.',
      '',
      'The ✓ overlay is now slate-800 (dark) on lime anchor diamonds (was white — invisible) and stays white on slate non-anchor diamonds.',
    ],
  },
  {
    version: '4.3',
    date: '2026-05-14',
    notes: [
      'Gantt bar progress — reverted v4.2\'s lime-on-done treatment, replaced with a HATCH PATTERN.',
      '',
      'Why: turning the entire bar lime green at 100% broke the hierarchy color → role mapping. A done Build task is still a Build task, and recoloring it to a "done color" hides what kind of work it was. Confusing, especially for the eye scanning down a section.',
      '',
      'New approach: every bar keeps its HIERARCHY color (Mech slate, Controls green, Build tan, Wire yellow, etc.) at all progress levels. Progress is shown via a DIAGONAL HATCH OVERLAY that grows from left → right with the percent — same color underneath, just textured where it\'s been completed.',
      '   • 0%   →  solid hierarchy color, no hatching',
      '   • 50%  →  left half hatched, right half clean — bar color unchanged',
      '   • 100% →  entire bar hatched — still the hierarchy color, just fully textured',
      '',
      'Implementation: an SVG <pattern> with diagonal black lines at 32% opacity is injected into the gantt SVG <defs> after each render. The .bar-progress sub-rect (which frappe-gantt sizes to width × progress%) gets fill: url(#sdc-progress-hatch) via CSS. Hatch sits BEHIND the bar label text, so the task name stays readable.',
      '',
      'Reverted from v4.2:',
      '   • Solid SDC-lime fill on done bars',
      '   • ✓ prefix in done bar labels',
      '   • .is-done class application on duration bars',
      '',
      'Milestone done state (lime diamond with ✓) is unchanged — milestones don\'t have a "hierarchy color" to preserve, so they still flip to lime + checkmark when complete.',
    ],
  },
  {
    version: '4.2',
    date: '2026-05-14',
    notes: [
      'Gantt bars — completed bars now POP visually, in-progress bars look less harsh.',
      '',
      'You were right that v4.1 didn\'t touch the Gantt chart itself — that release only restyled the GRID Task-column pill. The bars on the chart were still using hierarchy colors with a heavy progress fill, so a 100% complete bar just looked like a darker version of an in-progress bar.',
      '',
      'Now:',
      '   • 100% COMPLETE bars get a solid SDC-LIME fill (#befa4f) with a dark-green border and a ✓ prefix in the bar label. Completely different from any in-progress bar — you can spot finished tasks at a glance scrolling through the chart. Matches the milestone-done diamond color scheme so duration tasks and milestones read the same when complete.',
      '   • IN-PROGRESS bars: progress-fill opacity softened from 0.55 → 0.28. The hard light/dark split between filled and unfilled portions used to look muddy, especially on darker hierarchy colors (Build / Mech). Now the progress overlay reads as a subtle indicator instead of dominating the bar.',
      '',
      '   • Critical-path red-stroke + over-allocated red-fill rules unchanged.',
    ],
  },
  {
    version: '4.1',
    date: '2026-05-14',
    notes: [
      '% complete pill restyled as a small PROGRESS BAR.',
      '',
      'Was: a flat, solid-colored pill — slate / red / green / deep-green by status. The deep-green 100% state felt heavy and visually similar enough to the in-progress green that "fully complete" didn\'t pop the way it should.',
      '',
      'Now: every pill is a neutral light container with an inner fill that grows left → right with the percent. You can SEE how done a task is without reading the number.',
      '   • 0%     →  empty outlined pill, "0%" text in slate',
      '   • 1–99%  →  red fill (if behind) or green fill (if on track), grows with the percent. Black text on top stays readable.',
      '   • 100%   →  fully-green bar, dark green border, white ✓ in the middle. Visually unmistakable — matches the milestone-check done state so duration tasks and milestones read the same when complete.',
      '',
      'Same edit-click behavior (click the pill → small inline number input). Same drift / on-track logic. Just a much better progress visualization.',
    ],
  },
  {
    version: '4.0',
    date: '2026-05-14',
    notes: [
      'Major milestone release rolling up everything since v2.2. Tagged for archival before the next round of work.',
      '',
      'Highlights since 2.2:',
      '',
      '   • ESTIMATE → SCHEDULE: import a Panduit T-066-style estimate xlsx and the server generates a full project schedule (sections 10 / 40 / 50, anchors, milestones, predecessors, allocations) from the quoted hours. Saved per-project so future opens auto-load the comparison.',
      '',
      '   • BACKLOG TASK between Receipt of PO and section 10 — accounts for kickoff / ramp-up calendar weeks before engineering starts. Red anchor-style pill, no progress / drift indicators (it\'s a calendar block, not work).',
      '',
      '   • ANCHOR MILESTONES (Receipt of PO / Mech 1 Release / Machine Power-Up / FAT / Ship Machine) redesigned: concentric double-diamond (smaller inner, tight outer ring at the same size as non-anchor milestones), SDC lime fill, name on the right and date on the left of each anchor on the Gantt.',
      '',
      '   • DRIFT / % COMPLETE pills in the Task column, with status colors (slate / red / green / emerald) and a unified done-state across milestones and duration tasks.',
      '',
      '   • QUOTE VS SCHEDULE comparison modal (bucket-by-bucket hours), with a one-click entry point to the FINANCIAL MILESTONES editor from inside it.',
      '',
      '   • VIEW PILL in the toolbar — four icon-toggles: ≡ Flatten + sort, $ Financials, ↔ Arrow lag labels, % Bar meta (allocation/duration above bars).',
      '',
      '   • ARROW LAG LABELS on Gantt predecessors with non-zero lag ("+2 wks" / "−1 wk"); click any pill to edit the lag inline. Avoids arrows / overlaps adjacent labels.',
      '',
      '   • BAR META labels (allocation% · duration weeks) below each bar, placed intelligently to avoid arrows.',
      '',
      '   • UNDO + REDO buttons in the toolbar, 20-deep history, covers cell edits, pill edits, and lag edits.',
      '',
      '   • TOOLBAR REBUILD: Show/hide columns (far left, drag rows to reorder grid columns live), ⌕ Filters, ⚖ Quote vs Schedule, Grid|Both|Gantt, View pill, ◎ Baseline dropdown (turn on / show overlay / reset / turn off, with in-app confirmations), stacked − Weeks/Rows + pill, ↶ Undo, ↷ Redo, 👤 For Customer, ⛶ Zoom to fit.',
      '',
      '   • PROJECT NAME BANNER between the toolbar and the schedule split — SDC-lime outlined pill, centered, works for all pane modes.',
      '',
      '   • 👤 FOR CUSTOMER view toggle — strips chrome / extra columns / editing affordances, forces pane=both, zoom-to-fits the Gantt. Floating exit button to come back. Section headers stay clickable for collapse/expand.',
      '',
      '   • ZOOM TRANSITION fix — switching across Day/Week/Month boundaries keeps the centered date stable instead of jumping off-screen.',
      '',
      '   • In-app CONFIRMATION POPUPS anchored to the click site (no more native browser dialogs far from where the action was triggered).',
      '',
      'See entries 3.0 → 3.66 for the per-iteration history.',
    ],
  },
  {
    version: '3.66',
    date: '2026-05-14',
    notes: [
      'Customer view: section headers visible again, with clearer click affordance.',
      '',
      'Bug: v3.65\'s column-hide rule was `td:not([data-col="name"]):not([data-col="start"]):not([data-col="finish"]) { display: none }`. Section header rows (.group-header) render as a single TD with a colspan and NO data-col attribute — so they matched all three :not selectors and were getting hidden. That removed the user\'s ability to toggle sections open/closed when showing the schedule to a customer.',
      '',
      'Fix: scoped the column-hide rule to `tr:not(.group-header)`, so it only touches data rows. Section headers stay visible and clickable (existing handler — click toggles the group into collapsedGroups).',
      '',
      'Also bumped the section-header click affordance in customer view: bigger caret (▾ / ▸), clearer hover state, explicit cursor:pointer. So when you\'re demoing to a customer and want to collapse Section 40 down to "Machine Testing", a click on the section bar collapses it the same way it would in the editing view.',
    ],
  },
  {
    version: '3.65',
    date: '2026-05-14',
    notes: [
      'Replaced the troublesome "📄 Export PDF" feature with a much simpler "👤 For Customer" view toggle.',
      '',
      'The PDF path kept fighting browser print quirks (panel sizing, viewBox scaling, beforeprint timing, slow preview, bars misaligning with grid rows). The new approach skips all that — clicking "👤 For Customer" just transforms the live on-screen app into a customer-friendly state:',
      '   • Hides: topbar / project tabs / toolbar / row-height handle / "+ Add task" / Gantt legend / project-stats popup.',
      '   • Keeps: project name banner (now reads as a page header).',
      '   • Grid columns shrink to Task / Start / Finish ONLY — everything else hidden via CSS. Task column hugs its text (~140px min), Start/Finish hug their date strings (~64px each).',
      '   • Pane forced to Both (grid + Gantt visible).',
      '   • Editing affordances removed from the Task column (no "0%" pills, no milestone checkboxes, no drift chips — read-only customer view).',
      '   • zoomToFit runs after the layout reflows so the Gantt actually fills the available width — no empty months on either side.',
      '   • A floating "✕ Exit customer view" button (lime-outlined, top-right) is the only visible affordance to leave the view, since the main toolbar is hidden. Click it (or hit the toolbar button after exiting another way) to restore the editing layout — your prior pane mode, zoom, and scroll position are saved and restored exactly.',
      '',
      'How to share with a customer: enter the view, then screenshot it, screen-share it, or hit Ctrl+P → Save as PDF if you want a file. The same hidden-chrome layout applies during print, so a manual Print works the same way an Export PDF would have — without any of the JS gymnastics that were freezing the preview.',
      '',
      'Removed: the entire beforeprint/afterprint SVG viewBox manipulation, the printing-customer-view CSS class (renamed to .customer-view), and the showAlertDialog "PDF export is per-project" wording.',
    ],
  },
  {
    version: '3.64',
    date: '2026-05-14',
    notes: [
      'PDF Export — Gantt now uses the full chart width instead of being squished.',
      '',
      'Root cause: the SVG kept whatever zoom level the user had on screen — usually zoomed-in showing weeks/days. That made the SVG 3000–5000px wide while the actual project bars only spanned a fraction of that range. Scaling the entire wide SVG down to fit the print page produced a chart where the bars looked tiny and far apart, surrounded by an empty sea of date columns past the project end.',
      '',
      'Fix: call zoomToFit() in normal JS context BEFORE applying the print class. That resizes the SVG so its width is exactly the project span (PO → Ship) — no wasted date columns. Then the viewBox-scale-to-page logic in beforeprint just stretches the project-span SVG to the printed Gantt area. Bars now use the full available width.',
      '',
      'zoomToFit was the same trick that caused the print preview to freeze in v3.62 — but only because v3.62 ran it INSIDE the beforeprint hook (synchronous-with-the-dialog, so the renderGantt() it triggered blocked the dialog opening). Running it in normal JS context first, then setTimeout(window.print, 200) to give the repaint room, keeps the dialog snappy.',
      '',
      'After-print also restores your prior zoom + scroll position, so your editing view is untouched once the dialog closes.',
    ],
  },
  {
    version: '3.63',
    date: '2026-05-14',
    notes: [
      'PDF Export — three more fixes after v3.62 feedback:',
      '',
      '   ▸ LANDSCAPE (sorry, misread you in v3.62 as wanting portrait). The Gantt benefits from every inch of horizontal room — landscape gives ~50% more width than portrait at the same paper size, so bars stay readable instead of getting squashed.',
      '',
      '   ▸ GANTT BARS NOW ALIGN WITH GRID ROWS. The viewBox approach in v3.62 used uniform aspect-ratio scaling, which compressed Y just as much as X — bars ended up at half the height of their grid rows, floating above the task names instead of next to them. Now: preserveAspectRatio="none" + width=100% + height=natural — the SVG scales X to fit the page width but keeps Y in natural pixels. Bars line up with their task names exactly as they do on screen.',
      '',
      '   ▸ FASTER print preview. The Gantt SVG has a LOT of overlay groups (arrows, lag pills, bar-meta labels, drift chips, baseline ghosts, today line, financial overlay) — on a large schedule that\'s hundreds of extra elements the browser has to render to the print buffer. None of those help a customer reading the schedule, so they\'re now hidden during print. Customer sees just bars + milestones + project name. Should be noticeably faster on big projects.',
      '',
      '   ▸ Bonus cleanup: hid the "0%" / "✓" / drift chip pills from the Task column in print. Customer doesn\'t need to see editing affordances; they need a clean read-only schedule.',
    ],
  },
  {
    version: '3.62',
    date: '2026-05-14',
    notes: [
      'PDF Export fixed — v3.61 had three problems user reported:',
      '   1) Browser froze on print preview for larger schedules.',
      '   2) Output was landscape; user wanted portrait.',
      '   3) Gantt area printed empty (just date grid lines, no task bars).',
      '',
      'Root causes + fixes:',
      '',
      '   ▸ FREEZE — v3.61 ran zoomToFit() inside the beforeprint hook, which triggers a full renderGantt(). On large schedules that synchronous re-render took multiple seconds with the print preview blocking on it. Fixed by REMOVING the auto-zoom-to-fit and replacing it with a much cheaper SVG viewBox trick.',
      '',
      '   ▸ PORTRAIT — flipped @page { size: portrait } and tightened margins to 0.3in so the chart gets as much horizontal room as possible.',
      '',
      '   ▸ EMPTY GANTT — the SVG had width="2400" (or wider, depending on project span) while the print page was only ~8 inches. The bars rendered at their full pixel positions and sat way off the right edge of the page. Fixed by adding a viewBox to the SVG on beforeprint, then setting width=100% — the chart now scales uniformly to fit the available page width. afterprint restores the original SVG dimensions so your editing view is untouched.',
      '',
      'Also: removed the hard-coded grid width (280px) that was clipping the Start / Finish columns. Now the grid sizes itself to the content with sensible min-widths on the visible columns (Task ≥ 160px, Start/Finish ≥ 68px).',
    ],
  },
  {
    version: '3.61',
    date: '2026-05-14',
    notes: [
      'Two additions:',
      '',
      '   • PROJECT NAME BANNER — moved off the top of the toolbar onto its own row BETWEEN the toolbar and the schedule split. Pill style with SDC-lime outline (same color as anchor milestones), centered, short. Now reads as "what project is this?" no matter which pane mode you\'re in (Grid-only, Both, Gantt-only) — previously the centered name above the toolbar only made sense in Both view.',
      '',
      '   • EXPORT PDF (📄) — new button in the toolbar (next to ↷ Redo). Click to open a customer-friendly print view:',
      '       ▸ Hides: top bar / project tabs / toolbar / row-height handle / "+ Add task" button / Gantt legend / project-stats popup.',
      '       ▸ Keeps: the project name banner (as a title) + grid + Gantt.',
      '       ▸ Grid shows ONLY the Task / Start / Finish columns — every other column (allocation, progress, predecessors, assignee, notes…) is hidden via CSS.',
      '       ▸ Auto-zoom-to-fit on print: a beforeprint hook calls zoomToFit() so the customer sees the entire project span in one shot; afterprint restores your prior zoom so your editing view is untouched.',
      '       ▸ Page setup: landscape orientation, 0.4in margins, colors preserved (print-color-adjust: exact).',
      '   Opens the standard browser print dialog — pick "Save as PDF" as the destination to share the file with the customer. Works in Chrome / Edge / Firefox / Safari.',
      '',
      'Implementation note: the customer view is just a CSS class on <body> (printing-customer-view) plus @media print rules. No separate "customer mode" state, no extra render path. If you want to preview what the customer will see WITHOUT printing, you could right-click the export button and inspect-element the class onto body manually — but the easier flow is just clicking the button and using the print dialog\'s preview.',
    ],
  },
  {
    version: '3.60',
    date: '2026-05-14',
    notes: [
      'Two fixes from v3.59 feedback:',
      '   • ⚖ Quote vs Schedule MOVED off the centered project-name row and INTO the toolbar, sitting right next to the ⌕ Filters button. Project-scoped actions belong with the other filters/controls, not floating next to the project label. Disabled when no specific project is selected; tooltip explains why.',
      '   • $ FINANCIAL MILESTONES button is now prominent at the TOP of the Quote vs Schedule modal — in the same banner row as the "Replace estimate…" picker, styled in SDC primary so it actually reads as an action. v3.59 had it tucked in the modal footer as a ghost-style button which made it easy to miss (user feedback: "there is no option for the financial milestones currently").',
      '',
      'No behavior change: button click still opens the existing per-project financials editor ON TOP of the comparison modal so you can confirm billing while the hours-vs-quote view stays in sight.',
    ],
  },
  {
    version: '3.59',
    date: '2026-05-14',
    notes: [
      'Three additions:',
      '   • REDO button in the toolbar (↷ Redo), right after ↶ Undo. Click to re-apply the most recently undone change. The redo stack is wiped when you make a NEW edit (otherwise redo would try to re-apply a "future" state that conflicts with what you just did). Capped at the same 20-entry depth as undo. Tooltips on both buttons show what would happen if you clicked.',
      '   • ⚖ QUOTE VS SCHEDULE button is back, on the centered project-name row (immediately to the right of the project name). Opens the existing comparison modal. Was previously only reachable via project tab right-click → "Quote vs Schedule…" — that\'s still there, but the visible button is the primary entry point again.',
      '   • EDIT FINANCIAL MILESTONES is now a one-click action FROM the Quote vs Schedule modal — new "✎ Edit Financial Milestones…" button at the bottom of the modal. Opens the existing financials editor on top (the quote-vs-schedule modal stays open behind it so you can confirm a billing event while looking at the hours comparison). Was previously only reachable via project tab right-click; that path still works.',
    ],
  },
  {
    version: '3.58',
    date: '2026-05-14',
    notes: [
      'View pill: Flatten and Sort-by-start-date merged into ONE linked toggle.',
      'They were two separate icons (≡ and ↕) but they\'re almost always used together — you flatten to get a chronological run of section 10 / 40 / 50, which means you also want start-date sort within each section. Flattening without the sort gives you a jumbled mess; sorting without flattening keeps you stuck in the hierarchy view.',
      'Now: a single "≡" icon toggles BOTH flags on, or BOTH off. The icon highlights only when both are on. The underlying state.scheduleView.flatten and .sortByStart flags still persist independently — settings saved from earlier versions are honored on load, the next click just brings them in sync.',
      'View pill icon order is now:  ≡  $  ↔  %  (was: ≡  ↕  $  ↔  %).',
    ],
  },
  {
    version: '3.57',
    date: '2026-05-14',
    notes: [
      'Fix v3.56 over-suppression of bar-meta labels — most labels were getting hidden because the collision check used SVG getBBox() on each arrow path, which for L-shaped arrows returns the rectangle enclosing BOTH legs plus the L\'s empty corner area. Bars sitting inside that corner zone (most of them, for any multi-row L-shape arrow) showed as "colliding" even though the actual arrow line was nowhere near them.',
      'Now: each arrow line is parsed into its axis-aligned segments (1 or 2 per L-shape), and the label rect is intersection-tested against the SEGMENTS only. Since all our arrow segments are pure horizontal or vertical by construction, the segment\'s bbox equals the segment itself — so segment-vs-rect intersection is exact, not conservative.',
      'Net result: labels show up on most bars again (instead of ~1/3 in v3.56), and the few that legitimately collide with an arrow line still flip above or skip cleanly.',
    ],
  },
  {
    version: '3.56',
    date: '2026-05-14',
    notes: [
      'Bar-meta labels (% / weeks) now AVOID arrows. v3.55 always placed the combined label below the bar, but an arrow exiting the bar\'s bottom edge (e.g. Build → Wire Panel directly below) ran right through the label position and made it unreadable.',
      'New placement logic, in priority order:',
      '   1. BELOW the bar — preferred (each row\'s below-gap is dedicated, so adjacent rows don\'t collide).',
      '   2. ABOVE the bar — fallback when an arrow would cross the below position.',
      '   3. Skip — only when both below AND above are blocked by arrows. Rare.',
      'Detection: we snapshot every drawn arrow\'s SVG bounding box and AABB-test each candidate label position against them. drawBarMeta() runs after drawCustomArrows() so the arrow <path> elements are already in the DOM.',
    ],
  },
  {
    version: '3.55',
    date: '2026-05-14',
    notes: [
      'Bar-meta labels (v3.54 % toggle) reworked to fix overlap on adjacent rows.',
      'v3.54 put allocation ABOVE each bar and duration BELOW. That looked fine for isolated rows, but when two task bars sat right next to each other with the same x-range (e.g. Test/Debug Engineer 1 + 2, Teardown + Install) the bottom label of the top task landed in the same gap as the top label of the bottom task — same x, overlapping y — and you couldn\'t read either one.',
      'Now both values render as ONE COMBINED label BELOW each bar:  "50% · 3w".',
      '   • Each row\'s label sits fully inside that row\'s below-bar gap.',
      '   • No cross-row collision is possible (the next row\'s bar starts where the gap ends; their label is in THEIR gap).',
      '   • Either side of the dot is omitted when that value is 0 or missing — a 100%-allocated milestone-ish task with no listed duration shows just "100%", and so on.',
    ],
  },
  {
    version: '3.54',
    date: '2026-05-14',
    notes: [
      'NEW View pill icon: "%" — toggles per-bar allocation + duration labels on the Gantt. When on:',
      '   • ABOVE each bar: allocation % (e.g. "50%") in slate text.',
      '   • BELOW each bar: duration in weeks (e.g. "3w") in lighter slate text.',
      'Lets you see staffing at a glance without scrolling the grid to read the Allocation / Duration columns. Especially useful when the bar itself is too short for the task name + numbers to fit inside.',
      '',
      'Skipped for milestones, anchors, and Backlog (none of those have meaningful "allocation × duration" staffing).',
      'Labels paint in a group above the bars (same layering as the lag pills from v3.53) so they\'re never hidden by tasks. Defaults to OFF — opt-in, since it does add some chart density. Persisted per browser via the existing scheduleView storage.',
      '',
      'View pill icon order now: ≡  ↕  $  ↔  %',
    ],
  },
  {
    version: '3.53',
    date: '2026-05-14',
    notes: [
      'Arrow lag labels (v3.52) refined per feedback:',
      '   • TOGGLEABLE — new 4th icon in the View pill: "↔". Click to hide/show all the ±N wks pills on Gantt arrows. State persists per browser. Defaults to ON so the feature is discoverable; flip it OFF if you don\'t want the chart cluttered.',
      '   • Labels now paint ABOVE bars instead of behind them. Previously they sat in the same SVG group as the arrow lines (which sit BEHIND bars on purpose so routes can duck under task rows), so labels got covered by tasks (feedback: "−2 weeks from designer view is underneath mechanical design"). Now they\'re in a separate group appended at the end of the SVG → always on top.',
      '   • OVERLAP RESOLUTION — when two labels would land at the same spot (e.g., Robot programming + Vision arrows whose midpoints coincide, or HMI programming\'s "−1 wk" stacking on Build Buyer\'s "+2 wks"), the renderer now shifts the second label vertically by one pill-height and tries again, expanding outward (one slot up, then one slot down) until the spot is clear. Up to ~12 attempts before giving up. Labels stay close to their arrow but don\'t pile on top of each other.',
      '',
      'No functional change to the click-to-edit behavior — every lag pill is still clickable to tweak the lag in weeks, push goes through undo.',
    ],
  },
  {
    version: '3.52',
    date: '2026-05-14',
    notes: [
      'Four wins in this release:',
      '   • BACKLOG line number fixed. The Backlog row now gets a stable line number in the grid (right after Receipt of PO), so Mechanical Design 1\'s predecessor displays as e.g. "2FS" instead of "?N". buildCanonicalTaskOrder was walking the HIERARCHY groups for line assignments but didn\'t include Backlog (its phase_group is null) — now it\'s inserted between PO and section 10 to match where the grid renders it.',
      '   • LAG LABELS on Gantt arrows. When a predecessor carries a non-zero lag (e.g., "5FS −2w" = start 2 weeks before pred finishes), a small "+N wks" / "−N wks" pill renders on the arrow itself, anchored to the longer leg of the route. Only shows when |lag| ≥ 1 week — the dense schedule isn\'t cluttered with "0d" labels. So now "we release standard components 7 weeks before build panel starts" reads at a glance directly on the chart.',
      '   • CLICK a lag pill to edit it inline. Opens a tiny popup next to the click: number input (in weeks), Save / Cancel. Negative values shift the successor earlier; positive shifts it later; 0 removes the lag entirely (predecessor collapses to its base form, e.g. "5FS"). No need to dive into the predecessor cell on the grid.',
      '   • UNDO BUTTON in the toolbar (↶ Undo, sits just before Zoom-to-fit). Tracks the last 20 task edits in this session — cell edits in the grid, pill edits in the Task column, lag edits on arrows. Click to revert the most recent edit. Tooltip shows what would be undone ("Undo: Edit duration on \\"Build Panel\\"") and how many edits are in history. Disabled when nothing\'s been changed yet. Stack clears on page reload (no DB persistence).',
    ],
  },
  {
    version: '3.51',
    date: '2026-05-14',
    notes: [
      'Five fixes / refinements on top of v3.50:',
      '   • ZOOM no longer jumps off-screen at the Week→Month / Day→Week boundaries. v3.50 scaled scroll position linearly between zoom levels, but when getZoomConfig flips view modes the gantt\'s start-date and column-width formula change discontinuously — so the pixel that meant "Aug 10" in Week mode mapped to "Nov 3" in Month mode. Now we capture the DATE under the viewport center BEFORE the rebuild and scroll the new chart so that same date stays centered. Stable through every mode flip.',
      '   • MILESTONES (anchors AND smaller non-anchor markers like Mech 2 Release, BOM Review, Order Long Lead) are now ALWAYS visible regardless of which quick filter is on. Previously only anchors were exempt; non-anchor milestones got hidden by Behind / Ahead / Assigned / Over-allocated. Schedule spine stays put across every filter.',
      '   • NEW filter chip: "Critical path only". Hides every task that isn\'t on the critical-path chain (plus any milestone), so you can isolate the spine of the project. Auto-enables the "Critical path" highlight chip too (the only-filter relies on the chain being computed).',
      '   • FAT stats row now shows ±WEEKS variance instead of ±days / "On baseline". 1–2 days drift rounds to 0 wks, 3–7 to 1 wk, 8–12 to 2 wks, etc. Matches how the rest of the app talks about time (estimate sheets, durations are all weekly).',
      '   • Project-stats popup is NARROWER (min-width 210 → 170px) and now has a 2px SDC LIME outline — same color as the anchor milestones so the popup reads as "important project-level info" instead of just a generic card.',
    ],
  },
  {
    version: '3.50',
    date: '2026-05-14',
    notes: [
      'Toolbar rebuilt to the approved layout. Project name now centered on its own row above the toolbar; toolbar reads left → right:',
      '   1. SHOW/HIDE COLUMNS (far left, renamed from "⋮ Columns"). Click to open a dropdown listing every column in current grid order. Each row has a drag-handle (⋮⋮) — drag rows up/down inside the dropdown and the grid columns reorder LIVE to match. The "Task" column stays locked at position 0 (it\'s the row label).',
      '   2. (large flexible gap)',
      '   3. ⌕ Filters',
      '   4. Grid | Both | Gantt pane picker',
      '   5. VIEW pill — small pill with "View" label on top and three icon-toggles below: ≡ (Flatten sub-sections), ↕ (Sort by start date), $ (Financial milestones overlay). Each icon highlights when its option is on. Moved out of both the Filters popover AND the Gantt stats popup so view options have one home.',
      '   6. ◎ Baseline ▾ dropdown — with two bug fixes:',
      '       • "Turn off baseline" actually fires now. Previous code wired click handlers by index walking just .dropdown-item elements, but the items array also included separators, so indices were off-by-one and the wrong handler (or no handler) ran on click. Now the menu renders and wires handlers in lock-step so item indices always match.',
      '       • Confirmation dialogs ("Set baseline? / Reset? / Turn off?") now open as in-app popups anchored RIGHT AT the click location, instead of the centered native browser dialog ("Internet Explorer popup"). Enter to confirm, Esc to cancel, click outside to cancel.',
      '   7. STACKED WEEKS/ROWS pill — one pill, two rows: top row is the chart zoom (− Weeks/Days/Months +), bottom row is the row-height (− rows +). Halves the toolbar width vs the two side-by-side pills it replaces.',
      '   8. ⛶ Zoom to fit (far right).',
      '',
      'Project-stats popup (top-right of the Gantt) is back to just Duration / Complete / FAT date with the variance chip. View options moved to the toolbar where they\'re visible all the time.',
    ],
  },
  {
    version: '3.49',
    date: '2026-05-14',
    notes: [
      'Major toolbar / popover overhaul based on feedback that the layout was "messy":',
      '   • FILTERS popover stays OPEN when you click a chip (instead of closing after every toggle). Lets you stack multiple filters in one sitting. Closes when you click outside, same as before.',
      '   • CRITICAL PATH joined the Quick filters chips (Behind / Ahead / Milestones / Assigned / Over-allocated / Critical path). It\'s a "show me this subset" toggle — fits naturally there.',
      '   • VIEW OPTIONS moved out of the Filters popover and INTO the floating project-stats popup on the top-right of the Gantt. Toggles for Flatten / Sort by date / $ Financial milestones now sit right where you read project Duration / Complete / FAT — one place for "what you\'re looking at + how to look at it".',
      '   • FAT row now shows the actual FAT DATE (M/D/YY) instead of just "On baseline" / "+3d late". The variance chip is still there as a small pill to the right of the date when a baseline is set — so you get the date + the drift signal in one row.',
      '   • BASELINE collapsed from a 2-button segmented pair to a single dropdown menu (◎ Baseline ▾). Inside: "Turn on baseline" when none is set, or "Show overlay (on/off) / Reset baseline / Turn off baseline" when one exists. The "Turn off" entry is finally first-class — previously it was buried under right-click → Clear baseline, which most people never found.',
      '   • ROW-HEIGHT pill (− rows +) and ZOOM pill (− Days/Weeks/Months +) now look IDENTICAL — same height, same border, same font weight. They were visually mismatched before (rows pill smaller and lighter, zoom pill taller and bolder).',
      '',
      'Still on the list (called out in v3.48, mentioned again):',
      '   • Drag-to-reorder columns inside the Columns dropdown.',
      '   • Customer view mode for sharing schedules externally.',
    ],
  },
  {
    version: '3.48',
    date: '2026-05-13',
    notes: [
      'Toolbar reorganized to fit on a single row + duplicate header buttons removed:',
      '   • COLUMNS dropdown moved to the FAR LEFT of the toolbar so it\'s the first control you see (it\'s the most frequently-used "how the grid looks" toggle).',
      '   • Flatten / By date / Critical / Only / $ Financial overlay all MOVED OUT of the toolbar and INTO the Filters popover (top-left of the project header). New "View options" section in the popover with checkmark-style toggles. Same state, same shortcuts — just in one place where the row toggles logically belong.',
      '   • $ Financials button (modal opener) removed from the project header info area — it was a duplicate of the toolbar overlay toggle. Still reachable via project tab right-click → "Financial milestones…".',
      '   • ⚖ Quote vs Schedule button removed from the project header info area for the same reason. Moved into the project tab right-click menu as "⚖ Quote vs Schedule…".',
      'Result: the project header is now just the project name on the left + the streamlined toolbar (Columns, Pane picker, Row height, Baseline, Zoom) on the right. Fits one line at typical window widths.',
      '',
      'Coming next (not in this release):',
      '   • Drag-to-reorder columns in the Columns dropdown, with the grid order following — currently you can show/hide but not rearrange.',
      '   • Customer view mode that strips out internal-only UI (baseline, financials, quote-vs-schedule, etc.) for sending a clean schedule to a customer.',
    ],
  },
  {
    version: '3.47',
    date: '2026-05-13',
    notes: [
      'Baseline overlay toggle is now obvious. The right button of the Baseline segmented pair used to just say "Baseline" with a subtle is-active highlight when on — easy to miss that it was a toggle. Now the label flips with state:',
      '   • No baseline yet → "Show baseline" (click prompts to set one)',
      '   • Baseline set, overlay off → "Show baseline"',
      '   • Baseline set, overlay on → "Hide baseline"',
      'Tooltip also rewritten to make explicit: hiding KEEPS the baseline snapshot saved — only Right-click → Clear baseline removes it. So you can hide the overlay while making changes, then show it again later to see drift.',
      'No behavior change in the click handler — this is purely a labeling/discoverability fix.',
    ],
  },
  {
    version: '3.46',
    date: '2026-05-13',
    notes: [
      'Two corrections from v3.45 feedback:',
      '   • Task name no longer turns green when a duration task hits 100%. v3.45 re-added the .name-done class which painted the name emerald-bold, but that conflicted with how milestones behave (their names stay normal-black when complete). Now duration tasks and milestones BOTH leave the name black when done — the green ✓ pill on the right is the sole "done" signal across both row types.',
      '   • % complete pill INPUT now actually stays the size of the pill when you click to edit. v3.45 added !important to the input\'s width/margin/padding rules, but the underlying problem was the inline-block PILL shrink-to-fitting to the input\'s default ~144px intrinsic width (size=20 chars × ~7px). Fixed by locking the pill itself to width:38px / max-width:38px / overflow:hidden during editing, so the inline-block can no longer expand to fit a wider child.',
    ],
  },
  {
    version: '3.45',
    date: '2026-05-13',
    notes: [
      'Five visual fixes on the Task column and milestones:',
      '   • Anchor diamond geometry: inner shrunk, outer ring SAME SIZE as non-anchor milestones. Anchor outer = h × 1.20 (matches non-anchor diamond exactly). Inner = h × 0.75 (much smaller, leaves a clear ring of negative space). Net: anchors and non-anchors have the SAME total footprint — distinguished by the ring (anchor only) and color (lime vs slate), not by size.',
      '   • 100% complete TASK NAME goes green-bold again (.name-done class restored). Pairs with the green ✓ pill on the right so the "this task is done" signal is unmistakable both reading the name AND scanning the pill column. (No inline ✓ prefix before the name — the right-side pill already does that.)',
      '   • Drift chip ("+3d" / "-2d") is now SKIPPED on 100% complete tasks. Once a task is done, ahead/behind no longer applies — it\'s just done. Drift is only for in-progress work.',
      '   • Done pill colors HARDCODED to match the milestone done state (bg #16a34a, border #14532d, white text). Previously the pill used the --pct-done-fill var which could be a lighter custom color from saved Setup settings; that made the task-done pill look different from a milestone-done pill. Now they always match.',
      '   • Pill EDIT INPUT no longer blows up to half the column when you click to edit. The shared .cell-edit-input style (designed for full-cell editing) applied negative margins that leaked into the tiny pill. Overridden with !important so the input stays exactly the pill\'s 38×16 footprint.',
    ],
  },
  {
    version: '3.44',
    date: '2026-05-13',
    notes: [
      'Anchor milestone diamonds (Receipt of PO / Mech 1 Release / Machine Power-Up / FAT / Ship Machine) shrunk down significantly. They were way too big — the outer ring overlapped adjacent task bars and dominated the chart.',
      '   • Inner diamond: h × 1.45 → h × 1.20 (same size as non-anchor milestones now).',
      '   • Outer ring: 1.75× the inner → 1.30× (tight halo instead of a wide concentric ring).',
      '   • Net effect: anchor diamonds are now ~38% smaller in total radius than before. The lime-green fill + thin ring still reads as a distinct "spine marker" vs the slate non-anchor diamonds, but they no longer dwarf the task rows around them.',
      '   • All three places that use the formula updated in lockstep: drawMilestoneDiamonds (the actual diamond rendering), drawMilestoneLabels (so name + date labels sit right next to the smaller ring), drawCustomArrows (so predecessor arrows terminate at the new outer edge).',
    ],
  },
  {
    version: '3.43',
    date: '2026-05-13',
    notes: [
      'Unified "done" treatment for duration tasks and milestones. Previously a duration task at 100% showed a LIGHT green pill with the text "100%" plus a green ✓ prefix in front of the task name AND green-bold name styling — three separate signals saying the same thing. Milestones, by contrast, just flipped their checkbox to a deep-green pill with a white ✓.',
      'Now both look the same when complete: a deep-green ✓ pill (#16a34a + white) on the right side of the row. No more green name text, no more inline ✓ prefix. The pill is still clickable to edit % back down to whatever value if work needs to reopen.',
      'Also applied to the Team page resource-timeline bars (.res-bar.status-done) so the "done" color is consistent across the app.',
      'Note: if you previously saved a custom "% complete done" color in Setup, your saved value still applies. Reset that field in Setup to pick up the new default deep-green.',
    ],
  },
  {
    version: '3.42',
    date: '2026-05-13',
    notes: [
      'Task column polish — make the row widgets feel quieter and more aligned:',
      '   • Milestone CHECKBOX now sized to match the % complete pill (38×16). Previously the milestone box was a tight 16×16 square, so milestone rows had a tiny widget far to the right while duration rows had a wider pill — the visual rhythm was off. Now both occupy the same slot, so the ✓ sits centered exactly where the % would be.',
      '   • % complete pill (especially the default "0%" state) is now subtler. Background lightened from slate-100 (#f1f5f9) → near-white (#fafbfc), and the border darkened less aggressively (80/20 mix instead of 70/30). The pill is still clearly a pill, just doesn\'t shout for attention on every row.',
      '   • Same treatment applied to the resource-timeline bars on the Team page so the color scheme stays consistent (Team bars share the same --pct-*-fill CSS vars).',
    ],
  },
  {
    version: '3.41',
    date: '2026-05-13',
    notes: [
      'Follow-up to v3.40 — two corrections after reviewing the Panduit project at a compressed row height:',
      '   • Anchor milestone DIAMONDS reverted to their original size. v3.40 inadvertently scaled them up (60% bar-height ratio meant proportionally larger diamonds via their h × 1.45 × 1.75 outer radius formula), causing the FAT / Ship outer rings to overlap with the Shop Debug task row above. Bar-height ratio is back to 50% and BAR_H_MIN back to 10px, so diamonds match v3.39 sizing.',
      '   • Bar TEXT now actually shrinks at compressed row heights. v3.40 set font-size via the SVG presentation attribute, which loses to frappe-gantt\'s CDN CSS rule (`.gantt .bar-label { font-size: 12px }`) on specificity — so the text stayed at 12px no matter the bar height, and descenders (g, y, p, S in "Test/Debug Engineer") kept hanging below the bar. Fixed by writing to `style.fontSize` (inline style) instead, which wins. Font scales from 8px (barH=10) up to 12px (barH≥14).',
    ],
  },
  {
    version: '3.40',
    date: '2026-05-13',
    notes: [
      'Polish pass on the schedule view — five visual cleanups requested while reviewing the Panduit project:',
      '   • Backlog now reads as an ANCHOR-style row (outlined red chip with bold red name + bold red date) instead of a regular task row. No more "-3d" drift chip on it either — backlog is by-design, not "behind schedule".',
      '   • Receipt of PO + Backlog are now visually GROUPED between the same pair of black borders (no border between them) so they read as a unit — "what has to happen before engineering starts".',
      '   • Predecessor ARROWS now terminate at the OUTER edge of anchor milestone diamonds instead of piercing through the outer ring. Looks intentional, not glitchy.',
      '   • Anchor NAME labels (right of diamond) and DATE labels (left of diamond) now clear the outer ring — they used to overlap the ring outline at short row heights.',
      '   • Bar TEXT now SHRINKS automatically when the row height is small. The descenders (g, y, p, S) used to hang below short bars (e.g. "Test/Debug Engineer" at row height 14). Font sizes interpolate from 8px → 12px as row height grows. (Superseded in 3.41 — see fix note above.)',
    ],
  },
  {
    version: '3.39',
    date: '2026-05-12',
    notes: [
      'Spine anchor DATES now painted on the Gantt opposite their name labels. Receipt of PO / Mech 1 Release / Machine Power-Up / FAT / Ship Machine show their date (M/D/YY) on the LEFT of the diamond while the name stays on the RIGHT. Useful in Both view when the grid is narrow and you can\'t see the date column without scrolling.',
    ],
  },
  {
    version: '3.38',
    date: '2026-05-12',
    notes: [
      'Backlog migration: any existing project that already has a Backlog task inside section 10 (created before the phase_group=null fix) now gets it moved above section 10 automatically on server startup. Safe to re-run; only matches tasks still in the old shape. Verified on the 25-2271-02 Panduit project — Backlog now sits between Receipt of PO and section 10.',
    ],
  },
  {
    version: '3.37',
    date: '2026-05-12',
    notes: [
      'Backlog refined — no longer a regular task with progress / drift indicators:',
      '   • Phase_group is NULL so it renders ABOVE section 10 (right under Receipt of PO) instead of inside it.',
      '   • No % complete pill (it\'s a calendar block, not work).',
      '   • No drift / behind-schedule chip (it\'s by design — not "behind").',
      '   • Grid row: subtle gray tint + italic text so it visually reads as a spine marker, not work.',
      '   • Gantt bar: gray fill with darker stroke so it stands out from the colorful work bars.',
      '',
      'ATTACH ESTIMATE to existing schedule — persistent, not just one-shot:',
      '   • Click "⚖ Quote vs Schedule" → if no estimate is attached, click "Attach estimate…" to pick the xlsx.',
      '   • Parses, populates the Quoted column, AND saves the quote to the project (via new POST /api/project/:project/quote endpoint) so future opens load it automatically.',
      '   • Projects that already have an attached estimate get a "Replace estimate…" button if you ever need to swap it out.',
      '   • Works for any project — Smartsheet imports, manually-created schedules, projects that pre-date the auto-persist behavior.',
    ],
  },
  {
    version: '3.36',
    date: '2026-05-12',
    notes: [
      'NEW: BACKLOG task between Receipt of PO and the start of engineering. Accounts for the kickoff / paperwork / ramp-up window before the team can actually pick the project up.',
      '   • Default 2 weeks (10 business days). Editable per project from the estimate modal — new "Backlog (weeks)" input next to Delivery (weeks).',
      '   • Rendered as a 0% allocation task in section 10 (right under Receipt of PO, above Mechanical Design). No real work, just calendar time so the rest of the schedule reflects the actual project start.',
      '   • Mech Design (and ME 2 when present) now depend on Backlog instead of starting at PO, so the entire engineering chain shifts right by the backlog weeks.',
      '   • Set Backlog to 0 to bypass entirely (older "start at PO" behavior).',
      '',
      'Also add the rule to the Setup → Estimate → Schedule Rules card.',
    ],
  },
  {
    version: '3.35',
    date: '2026-05-12',
    notes: [
      'Testing-phase allocations no longer over-shoot the quote when the schedule stretches:',
      '   • Test/Debug Engineer 1 (lead controls debugger) STAYS at 90% allocation — full time the whole testing window, per user direction.',
      '   • Test/Debug Engineer 2 (secondary, typically mech support) now DROPS to 25% / 50% / 90% based on the REMAINING quoted hours after T/D 1\'s full-time coverage. Snaps to the closest discrete tier.',
      '   • Shop Debug also snaps to 25% / 50% / 90% based on quoted shop hours over the testing duration.',
      'For the Panduit T-066 estimate at 36w delivery, T/D 2 lands at 25% (116 hrs vs quoted 228) and Shop Debug at 50% (232 hrs vs 180). Was previously over by +379 and +238 — now ±25%.',
      'Both allocations recompute automatically when testing extends to absorb schedule slack — so the over-allocation stays bounded at any delivery window.',
    ],
  },
  {
    version: '3.34',
    date: '2026-05-12',
    notes: [
      'Quote vs Schedule — added a "Load estimate file…" button for retroactive comparisons. If a project was created BEFORE the quote-persisting logic existed (or via Smartsheet import / manually), the modal now shows an amber banner with a file picker. Pick the original estimate xlsx → re-parses → reopens the comparison populated. Quote isn\'t saved to the project, just used for this view.',
      'Useful for the 25-2271-02 Panduit project and any other schedule that pre-dates the quote-save feature.',
    ],
  },
  {
    version: '3.33',
    date: '2026-05-12',
    notes: [
      'NEW: Quote vs Schedule comparison.',
      '   • Click the new "⚖ Quote vs Schedule" button in the project header (next to $ Financials) to pop up a side-by-side comparison of the estimate-sheet quoted hours and what\'s actually scheduled.',
      '   • Quoted column = the estimate hours per task bucket (Mech Eng / Controls Design / Controls Drawings / Controls Software / HMI / Robot / Vision / Build / Wire Panel / Wire Machine / Configure / Test/Debug / Shop Debug / Teardown / Install) saved when the project was created from + Open project → Analyze.',
      '   • Scheduled column = live calculation walking the project\'s tasks: SUM(duration_days × 8 × allocation%) per bucket, classified by task name + phase_group.',
      '   • Δ column shows the gap, green-pilled when within 10% of quote, amber when off, slate when no estimate is available.',
      '   • Totals row at the bottom rolls up grand-total quoted vs scheduled.',
      '   • Projects NOT created from an estimate sheet (manual, Smartsheet imports, duplicates) show 0 in the Quoted column with a banner explaining — Scheduled side is still accurate.',
      '',
      'Server-side: /api/estimate/create now persists the parsed estimate to settings (key `project_quote:<name>`) so the modal can fetch it later via GET /api/project/:project/quote.',
    ],
  },
  {
    version: '3.32',
    date: '2026-05-12',
    notes: [
      'Estimate hours table tightened further:',
      '   • DEVICE column was clipping — bumped from 48 → 52px so the header text fits.',
      '   • Cell padding 3×4 → 2×2 px (group headers 8×6 → 4×2, sub-headers 6×4 → 3×2) so the words sit tight to the cell edges instead of being padded out.',
      '   • Removed `white-space: nowrap` on header rows — they\'ll wrap if a narrower-than-expected column ever shows up, rather than getting clipped.',
    ],
  },
  {
    version: '3.31',
    date: '2026-05-12',
    notes: [
      'Staffing table weeks now round UP to the nearest half-week. No more 9.7w / 3.9w / 5.3w / 5.7w noise — those become 10w / 4w / 5.5w / 6w. Matches the half-week grid the duration generator uses everywhere else.',
    ],
  },
  {
    version: '3.30',
    date: '2026-05-12',
    notes: [
      'Estimate hours table tightened up:',
      '   • All group headers wrap to TWO lines via explicit `<br>` — "Mech / Eng", "Controls / Eng", "General / Eng", "Mech / Build", "Elec / Build". Single-column groups no longer need their column padded out to fit a long header.',
      '   • Column widths tightened: Section 170 · Mech / Mech Build 54 · Design+Drawings / Elec Build 92 · Software / Database 58 · HMI / Robot / Vision / Device 48 each. Cell padding 3×4 px (down from 4×6).',
      '   • Every cell centered (was right-aligned by default, body centered, footer right-aligned) — Department Totals row now lines up with the numbers above it instead of being shifted to the right.',
      '   • Hash-split cells (Section 10\'s Design + Drawings and Elec Build) shrunk inputs to 30px so they fit cleanly inside the 92px columns.',
    ],
  },
  {
    version: '3.29',
    date: '2026-05-12',
    notes: [
      'Fixed truncated columns in the Estimate hours table. Switched to `table-layout: fixed` with explicit per-column widths sized to actual content:',
      '   Section 200px · Mech Eng / Mech Build 64px · Design+Drawings / Elec Build 120px (room for the hash-split inputs) · Software / Database 70px · HMI / Robot / Vision / Device 56px',
      'Group headers shortened to "Mech Eng / Controls Eng / General Eng / Mech Build / Elec Build" so they fit their column width without "MECHANICA..." ellipses. Sub-headers and section labels can wrap if needed so nothing truncates.',
    ],
  },
  {
    version: '3.28',
    date: '2026-05-12',
    notes: [
      'Fixed: Setup page couldn\'t scroll vertically once it grew past the viewport (added "Estimate → Schedule Rules" card pushed Hierarchy Colors + Phases + Financial Milestones + Project Milestones below the fold). Body has overflow:hidden so without an explicit overflow-y on #view-setup the page just truncated. Now scrolls smoothly to the bottom of the cards.',
    ],
  },
  {
    version: '3.27',
    date: '2026-05-12',
    notes: [
      'Fixed the broken column alignment in the Estimate hours table — the "General" sub-header for Mech Eng was pushing every cell one column to the left, throwing Device into the wrong group. Each major column now has its own sub-header ("General" for Mech Eng / Mech Build / Elec Build; Design+Drawings / Software / Database for Controls; HMI / Robot / Vision / Device for General).',
      'Group tints swapped to use the HIERARCHY palette from Setup — same blue / slate / sky / lime tones the rest of the app uses (Mech Eng #D4E8F7, Controls #C5CDD8, General Eng #AACEE8, Mech Build #D5F0A0, Elec Build #BEFA4F).',
      'Modal max-width bumped 860 → 1080px so the table fits without truncating column headers like "MECH…" or "ELECTR…".',
      'Inputs centered + transparent background — they only show their border on hover/focus so the table reads as a clean spreadsheet rather than a form.',
    ],
  },
  {
    version: '3.26',
    date: '2026-05-12',
    notes: [
      'Estimate hours table redesigned to mirror the SUMMARY FOR RELEASE tab layout — same column groups, same color tints (green = Mech Eng, blue = Controls, teal = General, peach = Mech Build, yellow = Elec Build). No more breaking every column out with stray zeros.',
      'Splits only where they need to be:',
      '   • Section 10 → "Design + Drawings" cell now contains TWO inputs separated by a small "/" hash, defaulting to a 50/50 split of the ce_design hours.',
      '   • Section 10 → "Electrical Build" cell now contains TWO inputs (Panel | Machine) with a hash divider, defaulting to 25/75.',
      '   • Sections 40 and 50 keep Design + Drawings and Electrical Build as single combined cells (no split needed for testing/install).',
      'Click any input to override. Totals row updates live.',
    ],
  },
  {
    version: '3.25',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet modal — hours table fully editable, broken out per task:',
      '   • Section row × discipline column matrix, with the discipline columns split into sub-cells matching individual template tasks:',
      '       Mech | Controls (Design / Drawings / Software) | General (HMI / Robot / Vision) | Build | Wire (Panel / Machine)',
      '   • Every cell is a number input — click and override before generating. Footer totals update live.',
      '   • Default splits: Controls Design ↔ Drawings is 50/50 of the ce_design estimate column. Wire Panel ↔ Machine is 25/75 of the elec_build column. Edit either side to taste.',
      '   • Backend now consumes a `hours_breakdown` payload so user-edited per-task hours flow into the right template task (Controls Design vs Drawings, Wire Panel vs Machine each get THEIR cell values, not an averaged split).',
      '   • Staffing table collapsed to a single row-per-role list: Role | Hours | People | Weeks. Locked rows (Sec 40 testing = 2 fixed, Sec 50 teardown/install = auto from hours) show their value without +/− buttons.',
    ],
  },
  {
    version: '3.24',
    date: '2026-05-12',
    notes: [
      'Duration column now displays half-week durations correctly. Generated tasks live on a half-week grid (3, 5, 8, 10, 13, 15, 18, 20, 23, 25, 28, ... days) but the old display formula `days÷5` showed them as 0.6w / 1.6w / 2.6w / 5.6w / 11.6w. Now detects the half-week bucket and labels: 3 → 0.5w, 8 → 1.5w, 13 → 2.5w, 18 → 3.5w, 28 → 5.5w, 58 → 11.5w, etc. Whole-week durations stay clean (5 → 1w, 10 → 2w). Arbitrary user-entered values that don\'t match a half-week bucket fall back to the original decimal display.',
    ],
  },
  {
    version: '3.23',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet duration granularity = half weeks (was whole weeks). Task durations round UP to the nearest half-week bucket:',
      '   0.5w → 3 days · 1w → 5 · 1.5w → 8 · 2w → 10 · 2.5w → 13 · 3w → 15 · 3.5w → 18 · 4w → 20 · ...',
      'For the Panduit estimate at 1-person-each, 36-week delivery, the schedule now shows:',
      '   • Mechanical Design 10w · Controls Design 2.5w · Controls Drawings 2.5w · Controls Software 5.5w',
      '   • HMI 4w · Robot 2w · Vision 1.5w',
      '   • Builder 1 5.5w · Build and Wire Panel 1.5w · Wire Machine 4w',
      '   • Configure Machine 0.5w ← matches the "half a week for small machine" rule',
      '   • Test/Debug 1+2 + Shop Debug 11.5w (extended to absorb slack)',
      'No more 2.2w / 5.4w / fractional weirdness — every task lands on a half-week.',
      'Also fixes the testing-extension step to add slack in half-week increments (3, 5, 8, 10 days) so the extended duration stays on the same grid.',
    ],
  },
  {
    version: '3.22',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet schedule generation — fundamental rule change:',
      '   • MECH ENG IS NEVER STRETCHED. Main mech engineer is ALWAYS at 90% allocation, full time. Goal is to get the machine to Machine Power-Up + testing ASAP — not pace the engineer at 50%. Previously stretched mech eng to absorb schedule slack; that\'s wrong.',
      '   • SLACK NOW ABSORBS INTO TESTING. If the minimum critical path is shorter than delivery, the Test/Debug Engineer 1 + 2 tasks (and Shop Debug running in parallel) extend in whole-week steps until T/D 1 ends 1 week after the quoted FAT. Testing always takes longer than estimated, so the extra time goes there.',
      '   • ALL DURATIONS IN WHOLE WEEKS. 50d / 10w / 20w / 30d only — never 2.5 weeks or 5.4 weeks. Always rounded UP from minimum hours to give the engineer a buffer.',
      '   • Per-task allocations are now 90% across the board (no more 50% / 25% stretching tiers, since we don\'t stretch any more).',
      '',
      'For the Panduit T-066 estimate at 36-week delivery, 1 person each:',
      '   • Mechanical Design 10w @ 90% (no longer 18w @ 50%)',
      '   • M1R at end of mech eng (week 10)',
      '   • Controls Software 6w, HMI 4w, Robot 2w, Vision 2w',
      '   • Test/Debug 1+2 EXTENDED from 5w → 11w to fill the available window',
      '   • Computed FAT = quoted FAT ± 2 days. Message: "Schedule fits the delivery window."',
      '',
      'Variance message also rewritten to compare COMPUTED FAT vs QUOTED FAT (not pre-FAT task end), since testing-by-design extends past FAT by 1 week.',
    ],
  },
  {
    version: '3.21',
    date: '2026-05-12',
    notes: [
      'MILESTONE CHECKBOX in the Task column. Non-anchor milestones (Mech 2 Release, BOM Review, Receive Mech 1, Order Long Lead, etc.) now show a click-to-toggle checkbox on the right edge instead of nothing. Empty box = not done; filled green checkmark = done (progress = 100). One click flips it. Same place where regular tasks show their % complete pill — consistent layout regardless of row type. Anchors keep their existing green chip treatment.',
      '',
      'CONTROLS + GENERAL Eng broken out into per-task buckets. Each template task now gets the estimate hours for ITS specific column, not an averaged lump:',
      '   • Controls Software → ce_software hours only',
      '   • Controls Design + Drawings → ce_design + ce_database hours',
      '   • HMI Programming → gen_hmi (Sec 10 + Sec 40 debug rolled together)',
      '   • Robot Programming → gen_robot (same)',
      '   • Vision Programming → gen_vision (same)',
      '   Previously the whole Controls bucket averaged 350 hrs across 3 tasks (117 each); now Controls Software gets its 195 hrs and Controls Design + Drawings each get their share of 155 hrs. Same for general — HMI gets 130 hrs (40 design + 90 debug), Robot gets 60, Vision gets 40.',
      '',
      'NEW Setup card: "Estimate → Schedule Rules". Lists the 15 rules the auto-generator currently uses (default headcount, allocation tiers, Mech 1 Release timing, wire 1:3 split, Section 40 staffing, Configure Machine = 5%, FAT = T/D 1 FF -1w, etc.). Read-only for now — tell me which ones you want to tune from this card.',
    ],
  },
  {
    version: '3.20',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet schedule generation — three more user-driven fixes:',
      '   • FAT predecessor is now "Test/Debug Engineer 1 FF -1w" — FAT lands 1 week before main test/debug ends, instead of being pinned to the quoted date and letting Test/Debug extend past it. Was previously clearing FAT\'s predecessor; the cascade now drives FAT from the testing chain (run FAT, then 1 week of cleanup with the engineer still on-site).',
      '   • Wire bucket splits 1:3 between Build and Wire Panel (25% of wire hours) and Wire Machine (75%). Panel build is shorter; machine wiring is the bulk. Auto-detected by task name. Eventually this should read the actual "standard SDC panel package" line from the ESTIMATE SHEET tab when a value is present — for now uses the 1:3 default per user direction.',
      '   • Delete confirmation popups REMOVED. Right-clicking a task row or project tab and picking "Delete" is already two intentional clicks — no third "are you sure" prompt. Applies to both single-task delete and full-project delete.',
    ],
  },
  {
    version: '3.19',
    date: '2026-05-12',
    notes: [
      '+ Open project picker now shows ALL projects, not just closed ones. Already-open projects display with a small green "open" badge and slightly dimmed name — clicking them just switches to their tab. Previously the list filtered out anything in the open-tabs row, which made it look like a project had disappeared after creation.',
      'Also: cleaned up two orphan test projects from my own scripting work — TEST_Estimate_Panduit and the duplicate "25-2271-02 - Panduit - T-066 Assembly Machine" stub. Going forward my test scripts auto-clean after themselves.',
    ],
  },
  {
    version: '3.18',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet schedule generation — staffing + allocation refinements:',
      '   • Template duplicate tasks COLLAPSED when headcount = 1. ME 2 is dropped when mech_eng = 1 (Mechanical Design absorbs all 360 hrs). Builder 2 is dropped when build = 1 (Builder 1 takes all 190 hrs). One person, full time — not two at half time.',
      '   • Allocations snap to DISCRETE tiers: 25%, 50%, or 90%. No more 87%, 81%, 46% noise. Mech Eng picks the lowest tier whose duration still fits the available window (err on more time per user direction). Everything else snaps to the nearest of the three.',
      '   • Default headcount = 1 EVERYWHERE (was build=2). Auto-bump only kicks in when the minimum critical path overruns the delivery window.',
      '   • Critical-path math now adds a 3-week TEMPLATE_OVERHEAD_WEEKS allowance for the predecessor lags baked into SDC_Template (2-week gap between Build & Wire Panel and Wire Machine, plus handoff buffers between phases). The previous math underestimated real cascade spans and would say "fits" on projects that actually overrun once built — auto-bump now triggers correctly.',
      '   • Task durations rounded UP (Math.ceil) instead of nearest — small buffer on every task per user direction.',
    ],
  },
  {
    version: '3.17',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet schedule generation — rebuilt around the real SDC rules:',
      '   • Default headcount = 1 EVERYWHERE (was 2 builders). "Use as few resources as possible" — auto-bump only when the minimum critical path overruns the delivery window. The picker\'s suggested-staffing table flags the bottleneck.',
      '   • Mech 1 Release pinned to END of mechanical engineering (FF the latest mech-eng task with no lag, overriding the template\'s -2W default). Latest M1R possible = most time before procurement.',
      '   • Mech Engineering STRETCHES to absorb slack. If the natural critical path is shorter than the delivery window (e.g. 28w of work for a 36w quote), Mech Design + ME 2 each get the full stretched duration in parallel. Allocation drops below 90% so total hours still match the estimate. Result: M1R lands as late as possible and the rest of the schedule (Build/Wire/Test) packs right up against FAT — no idle gap.',
      '   • Configure Machine duration = 5% of section 40 engineering debug hours (~half a week small / one week large). Test/Debug 1 + 2 share the remaining 95% running in parallel. Per Panduit estimate: Configure = 3d at 81%, Test/Debug = 26d at 89% each.',
      '   • Shop Debug duration = engineering debug duration (run in parallel). Headcount auto-scales: shop_hours ÷ debug_weeks ÷ 40 ÷ efficiency.',
      '   • Verified end-to-end on the Panduit T-066 estimate: 36-week delivery → "Schedule fits the delivery window." Test/Debug ends 1 day before FAT. M1R at end of 18-week mech eng phase. All buckets total within ±5% of the quoted hours.',
    ],
  },
  {
    version: '3.16',
    date: '2026-05-12',
    notes: [
      'In-app confirm + alert dialogs replace the browser-native window.confirm and window.alert popups. Same SDC look-and-feel as the rest of the app — centered in the viewport, dimmed backdrop, rounded card with header / body / footer, red "Delete" button on destructive actions.',
      'Migrated the most user-visible flows:',
      '   • Delete project (tab right-click + closed-project right-click in picker)',
      '   • Delete task (row delete button + anchor-protected warning)',
      '   • Delete financial milestone',
      '   • Estimate-sheet parse + create errors and success message',
      '   • Template-protected delete warning',
      'Keyboard: Enter confirms, Escape cancels. Click the backdrop to cancel.',
    ],
  },
  {
    version: '3.15',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet → schedule create — major math fixes:',
      '   • Task DURATIONS now computed from estimate hours, not the template\'s arbitrary defaults. Each task gets its share of the bucket\'s quoted hours: duration_days = (bucket_hours ÷ task_count) ÷ (8 hr/day × efficiency). On the Panduit estimate: Mech Design + ME 2 = 25 days each (360 hrs total ÷ 2 tasks at 90% = 25d each). Builders 1+2 = 13 days each (190 hrs ÷ 2 = 95 hrs each).',
      '   • ALLOCATION pinned to efficiency on every work task. If you enter 90% efficiency, every task lands with 90% allocation. Was previously taking the template\'s default which read as 100%.',
      '   • Section 40 testing bucket = mech_eng + controls_eng ONLY (lead + secondary debugger). General/HMI/Robot/Vision section-40 hours fold into the Section 10 general bucket (HMI/Robot/Vision programming tasks). Shop debug gets its own bucket from section-40 mech_build + elec_build. Per Panduit: testing = 390 hrs across 3 tasks (Configure + Test/Debug 1 + Test/Debug 2) = 18 days each, not the previous 23-day-each over-estimate.',
      '   • PO + FAT anchors now have predecessors CLEARED on import so cascadeSchedule() doesn\'t override the pinned dates. Was previously rounding FAT forward 2 weeks because cascade was overriding the anchor with predecessor math. Now PO → FAT span = exactly delivery_weeks.',
      '   • Result message now reports schedule slack vs delivery — "86 days of slack — last pre-FAT task ends 86 days before FAT" — so you can see whether the headcount you picked compresses the schedule (and could be reduced) or jams it (needs more people / longer delivery).',
    ],
  },
  {
    version: '3.14',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet modal — simplified per feedback. Dropped the SVG critical-path visual (was confusing) and tightened the staffing rules to match SDC reality:',
      '   • Hours table by section stays.',
      '   • Suggested staffing table now shows ROLES instead of disciplines: Mech Eng / Controls Eng (design) / General Eng / Build / Wire, plus a fixed "Sec 40 Controls debug" row (always 2 people — lead + secondary debugger, both CE Placeholder tasks in the schedule), plus separate "Sec 50 Teardown" and "Sec 50 Install" rows.',
      '   • Section 50 teardown + install each FIXED at 1 week — more hours just means more people, not more days. Headcount auto-computed from estimate hours.',
      '   • Wire ends ~1 week after Build (FF + 1w small jobs). Built into the build-window math.',
      '   • Per-row "Fit" chip now shows the actual weeks math (e.g. "10.0w by M1R" or "Build 3.5w") so you see whether each role has time, not just OK/Tight.',
      '   • Summary line below the table reads out the computed Mech 1 Release week + Build/Wire and Testing windows.',
    ],
  },
  {
    version: '3.13',
    date: '2026-05-12',
    notes: [
      'Estimate-sheet modal — major rebuild based on feedback:',
      '   • Hours table now shows section 10 / 40 / 50 separately (was collapsing them to a single row). Five discipline columns: Mech Eng / Controls Eng / General Eng / Build / Wire — matches the SUMMARY FOR RELEASE layout directly.',
      '   • Quoted FAT replaced with "Delivery (weeks)" — FAT date is now computed and shown as a read-only readout. Matches the way SDC quotes lead time.',
      '   • New "Suggested critical path" panel renders an inline horizontal timeline: Engineering bar from PO to Mech 1 Release, Build/Wire bar in the middle, Testing bar leading into FAT, Install bar after FAT. PO and FAT week markers visible. Mech 1 Release is auto-positioned exactly 6 weeks before Build start (procurement lag).',
      '   • Headcount auto-suggested starting from 1/1/1/2/1 (per user — typical = two builders, one electrician) and bumped per-discipline until the schedule fits the delivery window. Manual +/− buttons override the suggestion.',
      '   • Per-discipline weeks shown for Section 10 and Section 40 separately. Status pill flags any discipline that doesn\'t fit its window.',
      '   • Section 50 (Install) computed as a post-FAT add-on and rendered in the timeline as a separate amber bar.',
    ],
  },
  {
    version: '3.12',
    date: '2026-05-12',
    notes: [
      'NEW — build a schedule directly from an SDC estimate sheet:',
      '   • + Open project picker → "Or build a schedule from an SDC estimate sheet" → drop in the estimate xlsx → click Analyze.',
      '   • Server parses the "SUMMARY FOR RELEASE" tab for hours per discipline and the "ESTIMATE SHEET" tab for quote # / customer / machine title. Suggests a project name like "25-2271-02 - Panduit - T-066 Assembly Machine".',
      '   • Feasibility modal opens with the parsed hours. Pick PO date + quoted FAT date + efficiency (defaults to 90%), and the table shows per-discipline hours, headcount (+/- to adjust), available capacity in hours, and load %.',
      '   • Load chip color-codes the answer: green ≤80%, amber 81–100%, red >100% — instant read on whether the quoted lead time is realistic.',
      '   • Click "Create project" and the server clones SDC_Template into a new project, scales the PO→FAT span to the quoted dates, and resets progress to 0. From there the existing schedule grid + Mech 1 Release adjustment finish the job.',
      '   • Pre-PO template tasks (Machine Concepting etc.) keep their original offset; everything from PO onward gets scaled to the quoted span. Anchors land exactly on the requested dates.',
    ],
  },
  {
    version: '3.11',
    date: '2026-05-12',
    notes: [
      'Team page resource bars now color-coded by schedule status — same scheme as the % complete pill:',
      '   • Slate    = task not started (0%)',
      '   • Red      = in progress AND behind schedule',
      '   • Green    = in progress AND on track / ahead',
      '   • Emerald  = task done (100%)',
      '   Glance at the Team page and you can see which tasks are hot across the whole company without opening any schedule. Bar colors are driven by the same --pct-*-fill CSS variables as the grid pills, so customizing them on the Setup page re-tints every team bar in real time.',
      '   Project color is dropped from the bar (was inferred from project name); the project name is still in the bar label text, so you can still tell which schedule a task belongs to.',
    ],
  },
  {
    version: '3.10',
    date: '2026-05-12',
    notes: [
      '% Complete Pill Colors card simplified — one color picker per state instead of two:',
      '   • Text is now always black, no text-color picker. Just pick a background fill for each of the four states (Not Started / Behind / On Track / Done).',
      '   • Default Done color softened from emerald #16A34A to light emerald #86EFAC so black text reads cleanly on it.',
      '   • Border auto-derived from the fill via color-mix (70% fill + 30% black) so it darkens to match without a separate picker.',
      '   • Backward-compat: any existing saved settings using the old {fill, text} shape get normalized to just the fill value on load.',
    ],
  },
  {
    version: '3.9',
    date: '2026-05-12',
    notes: [
      'Smartsheet import — PREDECESSOR coverage massively improved:',
      '   • Phase-header references now resolve correctly. Smartsheet templates point predecessors at rollup phase rows ("27FF" → finish of ME Development Phase) which we don\'t import as tasks. The remapper now redirects these to the FIRST task in the phase (for FS/SS) or the LAST task (for FF/SF) so the dependency wiring lands somewhere sensible.',
      '   • Before: 7 of 31 tasks had predecessors. After: 21 of 31 tasks have predecessors. The remaining 10 either had no preds in Smartsheet or pointed at flagged-task reminder rows / blank rows that can\'t be recovered.',
      '',
      '% COMPLETE colors now customizable from Setup:',
      '   • New "% Complete Pill Colors" card on the Setup page. Four states (Not Started / Behind Schedule / On Track / Done), each with a fill + text color picker. Live preview repaints the grid as you change them — no save needed for the preview, click Save to persist.',
      '   • All four states default to the existing SDC palette (slate / red / green / emerald) so behavior is unchanged unless you change something.',
    ],
  },
  {
    version: '3.8',
    date: '2026-05-12',
    notes: [
      'Project header decluttered + responsive toolbar:',
      '   • REMOVED the "N tasks", "53% complete", and "FAT +17d vs baseline" chips from the project header. All three are already shown in the Gantt\'s floating project-stats popup (top-right of the chart), so they\'re redundant. Frees up the entire left half of the header for the project name + $ Financials button.',
      '   • Filters button MOVED from the top-level brand bar into the project header. Filters apply to a specific project, so they should sit next to the project they apply to — not above the tab strip. Restyled to match the light header background.',
      '   • Toolbar now wraps gracefully on narrow screens. Both `.schedule-project-header` and `.schedule-toolbar` use flex-wrap so individual buttons drop to a second row instead of getting clipped off the right edge. Resize the window to any width and every control stays reachable.',
    ],
  },
  {
    version: '3.7',
    date: '2026-05-12',
    notes: [
      'Task column refinements (continuing from 3.6):',
      '   • Allocation pill REMOVED from the Task cell — Allocation is back as its own column. It\'s set-once-and-forget data and doesn\'t belong next to the dynamic % complete chip. Migration brings it back to your visible columns automatically.',
      '   • Only the % COMPLETE pill lives in the Task cell now, pinned to the right edge with absolute positioning at the same x for every row, so they read as a vertical column when scanning down the grid (collimated).',
      '   • % complete pill color now reflects SCHEDULE STATUS, not just raw progress:',
      '       slate  = 0%, not started',
      '       red    = 1–99% AND behind schedule (progress < what elapsed time suggests it should be)',
      '       green  = 1–99% AND on track or ahead',
      '       emerald = 100% complete',
      '       Example: a task that\'s 90% done but should be 95% by today\'s elapsed-time math reads as red. Same task at 95% reads as green.',
      '   • Fixed row-clipping caused by the inline-block + max-width wrapper from 3.6. Name text now flows naturally inside the td with text-overflow ellipsis; pill floats on top via absolute positioning so it never affects row height.',
    ],
  },
  {
    version: '3.6',
    date: '2026-05-12',
    notes: [
      'Task column is now a self-contained "row card" — name + drift on the left, allocation + % complete pills pinned to the RIGHT:',
      '   • Allocation pill (slate) and % complete pill (slate when 0, lime when in progress, emerald when 100%) live inside the Task cell on the right edge. Click either pill to edit inline — same number input, save on blur/Enter. Saves you from scrolling sideways to find the dedicated columns.',
      '   • Standalone Allocation and % Complete columns are HIDDEN by default now. One-time migration drops them from your existing layout; re-enable from ⋮ Columns if you prefer the spread-out view.',
      '   • Task column bumped from 280 → 340px default to make room for the pills on the right edge without crowding the name + drift chip on the left.',
      '   • Anchors and 0-duration milestones skip the pills — their state is already conveyed by the chip color (lime = pending, emerald = done) and the diamond ✓ on the Gantt, so no need to duplicate.',
    ],
  },
  {
    version: '3.5',
    date: '2026-05-12',
    notes: [
      'Grid view — denser, more glanceable task column:',
      '   • Drift chip dropped the "behind" / "ahead" word. Red "-4d" / green "+3d" — the color already tells you the direction, no need to spell it out.',
      '   • % COMPLETE chip now lives INLINE in the task name cell. Small slate pill shows "65%" right after the drift chip. Only renders for 1–99% (0% would be noise; 100% is already covered by the green-name-with-✓ treatment). No more scrolling sideways to read the % complete column.',
      '   • ⋮ Columns button moved to the LEFT of the toolbar, right next to the Grid/Both/Gantt pane picker. Grid lives on the left, so its visibility control should too. Row-height ± moved with it as a "how the grid looks" cluster.',
      '   • Fixed: drag-panning the grid was leaving a blue text-selection highlight on the header row when the drag hit the leftmost scroll edge. Now the panel locks selection while panning and clears any pre-threshold selection on the first frame of the drag.',
    ],
  },
  {
    version: '3.4',
    date: '2026-05-12',
    notes: [
      'GRID-LEVEL VISUAL STATUS — every task row now shows where it stands without needing to look at the Gantt:',
      '   • Bigger milestone diamonds. Anchor diamonds bumped from 1.05× → 1.45× row height; non-anchor milestones from 0.85× → 1.20×. The ✓ glyph inside completed diamonds is now legible at the chart\'s default zoom.',
      '   • Completed REGULAR tasks (% complete = 100, with duration) get the same treatment as completed anchors — name turns emerald + bold, ✓ prefix appears to the left. Consistent visual language across all row types: green-with-check = done, default = in progress / not started.',
      '   • Inline schedule-drift chip in the task NAME cell (not its own column). "+3d ahead" pill in soft green, "-5d behind" pill in soft red, right next to the task name. Only shows on duration tasks (anchors and 0-duration milestones skip it because drift math is duration-vs-progress). Same numbers the Gantt\'s bar-edge pills show — just visible without scanning over to the chart.',
    ],
  },
  {
    version: '3.3',
    date: '2026-05-12',
    notes: [
      'MILESTONE COMPLETION — clear "done" state for every 0-duration row:',
      '   • Grid: completed anchor chips (Receipt of PO, Mech 1 Release, Machine Power-Up, FAT, Ship Machine) flip from the default lime green to a solid emerald with a bold ✓ prefix. The lime chip = "ahead of us / scheduled"; the emerald-with-check = "we hit this".',
      '   • Gantt: the diamond markers paint solid emerald with a white ✓ glyph centered inside when progress = 100%. Concentric outer ring on anchors also switches to dark green so the whole double-diamond reads as done, not just the inner shape.',
      '   • Same treatment for non-anchor milestones like Mech 2 Release / BOM Review / Receipt Major Commercials so all 0-duration rows read consistently — green check = hit, default = pending.',
      '   • Driven by the task\'s progress field. To mark a milestone complete, set its % complete to 100 in the grid (same as any other task) — the chip and the diamond update on the next render.',
    ],
  },
  {
    version: '3.2',
    date: '2026-05-12',
    notes: [
      'Smartsheet import — TWO fixes that were hiding the spine anchors and made it look like imports were broken:',
      '   • dedupAnchors was deduplicating across ALL projects. It picked the lowest-ID task per anchor_key globally, so when SDC_Template owns Receipt of PO id=1, every imported project lost its own Receipt of PO (and FAT, Ship Machine, etc.) because their IDs were higher. Now scoped per-project — each project keeps its own anchors regardless of what other projects have. This is why the 1129 Schneider re-import showed 0 anchor diamonds.',
      '   • Same root cause for the "Set baseline" / FAT variance pill reading "no baseline" — those depend on the project actually having a FAT anchor row, which the global dedup was eating.',
      '',
      'BULK IMPORT: the + Open → Import from Excel picker now accepts multiple .xlsx files at once. Drop in a whole folder of Smartsheet exports and they each become a project named after the filename — sequential upload, one alert at the end with success / failure summary and any auto-added team members. Single-file flow still works exactly the same.',
    ],
  },
  {
    version: '3.1.1',
    date: '2026-05-12',
    notes: [
      'Smartsheet import — date + % complete fixes:',
      '   • % COMPLETE now imports correctly. Smartsheet writes "95%" as a STRING with the percent sign baked in; the parser was calling Number("95%") which returns NaN, and every task fell back to 0. Now strips the "%" first, so ME 1 comes in at 95%, Mech 1 Release at 100%, Builder 1 at 50%, etc.',
      '   • DATES no longer get rewritten by cascade scheduling on import. Smartsheet often has manually-tweaked dates that don\'t match the declared predecessor math — e.g. Machine Power-Up\'s predecessor field points at Panel Building, but the actual scheduled date follows Machine Wiring-1. The cascade was overriding those manual fixes and pulling Machine Power-Up from 5/28 back to 4/20. Import now trusts whatever dates the Smartsheet has and skips cascade — the spreadsheet is the source of truth.',
      '   • Verified the 5 spine anchors (Receipt of PO, Mech 1 Release, Machine Power-Up, FAT, Ship Machine) all import with the correct anchor_key, so the Gantt picks them up and renders them at their canonical spine positions.',
    ],
  },
  {
    version: '3.1',
    date: '2026-05-12',
    notes: [
      'Smartsheet import polish (the + Open → Import from Excel flow):',
      '   • Teardown / Install / Wrap-up tasks now land in the right buckets. Smartsheet\'s template puts "Machine Teardown", "Install at Buyer\'s", "SAT", "Send Final Documentation Package", and "Conduct Project Wrap Meeting" AFTER the "Testing At SDC Phase" header with no header of their own, so the previous importer dumped them all into machine_testing/engineering. Added a name-based hint map (SMARTSHEET_TASK_HINTS) that catches these rows by name and routes them to teardown_install/teardown or teardown_install/install regardless of the inherited phase context.',
      '   • Confirmed assignees now flow through correctly — Adam Haviland on ME 1, Mark Ruane on ME 2, Jason Perry on the CE tasks, Jesse Brown / Dewayne Cantrell / Tim Shaffer on Build, Andre Shirk on Wire, Hailey Rizor on CE testing, etc. Names that aren\'t already on the team list get auto-added under the discipline of their parent phase header.',
    ],
  },
  {
    version: '3.0.4',
    date: '2026-05-12',
    notes: [
      'Fixed: team view\'s bottom dashboard cards (Behind schedule / Coming due / Key dates) were cut off at the screen edge because main wasn\'t a flex column container — overflow:hidden alone gave the view nothing to size against. Body and main are now both flex columns with the active view claiming flex:1, so the team view\'s internal scrollbar actually has a defined region to scroll.',
      'Schedule view\'s hardcoded calc(100vh - 90px) height also replaced with flex:1 so changes to topbar/tabs heights reflow automatically.',
    ],
  },
  {
    version: '3.0.3',
    date: '2026-05-12',
    notes: [
      'Resources timeline (the per-person workload section below the team cards) now follows the same row ordering as the cards above: placeholder first, then lead, then everyone else by sort_order.',
    ],
  },
  {
    version: '3.0.2',
    date: '2026-05-12',
    notes: [
      'Team card order flipped: PLACEHOLDER sits at the TOP of each card right below the capacity strip, separated from the rest of the team by a dashed divider. The lead is now the FIRST real member (star + bold), then everyone else in their dragged sort order.',
    ],
  },
  {
    version: '3.0.1',
    date: '2026-05-12',
    notes: [
      'Fixes for the team page from 3.0:',
      '   • Team-member row used a grid layout that squeezed names into a 12px column when the lead-star slot was empty — names were rendering as single characters. Reverted to flex layout so the name input takes whatever space is available.',
      '   • Team page no longer inherits the 1000px max-width from setup-pane — the 5 discipline cards now fill the full window width.',
      '   • Removed the "X tasks · Yd" meta line under each Resources-timeline name — it was getting cut off at tight row heights. Peak overload pill stays, now inline with the name.',
    ],
  },
  {
    version: '3.0',
    date: '2026-05-12',
    notes: [
      'Team page overhaul:',
      '   • SPECIALTY column added next to each member\'s name. Free-text — type whatever fits ("Hardware", "Vision", "Panel design", "Conveyors", "Robotics" …). Saves on blur, shows as italic placeholder when empty.',
      '   • Headcount badge in the top-right of each card removed — was redundant with "X people" in the capacity strip below.',
      '   • Rows tightened way down — grid layout instead of flex, 1–2px padding instead of 4–8px. Way more people fit per card.',
      '   • PLACEHOLDERS pinned to the bottom of every card right above the "+ Add member" button. Buttons line up at the same Y across all cards regardless of headcount, so the dashboard rows below are easier to reach.',
      '   • LEAD pinned to the top of each card with a star + bold name.',
      '   • DRAG-TO-REORDER — grab the ⋮⋮ handle next to a member\'s name and drag them up or down within their card. Reordering is per-discipline; placeholders stay put. Use it to group people by specialty.',
      '   • Team view scrolls now — the page-level overflow:hidden was hiding the dashboard cards below the team grid. Fixed.',
    ],
  },
  {
    version: '2.9.1',
    date: '2026-05-12',
    notes: [
      'Project stats popup pushed down so it sits BELOW the Gantt\'s date header (was overlapping the day numbers). Also hidden in Grid-only mode where there\'s no Gantt area to anchor to.',
    ],
  },
  {
    version: '2.9',
    date: '2026-05-12',
    notes: [
      'Team page reorganized:',
      '   • Discipline order now reads left-to-right: Project Management → Mechanical Engineers → Controls Engineers → Builders → Electricians. Same order propagates everywhere (Resources view tabs, dropdowns, etc.).',
      '   • All 5 cards live on ONE row at wide widths (5-column grid) so the Resources timeline and dashboard cards below stay visible without scrolling. Falls back to 3 columns under 1400px wide.',
      'CAPACITY STRIP added to each discipline card — shows headcount + scheduled hours + weeks of work at 90% efficiency. Hours calc: SUM(duration × 8 × allocation%) across every task currently assigned to a member of that discipline. Weeks: hours ÷ (real headcount × 40 × 90%). The "how many weeks until we run out of work" number you asked for.',
    ],
  },
  {
    version: '2.8',
    date: '2026-05-12',
    notes: [
      'Project stats popup added to the top-right of the Gantt — floats above the chart so you can see project health at a glance without scrolling back to the schedule header. Shows three rows:',
      '   • Duration — Receipt of PO → FAT in calendar weeks (rounded to the nearest whole week).',
      '   • Complete — labor-weighted % done.',
      '   • FAT — variance vs baseline (or "No baseline" when none is set).',
      'Popup is positioned in the schedule-split layer so it doesn\'t scroll with the Gantt content. Hidden on the All-projects view.',
    ],
  },
  {
    version: '2.7',
    date: '2026-05-12',
    notes: [
      'Fixed: baseline drift "−Nd" label was landing on top of anchor diamonds (FAT, Ship Machine, etc.). Drift label is now skipped for milestones — the ghost diamond/outline alone conveys the drift on those rows.',
      'PROJECT STATS in the schedule header — two new chips next to the project name:',
      '   • Labor-weighted % complete = SUM(duration × progress) / SUM(duration) across all non-milestone tasks. Blue.',
      '   • FAT vs baseline: "+Nd late" (red), "−Nd early" (indigo), "on baseline" (green), or "FAT — no baseline" (gray) when no baseline has been set.',
      'Project Management discipline added — Mike Gast, Xiaoli Liu, Karl Mansfield, Josh Belliveau now show up on the team page as a fifth discipline tab. They aren\'t offered in the task assignee dropdown (PMs don\'t typically own line items), but they\'re tracked alongside the rest of the team.',
      'DEPARTMENT LEAD flag — team members can now be marked as the lead of their discipline. Leads float to the top of each team card with a lime star, and the rest of the card members sort behind them. Hover any team member row to reveal a star button to toggle. Pre-marked the known leads: Mike Czenszak (Mech), Tim Wilmot (Controls), Mike Gast (PM), Dewayne Cantrell (Builders), Neil Simpson (Electricians).',
    ],
  },
  {
    version: '2.6',
    date: '2026-05-12',
    notes: [
      'Comments column added to the grid — free-text per task. Long notes truncate with an ellipsis; full content shown on hover. Toggleable via the columns menu.',
      'Fixed: when a task was both ahead-of-schedule AND its baseline-drift was negative (early), the "+Nd" schedule-status chip and the "−Nd" baseline-drift label landed on top of each other on the right side of the bar. Baseline drift label now always sits on the LEFT of the bar; schedule-status chip stays on the right. No more overlap.',
    ],
  },
  {
    version: '2.5.1',
    date: '2026-05-12',
    notes: [
      'Fixed priority numbering bug — a freshly-assigned task could land at priority 13 even when the person only had 2 tasks, because deletes and reassigns left gaps that "MAX + 1" then jumped over.',
      'Per-person priorities are now COMPACTED to a dense 1, 2, 3 ... sequence after every task create / update / delete. No gaps, no overlap, no inheriting old data.',
      'One-time startup migration cleaned up existing skewed priorities — every assignee\'s max priority now equals their task count.',
    ],
  },
  {
    version: '2.5',
    date: '2026-05-12',
    notes: [
      'TEAM PAGE DASHBOARD — three cards added below the Resources timeline, scoped to the currently selected discipline tab. Each discipline manager now has a focused at-a-glance view of their crew\'s state.',
      '   • Behind schedule — every task assigned to a team member of this discipline (incl. placeholders) that\'s behind the +/-Nd math, sorted most-behind first. Click a row to jump to it on the schedule.',
      '   • Coming due — tasks starting OR finishing within the next 14 business days for this discipline.',
      '   • Key dates — discipline-specific anchor across every project: Mechanical Engineers see Mech 1 Release dates, Controls Engineers see FAT, Builders see Ship Machine, Electricians see Machine Power-Up.',
      'Click any row in any card → switches to the Schedule view, activates that project\'s tab, and scrolls the row into the middle of the viewport.',
    ],
  },
  {
    version: '2.4.4',
    date: '2026-05-12',
    notes: [
      'Gantt scroll padding made symmetric — 6 months before the earliest task AND 6 months after the latest. Previously the pre-task pad was only 30 days, so on a project starting in May you could scroll way past Ship Machine but couldn\'t scroll back to April. Now you can park any edge bar near the middle of the viewport on either side.',
    ],
  },
  {
    version: '2.4.3',
    date: '2026-05-12',
    notes: [
      'Fixed: month names were getting stripped in Month view, leaving the date axis with just "2026" floating at the top and no monthly labels. The de-duplication that hides redundant month names in Week view shouldn\'t run in Month view (where the month IS the label).',
    ],
  },
  {
    version: '2.4.2',
    date: '2026-05-12',
    notes: [
      'Today line recolored to SDC blue (#1574C4).',
    ],
  },
  {
    version: '2.4',
    date: '2026-05-12',
    notes: [
      'Mech 1 Release promoted to a KEY MILESTONE (5th anchor) — lime concentric diamond, name chip in the grid, can\'t be deleted. Stays inside section 10 → Engineering → Mechanical Engineering where it belongs. Server migration backfills existing rows automatically.',
      'TODAY LINE on the Gantt — vertical dotted line at today\'s date spanning the chart, with a small "Today" label at the top. Easy at-a-glance "where are we relative to every bar?" reference. Hidden when today falls outside the chart\'s date range.',
    ],
  },
  {
    version: '2.3.1',
    date: '2026-05-12',
    notes: [
      'Dropped the "Other disciplines" group from the assignee dropdown — it would balloon the list with 80+ irrelevant people on a fully staffed team. If you need to assign cross-discipline, use the Resources view or move the task to the appropriate section first.',
      'One exception: if a task is currently assigned to someone from a discipline outside the filter (e.g. a builder on a procurement task that was set up that way), that person\'s discipline stays expanded so they don\'t vanish from the dropdown.',
    ],
  },
  {
    version: '2.3',
    date: '2026-05-12',
    notes: [
      'Assignee dropdown is now CONTEXT-AWARE — pick a task in CONTROLS ENGINEERING and you only see controls engineers; pick a task in BUILD and you only see builders; etc.',
      'Specific buckets (Mech / Controls / Build / Wire sub-departments) show ONLY their matching discipline.',
      'Ambiguous buckets — General Engineering, Section 40 Engineering, Section 50 Install Engineering — show ALL engineers. Same on the shop side.',
      'Legacy assignees not on the team list still appear under "Not on team" so old data stays editable.',
    ],
  },
  {
    version: '2.2',
    date: '2026-05-11',
    notes: [
      'Repo snapshot pushed to GitHub — everything from rev 1.7 through 2.1 is now on origin/main. Major chunks rolled up:',
      '   • Financial milestones — predecessor-triggered, full editor modal, vertical-line Gantt overlay, anchor-date sync.',
      '   • Gantt UX — drag-pan in both axes, no scrollbars, no page-level scroll, cross-panel row highlight on grid hover, 56px bottom-of-chart divider.',
      '   • Filters — popover reduced to quick chips (behind / ahead / milestones / assigned / over-allocated).',
      '   • Baseline — set / reset / clear, dashed-outline ghost in task colors, bare +Nd / −Nd drift labels, segmented toolbar control with dynamic Set / Reset label.',
      '   • Row-height toolbar control (− rows +), 14px minimum for very dense schedules.',
      '   • Anchor expansion to four (PO / Power-Up / FAT / Ship) with lime-green default.',
    ],
  },
  {
    version: '2.1',
    date: '2026-05-11',
    notes: [
      'Baseline toolbar control split into a segmented pair (like Critical / Only):',
      '   • LEFT button — labels itself "Set baseline" when no baseline exists for the active project, or "Reset baseline" when one is already set. So you can SEE at a glance whether the active project has been baselined.',
      '   • RIGHT button — "Baseline" toggle. Shows / hides the dashed overlay on the Gantt. Right-click → Clear baseline.',
      'Pin icon dropped — text-only labels.',
      'Set/Reset now prompts with a confirmation message tailored to whether you\'re snapshotting fresh or overwriting an existing baseline. First-time Set auto-enables the overlay so you immediately see the result.',
    ],
  },
  {
    version: '2.0.3',
    date: '2026-05-11',
    notes: [
      'Grid + Gantt bottom divider lines now align: the grid has the yellow "+ Add task" button between the last row and its bottom strip, so the Gantt\'s bottom strip gets a matching 57px of extra top margin to compensate. Both panels\' lines now sit at the same Y.',
    ],
  },
  {
    version: '2.0.2',
    date: '2026-05-11',
    notes: [
      'Baseline ghost outline now uses the SAME color family as the task itself — light fill shade for the dashed stroke. The overlay reads as a ghost of THAT specific task, not a generic purple bar.',
      'Drift display restored as a bare "+Nd" / "−Nd" label (no pill, no "vs baseline" wording) in the task\'s dark color with a white halo for legibility. Sits adjacent to the bar — late tasks have the label at the left edge of the bar, early tasks at the right edge.',
    ],
  },
  {
    version: '2.0.1',
    date: '2026-05-11',
    notes: [
      'Baseline overlay simplified — dropped the noisy per-task "+Nd vs baseline" chips. Fixed the off-by-one in the business-day drift math (a task at its baseline now correctly reads 0d, not 1d).',
    ],
  },
  {
    version: '2.0',
    date: '2026-05-11',
    notes: [
      'BASELINE FEATURE — snapshot a project\'s plan-of-record dates so subsequent edits can be compared against the original.',
      'New toolbar button: 📍 Baseline. Click to toggle the overlay on/off. Right-click for Set / Clear actions on the active project.',
      'Setting a baseline copies every task\'s current start/end dates into baseline_start_date / baseline_end_date. As you reschedule, the original positions stay put.',
      'When the overlay is on, each task that has drifted shows a DASHED PURPLE OUTLINE at its baseline date range, painted behind the live bar — so the visible parts of the ghost are exactly the portions the bar has shifted away from.',
      'A "+Nd vs baseline" / "−Nd vs baseline" chip (purple) floats just below each drifted bar so the magnitude of the slip is obvious at a glance.',
      'Re-set baseline (from the right-click menu) overwrites the existing snapshot — useful when you replan and want a fresh comparison point.',
      '',
      'Bottom of the schedule no longer reads as "cut off" — added 56px of empty space + a thin divider line below the last task on both the grid and Gantt panels. Aligned on both panels so they share a single visible bottom line.',
    ],
  },
  {
    version: '1.9.11',
    date: '2026-05-11',
    notes: [
      'Page-level (html/body) scrollbar removed — the document is pinned at 100% so no outer bar appears on the right edge.',
      'Scrollbar-hiding broadened with a universal selector inside the schedule split so every internal scrollable element (frappe-gantt\'s nested containers included) has its bars suppressed.',
      'Navigation now goes through drag-pan + mouse wheel only.',
    ],
  },
  {
    version: '1.9.10',
    date: '2026-05-11',
    notes: [
      'All scrollbars hidden on the grid + Gantt panels — drag-pan handles both axes.',
    ],
  },
  {
    version: '1.9.9',
    date: '2026-05-11',
    notes: [
      'Removed the vertical overscroll padding entirely — felt like too much empty space below the schedule.',
    ],
  },
  {
    version: '1.9.8',
    date: '2026-05-11',
    notes: [
      'Drag-pan no longer "moves the whole page" — main / #view-schedule now have overflow: hidden so drag stays contained to the inner panels.',
      'Horizontal scrollbar removed from the Gantt — drag-pan replaces it.',
    ],
  },
  {
    version: '1.9.7',
    date: '2026-05-11',
    notes: [
      'Financial overlay labels were STILL overlapping the date row because frappe-gantt\'s .upper-header / .lower-header are <g> groups, not <rect>s, so getAttribute("height") returned null and the fallback (38px) was way short of the real rendered header (~55px+ depending on zoom). Fixed by anchoring yTop to the topmost task bar\'s Y position — bars always start below the entire header, so this is robust to zoom/header-style changes.',
    ],
  },
  {
    version: '1.9.6',
    date: '2026-05-11',
    notes: [
      'Financial overlay labels were sitting on top of the date numbers because the header height calc only counted the upper "Month" header row. Bumped lane spacing too. (Iterated again in 1.9.7.)',
    ],
  },
  {
    version: '1.9.5',
    date: '2026-05-11',
    notes: [
      'Back to full-height financial overlay lines (top of chart to bottom of last task row). The "shorter lines anchored to trigger task" experiments didn\'t feel right.',
      'Lines are bolder: 1.6px dashed (pending) / 2.2px solid (sent), opacity 0.8 / 1.0. Reads cleanly over busy bars without overwhelming them.',
      'Labels now have a real white pill backdrop with a thin green border so they\'re readable wherever they land — including over task names like "Receipt of PO".',
      'When milestone dates are close together, labels stack vertically into 14px-tall lanes so they never sit on top of each other.',
    ],
  },
  {
    version: '1.9.4',
    date: '2026-05-11',
    notes: [
      'Financial overlay lines now extend ~70px above and ~30px below the strict overlapping range. The top extension lands the line in empty whitespace (no bars there at the milestone\'s X), giving the label a clean spot to sit without overlapping any task bar.',
      'Label is now anchored ~12px inside the top of the extended line — sits clearly in the open space, easy to read.',
    ],
  },
  {
    version: '1.9.3',
    date: '2026-05-11',
    notes: [
      'Financial overlay lines now span the Y range of every task that\'s actively in-flight at that milestone\'s date — so the line cuts through exactly the rows it crosses, no more no less.',
      'Label sits at the top of each line (just above the topmost overlapping task bar). Different milestones hit different sets of tasks → labels land at different vertical positions → no more crammed-together labels.',
      'Untriggered milestones (no date-overlapping tasks) fall back to a short tick at the date axis.',
    ],
  },
  {
    version: '1.9.2',
    date: '2026-05-11',
    notes: [
      'Financial overlay lines now STOP at their trigger task\'s row instead of running the full chart height. (Iterated again in 1.9.3.)',
    ],
  },
  {
    version: '1.9.1',
    date: '2026-05-11',
    notes: [
      'Financial overlay refined to look less aggressive: lines are thinner and lower-opacity (read as reference markers, not barriers). Hover the line and it pops to full opacity.',
      'Dropped the heavy green pill behind each label — labels are now plain green text with a thin white halo so they stay legible over busy task bars.',
      'When milestone dates are close together, labels stack vertically into lanes so they don\'t overlap each other.',
    ],
  },
  {
    version: '1.9',
    date: '2026-05-11',
    notes: [
      'Financial milestones now render on the Gantt as VERTICAL LINES through the chart at each derived date — much easier to see which tasks line up with which payment event than the old $ diamonds in the corner.',
      'Line is DASHED when the invoice hasn\'t been sent yet, and goes SOLID once you check the "Sent" box in the Financials modal.',
      'Each line carries a small green pill label at the top with the milestone name + percent (e.g. "Down Payment 30%").',
      '"Paid" column renamed to "Sent" — this app cares about invoice events, not the actual payment (that\'s accounting\'s job).',
    ],
  },
  {
    version: '1.8.1',
    date: '2026-05-11',
    notes: [
      '"Show on Gantt" checkbox added to the Financial milestones modal head. Toggling it overlays the milestones on the Gantt as green $ diamonds (same flag as the toolbar $ Financial button — just discoverable from inside the modal too).',
    ],
  },
  {
    version: '1.8',
    date: '2026-05-11',
    notes: [
      'Financial milestones are now event-triggered instead of being fixed dates. New "Trigger" column accepts predecessor syntax — anchor name aliases (PO / Power-Up / FAT / Ship) or task line numbers, with optional lag like "+1w" or "-3d". Example: "FAT +1w" = one week after FAT.',
      'When a Trigger is set, the Date column shows the auto-derived date (read-only). Clear the Trigger to enter a fixed date manually.',
      'Standard seeded milestones now come pre-wired: Down Payment → PO, Acceptance at SDC → FAT, Acceptance at Customer → Ship. Edit FAT in the schedule and the SDC acceptance date follows automatically.',
      'Gantt overlay now uses the derived trigger date too, so the $ diamonds reposition live when you edit the anchors / triggering tasks.',
      'Migration on server startup converts legacy sync_to_anchor values to the new predecessor format (idempotent).',
    ],
  },
  {
    version: '1.7.2',
    date: '2026-05-11',
    notes: [
      'Financials modal slimmed down: Name / Due date / % / Paid / (delete). Dropped the Project value field, the totals row, the Amount ($) column, and the Sync to column — only the four columns you actually edit remain.',
      'Bug fix: default_financial_milestones setting had been overwritten with an empty array (from an early Setup save before the defaults were seeded). Added an idempotent migration on server startup that restores it (and project_milestone_library) to its standard defaults if empty.',
      'Each project now correctly seeds its four standard financial milestones (Down Payment 30% / Major Commercials 40% / Acceptance at SDC FAT 20% / Acceptance at Customer SAT 10%) on first open of the modal.',
      'Acceptance at SDC FAT date still auto-syncs to the FAT anchor under the hood — that wiring lives in the data layer, no longer exposed in the modal UI.',
    ],
  },
  {
    version: '1.7.1',
    date: '2026-05-11',
    notes: [
      'Reverted financials editor back to a centered modal popup (the inline panel didn\'t feel right).',
      '$ Financials header button now matches the other toolbar buttons — plain white with a thin border, no more lime green.',
      '+ Add milestone no longer uses a native browser prompt (that ugly little dialog at the top of the window). Instead it drops an empty row at the bottom of the table and focuses the Name cell so you can type immediately.',
    ],
  },
  {
    version: '1.7',
    date: '2026-05-11',
    notes: [
      'Financials editor moved out of the modal popup and into an inline panel that slides down between the schedule header and the task grid. (Reverted in 1.7.1.)',
    ],
  },
  {
    version: '1.6',
    date: '2026-05-11',
    notes: [
      'Filters reworked: popover is now a row of quick-chip toggles — Behind schedule / Ahead of schedule / Milestones only / Assigned (real work) / Over-allocated. One click per chip; click again to turn off. Anchors (PO / Power-Up / FAT / Ship) are always exempt so the spine stays visible.',
      '"Behind" / "Ahead" use the same business-day math as the ±Nd chips on the Gantt — they\'re in sync.',
      '"$ Financials" button moved from the project tab to the schedule header (next to the project name + task count) so it\'s easier to spot. Click opens the per-project Financial Milestones editor. Right-click → Financial milestones… on the project tab still works as a backup.',
      'Financials modal now shows synchronous content immediately. If the API isn\'t reachable (e.g. the server hasn\'t been restarted with the new financials routes), the modal shows a clear "restart the server" message instead of a confusing dark backdrop with nothing in it.',
      'Row-height control compacted: just `−` `rows` `+` — no percentage label, smaller buttons. Frees up ~40px of toolbar space.',
      'Filters popover reparented to <body> so it paints above the project-tab bar (same stacking-context fix that was applied to the revision popover earlier).',
    ],
  },
  {
    version: '1.5',
    date: '2026-05-11',
    notes: [
      'Cross-panel row highlight — hovering a row in the grid now lights up the matching row across the Gantt with a continuous stripe. Makes it easy to spot which bar corresponds to which task without squinting at row alignment. Stripe disappears when the mouse leaves the grid.',
    ],
  },
  {
    version: '1.4',
    date: '2026-05-11',
    notes: [
      'Row height control moved from Setup to the schedule toolbar — sits next to the date zoom as a − / rows / + segmented control. Plus = taller rows (more breathing room), minus = shorter rows (more on screen). Each click changes by 2px. Persists per-browser.',
      'Row-height minimum lowered from 22px to 14px — tight, but lets you cram a 60-row schedule into one viewport when scanning.',
      'The bottom drag handle still works for fine-grained adjustment; the toolbar control and the drag handle stay in sync.',
      'Setup → Display card removed (it only held the row-height slider, which is now in the toolbar).',
    ],
  },
  {
    version: '1.3',
    date: '2026-05-11',
    notes: [
      'Anchor rows now have a heavy top/bottom border + slight background tint so they pop off the rows above and below. Fixes the "FAT looks like a SHOP task" confusion — the visual break makes it obvious FAT is a section-level gate, not another dept row.',
      'The colored name chip on anchor rows is slightly larger with a subtle drop shadow.',
      'Gantt + grid now have 50vh of overscroll at the bottom — you can drag/scroll PAST the last row to bring the bottom rows to the middle of the viewport instead of having them pinned to the very bottom. Same applies to vertical drag in either panel.',
    ],
  },
  {
    version: '1.2.1',
    date: '2026-05-11',
    notes: [
      'Fixed Receipt of PO double-rendering: stale phase_group=teardown_install on the PO anchor (left over from an earlier section-50 split migration) was causing it to appear both on the spine AND inside the teardown bucket. Bucket walk now matches anchors explicitly by anchor_key — only Machine Power-Up is admitted, regardless of stored phase_group. DB migration on next server restart clears the stale fields on PO / FAT / Ship Machine.',
    ],
  },
  {
    version: '1.2',
    date: '2026-05-11',
    notes: [
      'Machine Power-Up relocated from the top of section 40 to section 10 → Shop → Wire — it\'s the END of the wire team\'s work (the moment the panel turns on), not the start of testing. Still rendered with anchor styling (concentric diamond, name chip in the grid) so it reads as a key date.',
      'Existing Machine Power-Up rows are auto-migrated to the new placement on server restart.',
      'Per-project $ icon on every project tab — click it to open the Financial Milestones editor for that project. Replaces the harder-to-find "right-click → Financial milestones…" path (the right-click option still works as a backup).',
    ],
  },
  {
    version: '1.1',
    date: '2026-05-11',
    notes: [
      'Anchor milestones expanded from 3 to 4: Receipt of PO → Machine Power-Up → FAT → Ship Machine. Machine Power-Up auto-creates for every project and opens section 40.',
      'Default anchor color changed to SDC lime (#BEFA4F) with dark slate text — replaces the bolder green. Still configurable in Setup → Anchor Color.',
      'Non-anchor milestones (Mech Release, Design Review, First Part Full Auto, etc.) render as a solid slate diamond on the Gantt — clearly secondary to the lime concentric anchors.',
      'Financial milestones: per-project payment events (Down Payment 30% / Major Commercials 40% / Acceptance at SDC FAT 20% / Acceptance at Customer SAT 10%). Defaults auto-seed on first use, then fully editable per project.',
      'Right-click any project tab → "Financial milestones…" opens an editable table per project: name / due date / % / amount / sync-to-anchor / paid.',
      '$ Financial toolbar toggle overlays the financial milestones on the Gantt as green $ diamonds along the top band — diamond fills when paid.',
      'FAT-synced financial milestones (e.g. Acceptance at SDC) auto-mirror the FAT anchor\'s date — edit FAT in the grid and the financial marker follows.',
      'Setup → Standard Financial Milestones — edit the default name/percent/anchor-sync list that gets applied to new projects.',
      'Setup → Standard Project Milestones — library of common in-flow milestones (Mech Release 1/2, Design Review, Order Long Lead, Order Commercial Parts, First Part Full Auto).',
    ],
  },
  {
    version: '1.0',
    date: '2026-05-10',
    notes: [
      'Initial revision baseline.',
      'Schedule grid + Gantt with hierarchical sections (10 Design & Build, 40 Machine Testing, 50 Teardown & Install) and configurable section/department colors.',
      'Three hard-coded anchor milestones per project: Receipt of PO, FAT, Ship Machine — concentric red diamonds, configurable color.',
      'Business-day scheduling (Mon–Fri). Duration is the source of truth; end dates derive automatically. Default allocation 90%.',
      'Critical path mode with a linked "Only" sub-toggle to filter the view to just the project-driving chain.',
      'Ahead/behind chips on Gantt bars: green +Nd / red −Nd vs expected progress at today\'s date.',
      'Resources view per-discipline. Placeholder team members (anything named "X Placeholder") get role-stand-in treatment — no over-allocation warnings, no load strip, no priority pills; bars carry through duplicates so new projects start pre-staffed by role.',
      'Click any resource bar (placeholder OR real person) → menu to reassign across the team or jump to the task on the schedule.',
      'Priority pill on overlapping tasks is click-to-cycle (1 → 2 → … → N → 1). Priorities are per-person.',
      'Pane segmented control: Grid / Both / Gantt — auto-fits the Gantt when switching to a Gantt-visible mode.',
      'Project tabs with right-click menu: rename, duplicate, merge, mark as template (★, can\'t be closed), delete.',
      'Filters consolidated into a single topbar dropdown.',
      'Bar labels stay inside bars (binary-search-truncated with hover tooltip). Milestone labels are plain text to the right of the diamond, no pills.',
      'Arrow routing: at most two segments, no U-routes, arrows render behind bars via z-order.',
    ],
  },
];
